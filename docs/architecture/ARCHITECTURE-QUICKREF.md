# Architecture Quick Reference

**Read this file first.** Consult the longer documents linked at the bottom only when you need deeper rationale than a bullet here provides. This file must be updated after every architecture or migration phase — see the update rule in `CLAUDE.md`.

## 1. Current project state

- The game is **Chronicle**, an AP U.S. History RPG. "Republic Builder Engine" is retired as the project's identity; the eventual multi-subject platform has no final name yet.
- The playable vertical slice is Unit 1 / Case 1.01 ("The Atlantic Crossroads"), fully playable end-to-end with all three sequential cases (case-001 through case-003 all have real, cited historical content). Unit 2 ("Colonial Crossroads," case-004 through case-006 plus unit review) is also complete with real content as of 2026-07-14.
- The entire running game is one file, `apps/web/src/main.js` (8,038 lines as of Phase 36 — re-check with a quick line count before citing it if much time has passed, don't trust a stale figure; Phase 33's error-handling consolidation was the first phase in this run to net-remove lines rather than add them) — no framework, no React/Vue/Phaser. Procedural Web Audio lives in `apps/web/src/engine/audio-engine.js`; real-accounts/classroom/submission/grading logic lives in `apps/web/src/repositories/` (`remote-*`/`progress-repository.js`/`teacher-override-repository.js`) and `apps/web/src/engine/` (`auth-flows.js`, `evaluator-requests.js`, `evaluator-client.js`, `custom-content-authoring.js`).

## 2. Current branch / migration context

- Working branch: `platform-architecture-refactor`.
- This document sits at the end of a documentation-first architecture pass: repository audit → tooling audit → platform proposal → skeptical simplification review → this housekeeping pass.
- No application code has been changed by any step in that pass. The codebase today is byte-for-byte the same game it was before the architecture review started.

## 3. Current approved architecture

- Keep working code where it lives (`main.js`, `content/*.js`); wrap it thinly rather than moving it.
- Add `export` to specific pure functions worth unit-testing (collision math, badge logic, save-merge logic) and test them **in place** — no physical extraction of movement/collision/camera/NPC code.
- The long-term domain map (`PlatformCore`, `ContentRegistry`, `WorldComposition`, `QuestEngine`, `WorldRuntime`, `packs/<subject>/`) is real and documented, but is **future direction, not current structure** — see `PLATFORM-ARCHITECTURE-PROPOSAL.md`.

## 4. Work completed so far

- Repository audit, third-party tooling audit, platform architecture proposal, and a skeptical simplification review — all four documents exist under `docs/architecture/`.
- This housekeeping pass: corrected `CLAUDE.md`'s stale claims, repaired 4 confirmed placeholder-stub docs (see §11), repaired decision-log numbering (duplicate `0006` → `0006`/`0006a`; missing `0020` backfilled from an existing milestone doc), and created this file.
- Phase 2: added Vitest (+ jsdom for `main.js`'s DOM-dependent module scope), `export`ed six existing `main.js` functions/state (`ellipse`, `isCaribbeanLand`, `rectsOverlap`, `footBoxFor`, `badgeRecordsForUnit`, `unlockNext`, `progress`) in place, added a narrow `if (app)` boot guard so importing `main.js` no longer boots the live app as a side effect, and added 24 tests across 3 files under `tests/unit/`. Full writeup: `docs/development/UNIT-TESTING.md`.
- Phase 3: added Zod, wrote 6 schema files under `apps/web/src/content/schemas/` validating `unit-01-campaign.js`/`unit-02-campaign.js` in place (ids, titles, unit/case/source/mcq/saq structure, answer-index bounds, and cross-references like empire connections → evidence ids, triangle cargo → leg ids, region evidence → region ids), created `apps/web/src/repositories/local-content-repository.js` (one real caller: the validation script), and made `scripts/validate-content.js` a real Zod-backed validator with a global case/source id-uniqueness check across both units. All 17 content groups pass against real content today — no corrections were needed. Added 28 tests across 2 new files under `tests/unit/`. Full writeup: `docs/content/CONTENT-VALIDATION.md`.
- Phase 4: created `apps/web/src/repositories/local-progress-repository.js`, a thin wrapper around `chronicle-progress-store.js` (untouched, not moved or rewritten). Actual direct call-site count in `main.js` was **8**, not the ~4 originally estimated (`readProgress` ×1, `saveProgress` ×2 direct + ×1 via the `save()` wrapper ~60+ sites use, `resetProgress` ×3, `hasSavedProgress` ×1) — corrected and documented. Only the import statement and the one `readProgress()`→`loadProgress()` call site needed edits; `saveProgress`/`resetProgress`/`hasSavedProgress` kept identical names so every other call site (including all ~60+ indirect `save()` calls) needed no changes. Added a minimal additive `schemaVersion` field + `migrateProgress()` guard at the repository layer (store's data shape unaffected); verified old saves without `schemaVersion` still load correctly with nothing dropped. Added 10 tests in a new file under `tests/unit/`. Full writeup: `docs/architecture/LOCAL-PROGRESS-REPOSITORY.md`.
- Phase 5 (dead-code removal): re-verified every candidate from `CURRENT-REPOSITORY-AUDIT.md`/`ARCHITECTURE-REVIEW-AND-SIMPLIFICATION.md` against the current import graph (all still zero-risk — nothing had gained a caller since the audit), then deleted: the orphaned `apps/web/src/features/*` island (6 files + its 5 empty `.gitkeep` folders), `apps/web/src/content/chronicle-case-001.js`, `apps/web/src/content/cases/case-atlantic-crossroads.preview.js` (the island's sole content dependency), `apps/web/src/engine/content/author-content-store.js` (the island's sole store dependency), `apps/web/src/engine/player/player-profile-store.js`, the dormant `content/campaigns/` + `content/library/` JSON pipeline, and the placeholder-only root `assets/` tree. Updated `README.md`'s canonical-homes table, `docs/content-guide/naming-and-placement.md`, `.claude/skills/chronicle-institute-conventions/SKILL.md`, `docs/content/CONTENT-VALIDATION.md`, and added a historical note to decision-log `0005` to stop pointing at deleted paths. Left historical audit/proposal/review documents and milestone docs unedited (they're point-in-time records, not living references). Full writeup: `docs/migrations/DEAD-CODE-REMOVAL.md`.
- Phase 6 (minimal Author Mode persistence fix): confirmed the two broken fields are exactly `main.js`'s `authorPanel()` `[data-copy="unit-title"]`/`[data-copy="unit-question"]` inputs (backing `UNIT_01.title`/`UNIT_01.centralQuestion`) — `data-profile="name"` was already working and untouched. Created `apps/web/src/repositories/local-teacher-override-store.js` (storage key `republic-builder.chronicle.teacher-overrides.v1`, a flat `{ [contentId]: { [fieldName]: value } }` patch blob, Zod-validated per-entry so malformed data can't wipe unrelated overrides). Added `resolvedUnitTitle()`/`resolvedUnitCentralQuestion()` wrappers in `main.js` and used them everywhere the unit title/central question already render to students (institute hub line, archive atlas label, review/completion headings) plus one new "Guiding question" line in `archiveScreen()` — the central question had no student-facing display before this phase, so the fix's own required behavior ("show the override where it's displayed") needed one minimal new line, not a new editable field. Added a single combined "Reset content overrides to official text" button (shown only when an override exists) plus small "edited" flags in the panel, since no reset control existed. Kept the "Author Mode" label unchanged. `export`ed `resolvedUnitTitle`/`resolvedUnitCentralQuestion` from `main.js` (same in-place-export pattern as Phase 2) and added 18 tests across two new files, `tests/unit/local-teacher-override-store.test.js` (the store in isolation) and `tests/unit/main-teacher-overrides.test.js` (the `main.js` wiring). `npm run test` (81/81 passing), `npm run validate:content`, `npm run build`, and `npm run lint` (same one pre-existing unrelated error as before, no new errors) all pass. Full writeup: `docs/teacher-mode/MINIMAL-LOCAL-OVERRIDES.md`.
- Phase 7 (post-minimal-architecture reassessment): re-verified all 6 phases above live (`npm run test` 81/81, `npm run build` 90 modules, `npm run validate:content` 17/17, `npm run lint` unchanged from baseline, `apps/web/src/features/` confirmed actually deleted, all 3 repository files confirmed present) and searched for concrete forcing-function evidence across Phaser/Tiled, Playwright, inkjs, subject-pack extraction, PlatformCore/accounts/classrooms, and WorldComposition/AI generation — found none in any category. Flagged (not fixed, no code changed) a small set of pre-existing, non-blocking defects: 7 long-standing ESLint warnings in `main.js` including two suspicious defined-but-never-called functions (`moveInstitutePlayer`, `moveFieldPlayer`) worth a name-check before movement code is next touched, and two stale doc figures (`UNIT-TESTING.md`'s test count, `CLAUDE.md`'s `main.js` line count — actual is now 2,964 lines / 81 tests across 8 files). Recommended the next milestone be product work (expand real Unit 1 content into the next badge area) rather than any new architecture phase. Full writeup: `docs/architecture/POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md`.
- Phase 8 (quest-type architecture): a deliberate, explicit owner-directed override of Phase 7's hold-pattern (see §5) — not a rediscovered forcing function. Built exactly two quest types: `apps/web/src/quest-types/generic/mcq-quest.js` (extends the newly-exported `McqQuestionSchema` from `review.schema.js`) and `apps/web/src/quest-types/history/evidence-organizing-quest.js` (generalizes the `TRIANGLE_CARGO`/`REGION_EVIDENCE` factory-schema pattern), plus a two-entry lookup object at `apps/web/src/quest-types/index.js` (not a registry). Added first real content for Case 1.01 at `apps/web/src/content/quests/unit-01-quests.js` (3 MCQ items + 1 evidence-organizing quest, grounded in the real `CASE_001_SOURCES`). Wired into `local-content-repository.js`/`scripts/validate-content.js` (19/19 groups pass, was 17). Added 27 tests across 2 new files (`npm run test` 108/108, was 81). `npm run lint`/`npm run build` unchanged from baseline. **Important: `main.js` was not touched — nothing renders this content in the running game yet.** This was a deliberate scope boundary for this pass (see full writeup), not an oversight, but it means the new code is currently unreached at runtime, structurally similar to the now-deleted `features/` island until a screen is wired to it. Full writeup, including the explicit flag and recommended next step: `docs/architecture/QUEST-TYPE-ARCHITECTURE.md`.
- Phase 9 (2026-07-11, quest-type expansion + mini-games): a same-day, owner-directed follow-up to Phase 8, executed overnight with full autonomy per an explicit standing prompt. First stood up the 6-agent project subagent team (`content-designer`, `map-implementer`, `content-validator`, `test-writer`, `code-reviewer`, `doc-sync`) in `.claude/agents/` — all six already existed from setup, verified to match spec, no drift. Added 2 more quest types to `quest-types/`: `generic/sequencing-quest.js` (all-or-nothing chronological-reasoning scoring) and `history/source-analysis-quest.js` (the HIPP quest type — binary per-dimension scoring, schema-enforced identification-only distractor requirement, matching the real DBQ sourcing-point rubric). `QUEST_TYPES` lookup now has 4 entries (`mcq`, `sequencing`, `evidence-organizing`, `hipp`). Added real Case 1.01 content for both (`UNIT_01_SEQUENCING_QUESTS`, `UNIT_01_SOURCE_ANALYSIS_QUESTS`), wired into `local-content-repository.js`/`scripts/validate-content.js` (21/21 groups pass, was 19). Added 71 tests across 4 new files (`npm run test` 179/179, was 108). Built a separate, explicitly non-rubric-scored `apps/web/src/mini-games/` layer (flat, no subfolders): `cargo-sorting.js` and `storm-navigation.js`, both pure-logic-plus-render modules with their own tests. Built two dev-only preview harnesses (`apps/web/quest-type-preview.html`, `apps/web/mini-games-preview.html`, same non-build-entry pattern as the existing `tiled-preview.html`) and ran scripted Playwright passes confirming real browser rendering and click-driven interaction for all 4 quest types and both mini-games — not just Vitest passing. `npm run lint`/`npm run build` unchanged from baseline. **`main.js` still not touched** — same flagged "not yet wired" status as Phase 8, now covering 4 quest types instead of 2. Full writeup: `docs/architecture/QUEST-TYPE-ARCHITECTURE.md` (updated in place) and `docs/architecture/session-reports/2026-07-11-overnight-quest-types-and-minigames.md`.
- Phase 10 (2026-07-12, first live quest-type wiring): closed the "not yet wired" flag from Phases 8–9 for one quest type. Added a new `practice-check` screen to `main.js` (`VALID_SCREENS`, the render switch, a `practiceCheckScreen()` function) reachable from a new "Practice Check →" button in the Case 1.01 field's existing Evidence Channel aside panel — no field proximity/collision/movement code touched, since that panel is plain UI chrome, not a physical field object. Renders `UNIT_01_MCQ_QUESTS` via `renderQuest("mcq", ...)`/`gradeQuest("mcq", ...)` from `quest-types/index.js`. Added a `questResponses: {}` field to `DEFAULT_PROGRESS` in `chronicle-progress-store.js` (flat `{ [questId]: { selected } }` map, merged in `readProgress()` per the store's existing pattern) plus a `[data-mcq-quest]` branch in `main.js`'s existing `change`-event listener (radio inputs from `renderMcqQuest()` don't carry `data-action`, so this is a small addition alongside the existing `[data-profile]`/`[data-copy]` branches, not a new dispatch mechanism). Added `.quest-choices .choice`/`.quest-prompt`/`.quest-practice-item` rules to `global.css` (the shared `.choice` class's 3-column grid was built for `reviewScreen()`'s letter-badge layout and doesn't fit `renderMcqQuest()`'s 2-child markup, so this scopes an override under `.quest-choices` rather than changing the shared rule). Only **MCQ** was wired — Sequencing, Evidence Organizing, and HIPP remain built-and-tested-but-unreached, same as before. Verified with a scripted Playwright pass against the real dev server (not the preview harness): navigation, all 3 real MCQ items rendering, correct/incorrect grading against real `explanation` text, the answered counter, **localStorage persistence across a full page reload**, and the back-link all confirmed working. `npm run test` 179/179 (no new tests added — no exported pure logic was introduced, matching the existing convention that screen-assembly functions like `villageActivityScreen()`/`columbusActivityScreen()` aren't unit-tested either), `npm run validate:content` 21/21, `npm run lint` unchanged baseline (1 pre-existing error, 7 pre-existing warnings, none new), `npm run build` succeeds with a larger bundle (275 KB vs 243 KB) confirming `quest-types`/`content/quests` are now actually imported by `main.js`.
- Phase 11 (2026-07-12, same day, remaining three quest types wired): closed the rest of the Phase 8/9 "not yet wired" flag. Owner asked explicitly to be able to see all the built quest types in-game, "even as test features." Expanded `practiceCheckScreen()` into 4 sections (Multiple choice / Sequencing / Evidence organizing / HIPP source analysis) inside the same `practice-check` screen from Phase 10 — one entry point, no new button/screen sprawl. Rendered `UNIT_01_SEQUENCING_QUESTS`, `UNIT_01_EVIDENCE_ORGANIZING_QUESTS`, and `UNIT_01_SOURCE_ANALYSIS_QUESTS` via the existing `renderQuest`/`gradeQuest` lookup — no new quest-type code, this phase is pure wiring. Sequencing and Evidence Organizing needed real drag-and-drop, so extended `main.js`'s existing native-HTML5-DnD `dragstart`/`dragover`/`dragleave`/`drop` listeners (already handling `[data-map-piece]`, `[data-cargo-card]`, `[data-region-card]`, `[data-empire-card]`) with two more branches each, following the same `.closest()`-plus-`dataset`-plus-`is-over`-class pattern already established for the map-piece/triangle-leg/region-column drop zones — no new event-handling mechanism introduced. Sequencing reorders by reading the live DOM order of `[data-sequence-item]` siblings on drop and writing the new order into `questResponses[questId].order`; Evidence Organizing writes `questResponses[questId].placements[sourceId] = slotId` (clearing any prior source occupying that slot first, mirroring the existing map-piece placement logic) and a `[data-evidence-reflection]` branch was added to the `change` listener for the optional reflection textarea. HIPP needed only a `[data-hipp-option]` branch in the same `change` listener (radio inputs, same shape as the Phase 10 MCQ branch), writing `questResponses[questId].selected[promptId] = optionId`. No changes to `DEFAULT_PROGRESS`/`readProgress()` — the Phase 10 `questResponses: {}` flat map already covers arbitrary per-quest-type state shapes without a store change. Added CSS for `.quest-section-heading`, `.sequence-item`/`.quest-sequence-list`, `.evidence-card`/`.quest-evidence-sources`/`.evidence-slot`/`.quest-evidence-slots`/`.quest-reflection`, and `.quest-document`/`.quest-hipp-prompts`/`.hipp-prompt`/`.hipp-option`, plus `.is-over` variants for the two new drop targets, following the existing `.leg-drop`/`.region-column` visual pattern. Verified with a scripted Playwright pass against the real dev server: all 4 sections render; MCQ selection grades live; a native-event-dispatch drag from the first sequence item onto the last reorders the list and grades live; a native-event-dispatch drag from the first evidence source onto the first slot fills it and grades live; a HIPP radio selection grades live (1/2 points on the seeded case); **all 4 quest types' state persists correctly across a full page reload** (verified via reading the raw `questResponses` blob back out of `localStorage`); back-link returns to the field. `npm run test` 179/179 (unchanged — pure wiring, no new pure logic to test), `npm run validate:content` 21/21, `npm run lint` unchanged baseline (same 1 pre-existing error confirmed pre-existing via `git stash` diff, 7 pre-existing warnings, none new), `npm run build` succeeds.
- Phase 12 (2026-07-13, Practice Check UI polish + four-Cs taxonomy + Caribbean Tiled rebuild): three owner-directed product passes in one session, not a new architecture direction. **(a) Practice Check UI/UX**: added a screen-level completion summary, per-card unanswered/in-progress/correct/incorrect/partial states, keyboard-operable alternatives to drag-and-drop (sequence move buttons; an evidence "place in" `<select>`) wired through shared `applySequenceOrder`/`applySequenceMove`/`applyEvidencePlacement` helpers reused by the existing drop handlers, a distinguishable partial-credit state for HIPP scoring, a live reflection character counter, `aria-live` on feedback, and fixed a real pre-existing bug where the legacy 3-column `.choice span` rule (sized for a letter-badge layout) was squeezing Practice Check MCQ answer text into a 22px box. **(b) Four-Cs taxonomy**: formalized `evidence-organizing-quest.js`'s free-text `skillCategory` into a real `SKILL_CATEGORIES` enum (Comparison, Causation, Continuity and Change, Contextualization, Sourcing), retagged the one real Unit 1 evidence-organizing quest, and split `api/_lib/rubrics.js`'s single LEQ/DBQ "reasoning" rubric row into three diagnostic sub-signals (`reasoning-comparison`/`-causation`/`-ccot`, still one point total) plus gave the written-response output schema a real per-task-type `row` enum where none existed before. Added `docs/content-guide/skill-taxonomy.md`. **(c) Caribbean Tiled rebuild**: replaced Unit 1's 100%-CSS-drawn field with a real `.tmj` tile composite on the **Island survival** tileset (superseding an unwired Medieval-Harbor-based prototype that lacked tropical huts/palms/campfire tiles entirely) — see `docs/decision-log/0029-caribbean-tiled-rebuild.md` for the full rationale, including why the ground layer is generated by reusing `isCaribbeanLand`'s exact ellipse formula (so tile art and collision can't drift apart) and why the Spanish ship stays a CSS overlay. All three passes verified with scripted Playwright runs against the real dev server (not just `npm run build`/`lint`/`test`), including a collision check confirming the player stops cleanly at the new tile art's sand/water boundary. `npm run test` 179/179 (2 fixture values updated for the new enum, no new tests needed — no new pure logic), `npm run validate:content`/`lint`/`build` all pass.
- Phase 13 (2026-07-14, scoped main.js modularization pass): owner-directed, driven by a bloat concern ("I don't want files to get bloated... lean and copyable when I go to generate new games"), not a reversal of §3's no-extraction policy. Extracted exactly two new files, both following the existing `engine/tiled-map-loader.js` precedent (small, single-purpose, scope-boundary doc comment, flat named exports, pure computation kept separate from any side-effecting wrapper): `apps/web/src/engine/geometry.js` (`ellipse`/`rectsOverlap`/`footBoxFor` — pure, zero-DOM, already-exported-for-testing) and `apps/web/src/engine/audio-engine.js` (the full procedural Web Audio engine — genuinely self-contained WebAudio usage; `updateMusicForScreen`/`toggleAudio` now take an already-resolved `scene` string as a parameter instead of calling `sceneForMusic()` internally, since that function reads `progress`/screen-routing state and deliberately stayed in `main.js`). Two misplaced quest-state mutators (`applySequenceOrder`/`applySequenceMove`/`applyEvidencePlacement`) that had been wedged inside the old audio block by mistake were relocated to `main.js`'s existing progress-mutation-helper neighborhood, not extracted anywhere. Separately, decomposed the `handleAppClick`/`handleAppMousedown` ~570-line, ~50-branch inline dispatcher (the exact hotspot `FILE-AUDIT-2026-07.md` §7 item 4 flagged) into 7 named, in-place group functions (`handleChromeClick`, `handleOnboardingClick`, `handleHubClick`, `handleFieldClick`, `handleSourceReaderClick`, `handlePuzzleScreenClick`, `handleReviewClick`) behind a thin first-match-wins dispatcher loop — verified every one of the 52 distinct `action` strings is mutually exclusive before the split, making the regrouping provably zero-behavior-change (the only intentional change: ~17 branches that used to fall through without an explicit `return` now `return true;`, which changes nothing observable). Field/hub movement, DOM-touching collision, camera, and NPC-patrol DOM-writing code were explicitly left untouched, per §3. `main.js` dropped from 3,298 to 3,181 lines (net of the two extractions plus the group-function boilerplate the split added). Added 5 new tests in `tests/unit/engine-audio-engine.test.js` (the audio engine had zero coverage before; jsdom has no real `AudioContext`, so only the no-`AudioContext`-needed paths are covered — `isAudioEnabled()`, the off-transition of `toggleAudio`, `stopMusic()` on an empty list, and disabled-state no-op guards). `npm run test` 184/184 (was 179), `npm run lint`/`npm run build`/`npm run validate:content` unchanged from baseline. Verified live in the browser via a scripted Playwright pass against the real dev server: full onboarding→identity→institute flow, bidirectional audio toggle with correct `is-on` class and localStorage persistence across a reload on two different screens, proximity-gated Navigation Table interaction (blocked while far, succeeds once near), field screen render, NPC-dialogue proximity gating, and live player movement/collision — zero console errors throughout.
- Phase 14 (2026-07-15, Institute Archive Room — shell + Tiled interior): added a second walkable hub room reached via a new door in the Main Hall (`ARCHIVE_ROOM_GRID`/`ARCHIVE_ROOM_BLOCK_RECTS`/`ARCHIVE_ROOM_TARGETS`, generalized the same way `FIELD_MAPS`/`activeFieldMap()` already generalizes field exploration, via new `activeHubGrid()`/`activeHubBlocks()`/`activeHubTargets()` accessors), with one interactable (an Archive Terminal showing a placeholder "still being cataloged" line — no Archive Challenge content or data-model wiring yet). Same-session follow-up gave the room a real Tiled `.tmj` interior (`scripts/generate-archive-room-tmj.js`, the **Medieval Tavern** pack — the same proven 48px/16-column tile family as Riverbend/Caribbean, chosen over the unused `Modern Interiors` pack after direct pixel inspection found it has no uniform tile grid and its office/monitor furniture would clash with the project's gold/bronze/parchment visual language) replacing the room's previous plain-navy CSS fallback background. `ARCHIVE_ROOM_BLOCK_RECTS` was re-measured against the generated art. A real bug — a collision rect overlapping the room's own spawn point, freezing all movement — was caught only by a scripted Playwright walkthrough, not by `test`/`build`/`lint`/`validate:content` (all passed the whole time). Full writeup: `docs/decision-log/0030-archive-room-tiled-interior.md`. This is Phase 0 of a larger, not-yet-implemented plan (Investigation Challenges / Archive Challenges reorganizing the Navigation Table and quest system) — see `/Users/justinramsey/.claude/plans/yes-plan-the-look-snazzy-wozniak.md` for the full plan; only the Archive Room's shell and art are done, no schema/content/navigation-table work from that plan has shipped yet.
- Phase 15 (2026-07-15, Investigation/Archive Challenge cross-reference validation — plan Phase 1): added `checkChallengeReferences()` to `apps/web/src/content/schemas/cross-reference.js`, checking `case.archiveChallenge.questType`/`.questId` and `source.investigationMode`/`.investigationQuestId` pointers (schema fields added earlier but never validated) against the real `QUEST_TYPES` keys and each type's actual quest content — a check Zod alone can't express, since a Zod schema can't see application code, which is why these fields are typed as plain strings rather than enums. `scripts/validate-content.js` now builds a `questsByType` map (quest id sets per `QUEST_TYPES` key, across all three units) and runs two new cross-file groups against it (34 groups checked, was 32). Verified the check actually fires, not just that it compiles clean against content that doesn't use these fields yet: temporarily injected a dangling `questId` into case-006, confirmed `validate:content` failed with a clear message, then reverted (confirmed byte-identical to HEAD via `git diff`). Added 7 tests in `tests/unit/content-cross-reference.test.js`. `npm run test` 216/216, `npm run lint`/`npm run build` unchanged from baseline. Still Phase 1 of the same not-yet-fully-implemented plan referenced in Phase 14 — no case or source actually sets these fields yet.
- Phase 16 (2026-07-15, Archive Challenge quest types + case-006 migration — plan Phase 2): migrated case-006's ("Charter & Compact") bespoke `regionsScreen()` drag-and-drop activity onto the generic `evidence-organizing` quest-type contract, reachable from the Archive Terminal instead of the Navigation Table. Added `UNIT_02_ARCHIVE_CHALLENGE_QUESTS` (one quest, `case-006-archive-region-display`, migrating `REGION_EVIDENCE`/`REGION_RECORDS`'s 6 records/3 regions) to `content/quests/unit-02-quests.js`, wired into `local-content-repository.js`/`scripts/validate-content.js` (35 groups now, was 34). Set `case-006.archiveChallenge = { questType: "evidence-organizing", questId: "case-006-archive-region-display" }` in `unit-02-campaign.js`. Replaced the Archive Terminal's placeholder dialogue with a real `action: "archive-challenges"` screen transition (same pattern as the Navigation Table's `table.action = "archive"`) to a new `archiveChallengesScreen()`, following `practiceCheckScreen()`'s live-graded renderQuest/gradeQuest pattern — no separate "submit" step, completion is detected from placement/reflection state and written to `progress.archiveChallenges[questId]` plus `progress.completedCases` via the existing `unlockNext()`. A case already in `completedCases` from before this migration renders as complete without replay, preserving old-save completion. Added a new `"archive-room"` click action (distinct from `"home"`) so the back-link returns to the Archive Room rather than resetting hub spawn to the Main Hall. **Found and fixed a real pre-existing bug** in `applyEvidencePlacement()` (shared by every evidence-organizing quest, drag or `<select>`-driven): it evicted any other source already occupying a target slot, silently enforcing one-source-per-slot even though `renderEvidenceOrganizingQuest()`'s own markup groups multiple placed sources per slot and nothing in the schema requires 1:1. This was never exercised before because Case 4's only prior evidence-organizing quest happened to be 3 sources/3 slots (1:1); case-006's migration is 6 sources/3 slots (2:1) and immediately surfaced it — caught only via a scripted Playwright walkthrough placing all 6 records (`npm run test`'s 216/216 passed throughout, confirming compilation/test-passing alone would have shipped this bug). Fix: removed the eviction loop, keeping only the source's own placement mutation. Also removed now-dead `hubDialogueId`-driven dialogue markup from `archiveRoomScreen()` (both room targets now always transition screens via `action`, so `hubDialogueId` can never be set for that room). Verified live via three scripted Playwright passes against the real dev server: full Main Hall → door → Archive Room → Terminal → Archive Challenges → complete-all-6-placements-plus-reflection → completion written → back-link-returns-to-Archive-Room walkthrough (with real collision-aware pathfinding around `HUB_BLOCK_RECTS`); a legacy-save case (`completedCases` includes case-006, no `questResponses` entry) showing the complete-without-replay card; and confirming `regionsScreen()`/the `"regions"` route is still fully reachable and renders all 6 cards, per the plan's Phase 2 completion criteria (not deleted until Phase 5). Zero console errors across all three. `npm run test` 216/216 (unchanged — no new pure/exported logic, matching the existing convention that screen-assembly functions aren't unit-tested), `npm run validate:content` 35/35, `npm run lint`/`npm run build` unchanged from baseline. Still not done: Investigation Challenge gate (plan Phase 3), Navigation Table `navigationTableVisible` filter + unit-level `archiveChallenges[]` completion gate (plan Phase 4), `regionsScreen()` deletion (plan Phase 5).
- Phase 17 (2026-07-16, Investigation Challenge gate — plan Phase 3): built the vertical-slice pilot gating one real field source behind an Investigation Challenge, reusing the existing `hipp` quest type (`SourceAnalysisQuestSchema`/`renderSourceAnalysisQuest`/`gradeSourceAnalysisQuest`) as-is, reframed as a pre-reveal "Source Prediction" — no new quest-type module, per the plan's §3 "reuse `QUEST_TYPES`, no new interface" decision. Added `UNIT_03_INVESTIGATION_QUESTS` (one quest, `case-007-investigation-dunmore-proclamation`: Point of view + Purpose HIPP prompts on Lord Dunmore's 1775 proclamation) to `content/quests/unit-03-quests.js`, kept in its own array rather than merged into `UNIT_03_SOURCE_ANALYSIS_QUESTS` so it doesn't also surface as a `practiceCheckScreen()` card (mirrors how `UNIT_02_ARCHIVE_CHALLENGE_QUESTS` stays separate from `UNIT_02_EVIDENCE_ORGANIZING_QUESTS`). Set `commoncause-dunmore-proclamation.investigationMode = "hipp"` / `.investigationQuestId = "case-007-investigation-dunmore-proclamation"` in `unit-03-campaign.js` — the first source anywhere in the game to use this pointer. Wired into `local-content-repository.js` (`unit03.investigationQuests`) and `scripts/validate-content.js` (added a schema-validation entry plus the new quest's ids into the `hipp` cross-reference set; 36 groups now, was 35). In `main.js`: added `INVESTIGATION_QUESTS_BY_TYPE`/`investigationQuestFor()` (mirrors `ARCHIVE_CHALLENGE_QUESTS_BY_TYPE`/`archiveChallengeQuestFor()`), `sourceInvestigationComplete()`, and a shared `sourceEntryScreen(sourceId)` resolver used by both the click (`start-source-activity`) and keyboard (field "E" interact) entry points so they can't drift out of sync on gating — previously each computed `sourceActivityRoute()` independently. Added a new `"investigation"` screen (`VALID_SCREENS`, the render switch, `investigationScreen()`) following `archiveChallengesScreen()`'s live-graded `renderQuest`/`gradeQuest` pattern (no separate "submit" step; state lives in the existing `progress.questResponses` bucket, so leaving and resuming later just re-grades the same saved selections — no new save-state bucket needed). A completed challenge reveals a "Source Unlocked · Continue →" button (`investigation-continue` action, handled in `handleSourceReaderClick`) that continues into the normal `sourceReader()` worksheet, which still has its own separate, later "Institute Context" reveal gated behind writing an initial reading — Investigation and that reveal are deliberately two different layers, not redundant. Verified live via two scripted Playwright passes against the real dev server, both with real collision-aware keyboard pathfinding (no state injection to bypass movement/proximity gating): (1) full walk → proximity-gated "E" interact → Investigation Challenge renders → answer both HIPP prompts → "Source Unlocked" appears → continue → lands on the real `sourceReader()` for Lord Dunmore's Proclamation; (2) a resume test — answer only 1 of 2 prompts, leave via the back-link to `field`, re-approach and re-interact, confirms the gate correctly re-routes to `"investigation"` (not `"source"`) and the previously-selected radio option is still checked from `questResponses`. `npm run test` 216/216 (unchanged — no new pure/exported logic; screen-assembly functions aren't unit-tested per existing convention), `npm run validate:content` 36/36, `npm run lint`/`npm run build` unchanged from baseline (same 3 pre-existing warnings, 0 errors). Still not done: Navigation Table `navigationTableVisible` filter + unit-level `archiveChallenges[]` completion gate (plan Phase 4), `regionsScreen()` deletion (plan Phase 5), second-wave challenge-mode catalog (plan Phase 6+). No source outside case-007's Dunmore proclamation uses `investigationMode` yet.
- Phase 18 (2026-07-16, Navigation Table cleanup + unit-completion gate — plan Phase 4): set `case-006.navigationTableVisible = false` in `unit-02-campaign.js` (its Archive Challenge is live and reachable from the Archive Terminal since Phase 16, so it no longer needs a Navigation Table marker too) and filtered `archiveScreen()`'s marker list to `selectedUnit.cases.filter((c) => c.navigationTableVisible !== false)`. **Caught a real bug during browser verification, not by lint/test/build**: content is imported into `main.js` as raw JS objects, never passed through Zod at runtime (only `scripts/validate-content.js` does that, in a separate Node process) — so a truthy-check filter (`c.navigationTableVisible`) evaluated to `undefined` for every case that doesn't explicitly set the field (i.e. every case except case-006), hiding every Navigation Table marker in both units. Fixed by checking `!== false` instead, matching the Zod schema's `default(true)` semantics by hand at the one runtime call site that needs it. Added `UnitSchema.archiveChallenges: z.array(ArchiveChallengeSchema).default([])` to `unit.schema.js` for unit-level bonus Archive Challenges not tied to relocating one case (distinct from a case's own singular `archiveChallenge`); no unit sets any yet, so this is currently a no-op array everywhere. Added `unitArchiveChallengesComplete(unit)`/`unitReadyForReview(unit)` helpers to `main.js` and used them to gate both the "Begin Archive Review" button's visibility in `archiveScreen()` and, more importantly, the actual `completedUnits` write inside the `submit-review` handler itself (previously that handler only checked SAQ-fields-filled, with case-completion enforced only as button-visibility UI, not as a real guard) — a determined player navigating straight to the `review` screen could previously complete a unit without finishing every case; now `unitReadyForReview()` blocks it with a real error message. **Grandfathering verified explicitly**: since no code path removes entries from `progress.completedUnits`, a save that already has `unit-02` there from before this phase keeps it after the new gate ships — confirmed live by seeding exactly that save shape and reloading. Extended `scripts/validate-content.js`'s `archiveChallengeEntries()` to also emit cross-reference-check entries for `unit.archiveChallenges[]` (previously only case-level `archiveChallenge` and source-level `investigationMode` were checked); `checkChallengeReferences()` itself needed no changes, since it already took a flat generic entry list. Verified live via a single scripted Playwright pass against the real dev server covering the full gate: seeded a Unit 2 save with case-004/005 complete but case-006 not, confirmed case-004/005 markers still render and case-006's marker is gone and the Archive Review button is hidden; played case-006's Archive Challenge for real through the Archive Terminal's `<select>` fallback (all 6 evidence cards placed in their real correct region per `unit-02-quests.js`'s `correctSlotId`s, plus a real reflection), confirmed `progress.completedCases` picked up case-006 and the Archive Review button then appeared; submitted a full real Archive Review (all MCQ + SAQ fields) and confirmed `unit-02` landed in `completedUnits` and the screen advanced to `completion`; separately seeded a save with `unit-02` already in `completedUnits` pre-migration (no case-006 Archive Terminal playthrough) and confirmed it survives a reload untouched. `npm run test` 216/216 (unchanged — no new pure/exported logic), `npm run validate:content` 36/36, `npm run lint`/`npm run build` unchanged from baseline (same 3 pre-existing warnings, 0 errors). Still not done: `regionsScreen()`/`"regions"` route deletion (plan Phase 5, only after Phases 2–4 are confirmed stable), second-wave challenge-mode catalog (plan Phase 6+).
- Phase 19 (2026-07-16, delete `regionsScreen()`/the `"regions"` route — plan Phase 5): removed the bespoke `regionsScreen()` puzzle screen and everything that only existed to serve it, now that case-006's Archive Challenge (shipped Phase 16) has been the real, reachable version of "Charter & Compact" since Phase 4 took the marker off the Navigation Table. From `main.js`: `regionsScreen()` itself, its `"regions"` `render()` switch case and `VALID_SCREENS` entry, the `clear-regions`/`check-regions` click handlers, and the `region-card`/`region-drop` branches inside the shared drag/dragover/dragleave/drop handlers (careful surgery, since those handlers are multi-purpose — `data-leg-drop`/`data-sequence-item`/`data-evidence-slot`/`data-cargo-hold` branches in the same functions had to stay untouched). Removed the now-fully-dead `REGION_RECORDS`/`REGION_EVIDENCE` content arrays from `unit-02-campaign.js` (the migrated Archive Challenge quest duplicates this text rather than importing it, confirmed in Phase 16, so nothing else referenced them) and their `RegionRecordSchema`/`RegionRecordsSchema`/`buildRegionEvidenceSchema` schemas from `unit02-activities.schema.js`, plus the matching `local-content-repository.js` fields and `scripts/validate-content.js` validation groups (34 groups now, was 36) and CSS (`.regions-shell`/`.region-column`/`.region-columns`/`.region-card`, carefully unhooked from the shared `.leg-drop`/`.triangle-shell`/`.cargo-card` comma-grouped selectors they'd been sharing declarations with). **A real design question fell out of the deletion, not just code removal**: `case.route` was a required enum field (`CASE_ROUTES`) with no value that made sense for a case that's now Archive-only — resolved by making `route` nullable in `unit.schema.js` and setting `case-006.route = null`, then auditing every runtime read of `case.route` in `main.js` for null-safety. Three of four were already defensive (`c?.route || "archive"` in the travel-screen auto-advance timeout, a `=== "field"` equality check, a ternary with an `: selected.question` fallback); the fourth — the "Skip transition" button's `skip-travel` handler — assigned `c.route` directly with no fallback, a latent crash for exactly this case that had never been exercised before case-006 got a null route, fixed to match the other three (`c?.route || "archive"`). Verified live via a scripted Playwright pass against the real dev server covering all four risk areas the deletion touched: a save seeded with `currentScreen: "regions"` (the deleted screen) redirects safely to `institute` on boot rather than rendering blank; a legacy save with `selectedCaseId: "case-006"` can still reach the route panel's "Initiate Chronotravel" → "Skip transition" flow and lands on `archive` (not a crash) via the newly-added fallback; Triangle Ledger's (case-005) unrelated `leg-drop` drag-and-drop still places cargo correctly after the region-drop branches were removed from the same shared handlers; and case-006's real Archive Challenge (all 6 evidence selects + reflection) still completes and writes `completedCases`, confirming the deletion didn't regress the very feature that made it safe to delete. `npm run test` 215/215 (216 minus the one test suite for the deleted `RegionRecordsSchema`/`buildRegionEvidenceSchema`, replaced with a `route: null` variant of the existing Archive Room relocation test), `npm run validate:content` 34/34, `npm run lint` unchanged from baseline (same 3 pre-existing warnings, 0 errors), `npm run build` succeeds with a smaller bundle (379 KB vs 387 KB, confirming real dead-code removal). This closes the plan's Phases 0–5; only Phase 6+ (second-wave challenge-mode catalog, not scheduled in detail) remains.

## 5. Current active phase

**Investigation/Archive Challenge plan (`/Users/justinramsey/.claude/plans/yes-plan-the-look-snazzy-wozniak.md`): Phases 0–5 (the original vertical slice) plus a Phase 6 second-wave catalog-expansion pass are now shipped.** The second-wave pass (2026-07-16, plan `i-want-to-build-gleaming-narwhal.md`) added 4 more Investigation Challenge modes and 5 more Archive Challenge modes — all reusing the existing `mcq`/`sequencing`/`evidence-organizing`/`hipp` quest-type engines, no new interaction primitive:

- **Investigation Challenges** (source-level, gate `sourceReader()`): `taino-context` (case-001, `mcq`), `waldseemuller-map` (case-001, `sequencing`), `riverbend-ledger` (case-004, `evidence-organizing`), `commoncause-dickinson-letter` (case-007, `mcq`) — joining the existing case-007 Dunmore proclamation (`hipp`).
- **Archive Challenges, case-level** (relocating an existing case's activity, mirroring case-006's precedent exactly — `empireScreen()`/`triangleScreen()` stay fully playable from the Navigation Table, untouched): case-003 "Empire's Foundations" (`sequencing`, migrating `EMPIRE_CONNECTIONS`'s real causal chain), case-005 "Triangle Ledger" (`evidence-organizing`, migrating `TRIANGLE_CARGO`/`TRIANGLE_LEGS`).
- **Archive Challenges, unit-level bonus** (`unit.archiveChallenges[]` — a Zod field that existed since Phase 18 but had never been populated by any unit until now): one per unit, first real content on this path. Unit 1: `evidence-organizing` claim-and-evidence-builder over `EXCHANGE_RECORDS`. Unit 2: two `mcq` "strongest-evidence" quests. Unit 3: `evidence-organizing` comparing the Hall petition and Adams letter. **These are a genuinely new requirement**, not optional bonus content in the gameplay sense — `unitReadyForReview()` now blocks a unit's Archive Review submission until its bonus challenges are complete too, verified live (blocked with all cases done + bonus missing; unblocked after completing it; a pre-existing `completedUnits` entry stays grandfathered regardless).
- Deferred, not built: the literal `observation-challenge`/`political-cartoon` Launch-tier modes (no hotspot-click interaction engine exists; no political-cartoon source exists in current content) — explicit backlog, not silently reinterpreted.

**Two main.js changes beyond content wiring, both required for the new modes to actually work, not incidental:** (1) `archiveChallengesScreen()` gained an `archiveChallengeCard()` shared helper and a second render branch for `unit.archiveChallenges[]` bonus entries (previously only case-level challenges rendered). (2) Three new cross-type helpers — `isChallengeQuestComplete`/`challengeQuestAnsweredAny`/`challengeQuestHint` — replace `investigationScreen()`/`archiveChallengesScreen()`'s old completion checks, which hardcoded `result.complete` and only worked correctly for `hipp`/`evidence-organizing` (the only 2 types ever used there before); `mcq`/`sequencing` return `{answered, correct}` with no `.complete` field, so 3 of the 4 new Investigation Challenges and case-003's new Archive Challenge would have been **permanently uncompletable** without this fix — caught by both a live-browser Playwright pass and independently by the code-review pass, not by lint/test/build (all passed the whole time).

**Two more real, pre-existing bugs surfaced and fixed along the way, both caught only by live interaction, not compilation:**

- `applySequenceMove()` (the ↑/↓ move-button handler) resolved a sequencing quest's `items` via `PRACTICE_CHECK_QUESTS[activeFieldCaseId()].sequencing.find(...)` only — a lookup that could never contain an Archive/Investigation Challenge sequencing quest, since sequencing had never been used outside Practice Check before this pass. Fixed via a new `sequencingQuestItemsFor()` helper checking practice → archive → investigation in turn.
- The native HTML5 drag-and-drop handler for sequencing quests (`handleAppDrop`'s `[data-sequence-item]` branch) computed the reordered list via `list.querySelectorAll("[data-sequence-item]")` — but each item's own ↑/↓ buttons _also_ carry `data-sequence-item` (for their own click handler), so the unscoped selector matched 3× per item and corrupted the order array. Fixed by scoping to `li.sequence-item[data-sequence-item]`. (This bug predates this pass — it affected the original Unit 1/2/3 Practice Check sequencing quests too, just never triggered because drag interaction hadn't been exercised by an automated test before.)
- Also caught: 2 of the new sequencing quests' `items` arrays were authored in already-correct order (the exact anti-pattern `sequencing-quest.js`'s own doc comment warns against — the array's authored order is what renders before any player move, so an unscrambled array gives the answer away for free). Fixed by reordering the arrays while keeping `position` fields as the real answer key.

Content authoring for this pass used the project's `content-designer` subagent (3 separate passes, one per phase); one of its outputs (Phase C's unit-01 bonus quest) was appended into an array validated as a homogeneous `sequencing` list when it was actually `evidence-organizing`-shaped — caught before commit by `npm run validate:content`, fixed by splitting into a new `UNIT_01_ARCHIVE_EVIDENCE_QUESTS` array. A code-review pass (8 parallel finder angles) additionally found and fixed: a CSS regression where evidence-organizing's "all placed correctly, reflection still missing" state lost its green "success" styling in the generalized helper; two already-shipped screens' hint copy changed incidentally (hipp's exact wording restored, mcq given its own distinct hint); and a real validation gap — merging multiple content arrays into one `QUEST_TYPES`-keyed lookup per type had no check preventing a quest id collision across arrays, fixed by extending `checkUniqueGlobalIds` (already used for case/source ids) to also cover each quest-type bucket (4 new cross-reference groups, 46 total now, was 38). The review also flagged — but this pass deliberately did not fix, as out of scope — that `isChallengeQuestComplete`/`challengeQuestAnsweredAny`/`challengeQuestHint`'s per-type branching duplicates knowledge `practiceCheckScreen()` already encodes inline for the same 4 types, and duplicates knowledge each quest-type module's own `grade`/`render` functions already determine; a future pass could push `isComplete`/`answeredAny`/`hint` down into each `quest-types/` module (one more field per `QUEST_TYPES` entry, the same shape of growth the module already anticipates) rather than re-deriving it in `main.js`.

`main.js` is 3,984 lines now (was 3,882 at Phase 19). `npm run validate:content` 46/46 (was 34), `npm run test` 215/215 unchanged (no new pure/exported logic — quest content and screen-assembly wiring aren't unit-tested, matching existing convention), `npm run lint`/`npm run build` unchanged from baseline (same 3 pre-existing warnings, 0 errors). Verified live via multiple scripted Playwright passes against the real dev server: all 4 new Investigation Challenges driven via real keyboard field-navigation + collision (not state injection) including the specific `activityRoute`-continuation fix (`taino-context`→`village-activity`, `waldseemuller-map`→`map-jigsaw`, both confirmed reached post-completion, not skipped) and a resume check; both new case-level Archive Challenges; all 3 new unit-level bonus challenges plus the `unitReadyForReview()` gate/grandfathering; the partial-success CSS fix.

Only case-006/case-003/case-005 (Archive Challenges) and case-007's Dunmore proclamation plus the 4 sources above (Investigation Challenges) use the new data model so far — every other source/case is still untouched, per the plan's incremental-migration principle.

- Phase 22 (2026-07-18, real accounts/classrooms/submissions/AI-backed grading — a genuine forcing function, not owner override): the owner is planning to actually run this in a real classroom; per `POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10 this is exactly the named trigger ("a real teacher or second user needs an account" / "a real classroom pilot gets scheduled") for revisiting `PlatformCore`/accounts/classrooms — not a speculative build. Scoped deliberately narrow: only the Identity/Classroom/Submission/Evaluation/TeacherOverride slice, on a real backend. **Did not touch**: Phaser/Tiled/inkjs, `packs/<subject>/` extraction, `WorldComposition`/`QuestEngine`/`WorldRuntime`, or a `TeacherWorld`→`PublicationVersion` draft/publish/versioning pipeline (no second content author exists yet to justify it).
  - **New major dependency**: `@supabase/supabase-js` (Postgres + Auth + RLS) — the first production dependency added since `@anthropic-ai/sdk`. Schema + RLS policies live in `supabase/migrations/0001_init.sql` (`profiles`, `classrooms`, `roster_slots`, `student_world_profiles`, `submissions`, `evaluations`, `manual_grades`, `content_overrides` — immutability of `submissions`/`evaluations`/`manual_grades` enforced at the RLS layer itself, not just convention).
  - **Auth/roster model**: teacher-provisioned, not open self-registration for students. A teacher creates a classroom and provisions roster slots with system-generated ID codes (`api/roster/provision.js`); a student claims a slot with `(joinCode, studentIdCode)` and sets their own password (`api/roster/claim.js`, `api/roster/resolve-email.js` for returning-student sign-in on a new device), getting a real Supabase Auth identity behind a deterministic synthetic email (`student-<classroomId>-<code>@chronicle.invalid`). A teacher can reissue a one-time temporary password (`api/roster/reissue.js`) but can never see a student's actual current password. All three are service-role-only server functions — the browser only ever uses the anon key. Teachers self-serve sign up/in with real email+password (a judgment call made this session, not separately confirmed beforehand — flagged as the one open item worth a sanity check).
  - **The AI-grading backend is finally wired up**: `api/evaluate.js`/`api/_lib/rubrics.js` (built earlier, previously 100% disconnected — see `CURRENT-REPOSITORY-AUDIT.md`) is now called from the two free-text surfaces that previously had zero content grading — `sourceReader()`'s "initial reading" (HIPP rubric) and the Archive Review SAQ block (SAQ rubric, one call for all 3 parts) — via new pure request-builders (`apps/web/src/engine/evaluator-requests.js`) and a thin fetch client (`evaluator-client.js`). Added `hippElementsAsked` tags to `CASE_001_SOURCES`' 3 real sources so the HIPP rubric knows which elements each prompt actually asks for. Feedback is stored at `progress.submissions[taskId].feedback.payload` — the exact shape `api/_lib/rubrics.js`'s own header comment had anticipated — and mirrored to the new `submissions`/`evaluations` tables (no-op if signed out, so local-only play is unaffected). Still explicitly formative, never a grade — a teacher enters the real grade via a new `gradingScreen()`.
  - **Progress now syncs to the backend once signed in** (`apps/web/src/repositories/progress-repository.js`, a facade in front of the untouched `local-progress-repository.js`): `localStorage` stays the fast/offline path, with a debounced background push and a last-write-wins `resolveProgressConflict()` merge on hydration — addresses shared/rotating school Chromebooks losing a student's progress. `chronicle-progress-store.js` gained one additive field, `lastSavedAt`.
  - **Author Mode's content-override store gained a classroom-scoped remote sibling**: `remote-teacher-override-store.js` + a `teacher-override-repository.js` facade that falls back to the untouched local store whenever no classroom is active (signed-out/local dev keeps working exactly as before) — same allow-listed `title`/`centralQuestion` fields, no draft/publish pipeline added.
  - **New screens** (additive to `VALID_SCREENS`/`render()`'s switch/`CLICK_HANDLER_GROUPS`, no existing screen restructured): `join`, `login`, `teacher-dashboard`, `grading`. Two new `MAIN_MENU_ITEMS` entries ("Join a Classroom", "Teacher Sign In"). A `?entry=join|teacher-login` query param read once at boot lets an external link land directly on the right screen.
  - **Odysso integration** (separate repo, `OneDrive/Odysso` — the marketing/showcase site, Chronicle's flagship product): its previously-100%-fake `/sign-in` page (explicit "no account is created" placeholder) now links out to Chronicle's real `join`/`login` screens via the `?entry=` param, using a new `VITE_CHRONICLE_APP_URL` env var. No shared Supabase project between the two repos yet — deliberately deferred until a second real product exists, at which point Odysso can point its own sign-in at the same Supabase project without any Chronicle-side rework, since Chronicle's accounts are already real.
  - **Verification status — code-complete, backend-untested**: `npm run test` 276/276 (26 new, across `auth-flows.test.js`, `evaluator-requests.test.js`, `progress-repository-conflict.test.js`, `teacher-override-repository.test.js`), `npm run lint`/`npm run validate:content`/`npm run build` all pass. Live-verified via `npm run dev` + Playwright: the new main-menu entries, `join`/`login` screens rendering and tab-toggling correctly under the real `?entry=` boot param, a real network failure (no live Supabase project exists yet) surfacing as a graceful inline error rather than a crash, and the untouched core game (institute boot, onboarding) still working with zero console errors. **Not yet verified**: any real Supabase project doesn't exist yet, so the actual claim/login/provision/evaluate/grade round trips, RLS cross-tenant isolation, and the real `/api/evaluate` call have not been exercised end-to-end. Full session report: `docs/architecture/session-reports/2026-07-18-real-teacher-mode-accounts-classrooms-grading.md`.
- Phase 23 (2026-07-20/21, real Teacher Mode — content swaps, unit-floor gating, draft/publish): owner-directed expansion of teacher-facing tooling beyond Author Mode's 2-field text override, scoped down via clarifying questions to: curated-pool source/MCQ-quest swapping (not free-form authoring), a draft-then-publish step (not live-edit), and an early-access unit floor (never a hard ceiling — additive to a student's own `progress.unlocked`, same guarantee `unlockNextUnit()` already gave). **Also discovered mid-session that §6's long-standing "no production Supabase project exists yet" claim is now stale** — `.env.local`/`apps/web/.env.local` contain real, live Supabase credentials (project `rxhpxqbtrnswjevcuxon`), meaning Phase 22's backend is a real live project, just not yet verified end-to-end (see below).
  - **New migration `supabase/migrations/0006_teacher_mode.sql`** (written, **not yet applied to the live project** — no Supabase CLI/connection string is set up in this repo/session, and applying DDL to a real shared database wasn't done without the owner's go-ahead): `classroom_unit_progress` (one row per classroom, `enabled_unit_index` into `main.js`'s `UNITS` array) and `classroom_content_selections` (`(classroom_id, slot_kind, slot_content_id, status)` unique, `status` is `'draft'`|`'published'` — publish is an upsert-or-delete per slot, not a snapshot/versioning table). Both reuse the existing `is_classroom_teacher()` RLS helper; students get published-only/floor-only read policies, mirroring `content_overrides`'/`student_world_profiles`' existing teacher-write/student-read-only split. Deliberately **not** a widening of `content_overrides` — that table's `field_name` CHECK is hardcoded to `('title','centralQuestion')` for a reason (a text copy-edit is a different concern than a pointer to a pre-validated alternate content object).
  - **Two new repository files**, matching `remote-classroom-repository.js`'s plain-async-function style and `recordManualGrade`'s "resolve the acting user via `getSession()` internally" convention (not passed in from `main.js`, since `getProfile()` only returns `{role, displayName}`, no `id`): `remote-classroom-unit-repository.js` (`getClassroomUnitFloor`/`advanceClassroomUnit`) and `remote-content-selection-repository.js` (a `resolveSourceSlot`/`resolveMcqQuestSlot` in-memory resolution cache — mirrors `remote-teacher-override-store.js`'s cache pattern exactly, switchable between `"published"` (student/default) and `"draft"` (teacher preview) — plus teacher-editing CRUD `listSelectionsForCase`/`setDraftSelection`/`publishCaseSelections`).
  - **Curated alternate-content pool**: `apps/web/src/content/case-001-source-alternates.js` / `quests/case-001-mcq-alternates.js` — wrapper objects (`{replacesSourceId, source: {...}}` / `{replacesQuestId, quest: {...}}`) validated against the exact same `buildSourceSchema()`/`McqQuestSchema` shapes as official content, one alternate per slot, Case 1.01 only (proof-of-pipeline scope — extending to more cases/slots is pure content work). `scripts/validate-content.js` gained 2 new schema groups plus a new `checkAlternateReferences()` cross-reference helper (`content/schemas/cross-reference.js`) verifying every `replacesXId` resolves to a real official id — verified live by deliberately typo-ing one and confirming `validate:content` fails with a clear message, then reverting. 51 groups now, was 46.
  - **`main.js` resolution wiring**: `sourcesForCase`/`sourceById` (now `export`ed) and `practiceCheckScreen()`'s `mcq` lookup each gained one `.map(resolveSourceSlot)`/`.map(resolveMcqQuestSlot)` call — no-ops whenever the resolution cache is empty (every signed-out/local-dev session, every classroom with no customization), so official content renders byte-identical to before this phase **by construction**, not by a conditional. `isUnlocked()` itself needed **zero changes** — the new `hydrateClassroomUnitFloor()` works purely by additively pushing each enabled unit's first case into `progress.unlocked`, reusing `unlockNextUnit()`'s exact semantics.
  - **New UI**: a "Manage Content" entry point on `teacherDashboardScreen()` (two new screens, `manage-content`/`manage-content-case` — additive to `VALID_SCREENS`/`render()`'s switch/`CLICK_HANDLER_GROUPS`, new `contentUiState` + `handleManageContentClick()` following the existing `gradingUiState`/`handleGradingScreenClick` split-screen-family convention) with a per-case slot editor (dropdown per source/MCQ slot, draft vs. published status, a disabled-until-dirty Publish button, a Preview-as-student toggle that switches the resolution cache to `"draft"` and back), plus a "Unit access" block with an "Advance to [next unit] →" button directly on the dashboard.
  - **Verification status — code-complete and locally verified, live-DB flow not yet exercised**: `npm run test` 293/293 (was 290 — 3 new files: both new repositories against a hand-rolled thenable Supabase-client mock, since no test in this repo mocked `lib/supabase-client.js` directly before now, plus `main-teacher-mode-resolution.test.js` pinning the no-op-by-default guarantee against the _real_, unmocked resolution module), `npm run validate:content` 51/51 (was 46), `npm run lint`/`npm run format:check` unchanged from baseline (confirmed via `git stash` diff — same pre-existing warnings/CRLF-formatting quirks, zero new). Live-verified via Playwright against the real dev server: full Case 1.01 replay (field → Codex/Evidence Satchel showing all 3 official source titles unchanged → Practice Check MCQ answered and graded correctly against official content) with zero console errors; all 4 Teacher Mode screens' signed-out guard clauses render cleanly (matching the pre-existing `teacher-dashboard`/`grading` guard pattern exactly). **Not yet verified** (blocked on applying the migration to the live project, which needs the owner's go-ahead / Supabase SQL editor access this session didn't have): the actual classroom-scoped swap/publish/preview/revert round trip, the unit-floor advance-and-hydrate round trip, and RLS behavior for these two new tables against real teacher/student sessions.
- Phase 21 (2026-07-17, Unit 3 companion case — product content, not architecture): added `case-008` ("The Founding Debate," 1783–1789) to `UNIT_03.cases[]`, Unit 3's first companion case alongside flagship case-007, following Unit 1's case-001+case-002/003 shape per `NEXT-UNITS-ROADMAP.md` §3. Reused the `"ledger"` mechanic pattern (case-002's `exchangeLedgerScreen()`/`check-ledger`) but as a genuinely new, parallel route rather than a forced generalization of case-002's working code: added `route: "founding"` to `CASE_ROUTES` (`unit.schema.js`), a new `foundingScreen()` + `check-founding` handler in `main.js` (structurally identical to `exchangeLedgerScreen()`/`check-ledger`, reading a new `FOUNDING_RECORDS` export instead of `EXCHANGE_RECORDS`), a new `progress.foundingLedger` bucket in `chronicle-progress-store.js` (mirroring `exchangeLedger`), and reused the existing generic `ledger-success`/`upload` screens as-is (no new success-screen code needed — they were already case-agnostic). `FOUNDING_RECORDS` (4 records, `exchange-record.schema.js`'s existing `ExchangeRecordsSchema` reused unchanged) covers the Articles of Confederation's failures, the Constitutional Convention's Great/Connecticut Compromise, and the Federalist/Anti-Federalist ratification debate (Federalist No. 10 vs. Brutus No. I as a deliberate contrasting pair) through to the Bill of Rights (George Mason's Objections) — all real, cited primary sources, authored via the `content-designer` subagent. Content authoring and engine wiring were done in parallel (independent files, no shared edit surface) rather than sequentially. Corrected a stale claim in `NEXT-UNITS-ROADMAP.md` along the way: it said Comparison had no real quest content anywhere, but that gap was already closed by the earlier second-wave pass (Phase 19's Hall/Adams Comparison tag) before this document's own §3 was written — case-008 deliberately does not add quest-type content of its own, mirroring case-002's exact footprint (the ledger records are the whole graded mechanic, no separate Practice Check layer). `npm run validate:content` 47/47 (was 46), `npm run lint`/`npm run build` unchanged from baseline (same 3 pre-existing warnings, 0 errors). Verified live via a scripted Playwright pass against the real dev server: both case-007 and case-008 markers render on the Navigation Table; the Founding Debate ledger renders all 4 records with real citations; answering all 4 correctly and validating unlocks `case-008` in `completedCases`, persists `foundingLedger` answers, and auto-advances through the existing generic success/upload flow; case-002's Exchange Ledger and case-007's field case confirmed unaffected (no shared-state collision); zero console errors throughout.
- Phase 24 (2026-07-21, Manage Content redesign — owner-directed UI usability pass, scoped down via clarifying questions): Phase 23's Manage Content was hard to use as a teacher (flat case list, no framing of what kind of mission each case is, MCQ-only swaps, a bespoke non-student-accurate preview). Scoped with the owner into three decisions before building: (1) map missions (`case.route === "field"`) lock sources to official — no source-swap UI for these, only their questions — a deliberate pullback from Phase 23's shipped Case 1.01 source-swap capability; (2) a from-scratch "quest builder" (teacher writes brand-new MCQ/SAQ questions/answers, persisted) is explicitly out of scope — flagged as a distinct, larger future plan, since it needs a new content-storage model and promoting SAQ into a real quest type; (3) "Preview as student" must be a real, playable preview, not a static snapshot.
  - **Unit-grouped listing**: `manageContentScreen()` now iterates `UNITS` directly (one section per unit, showing `resolvedUnitTitle()`/`period`/`description`/`resolvedUnitCentralQuestion()`), listing every case — not just ones with existing alt content — each with a new `caseKindLabel(kase)` badge derived purely from existing fields (`"Walkable map"` for `route === "field"`, `"Archive Challenge only"` for `route === null`, `"Activity — " + mechanic` otherwise) plus its `summary`. No new content fields were needed — titles/descriptions/mechanics already existed, just weren't surfaced here.
  - **Generalized swap/draft/publish pipeline** from `{source, mcq-quest}` slot kinds to `{source, mcq, sequencing, evidence-organizing, hipp}` — every quest type now swappable, not just MCQ. `remote-content-selection-repository.js`'s MCQ-only `alternativesForMcqSlot`/`resolveMcqQuestSlot` became generic `alternativesForQuestSlot(questType, slotContentId)`/`resolveQuestSlot(questType, quest)`. New migration `supabase/migrations/0007_generalize_content_slots.sql` (additive, not editing `0006` in place, since the owner may already have applied it by hand) widens `classroom_content_selections.slot_kind`'s CHECK constraint and renames `'mcq-quest'` → `'mcq'` to match `QUEST_TYPES` keys exactly. **Not yet applied to the live project** — same owner-go-ahead-required status Phase 23's own migration had; confirmed live via Playwright that writes for every quest-type slot (including MCQ, now renamed) correctly 400 against the live DB's still-old constraint, surfacing a legible error to the teacher rather than failing silently — reads/dropdowns/alt-pool wiring all work today regardless, since only publish/draft writes touch the constrained column. Seeded 4 new curated-alternate content files (`case-001-sequencing-alternates.js`, `case-001-evidence-organizing-alternates.js`, `case-001-hipp-alternates.js`, `case-006-evidence-organizing-alternates.js`, authored via the `content-designer` subagent, one real historically-grounded alternate each, same proof-of-pipeline scope as Phase 23's originals) and wired `resolveQuestSlot` into `practiceCheckScreen()`'s sequencing/evidence-organizing/hipp arrays and `archiveChallengeQuestFor()` (previously only MCQ was resolved).
  - **Real "Preview as student"**: replaced the old bespoke `manageContentPreviewMarkup()` card entirely. For map missions, a new `previewSession` (module-level, never persisted) navigates the teacher into the actual walkable `field` screen — real movement/collision/camera/NPCs/Practice Check, reusing `goToCase()`'s spawn logic — with a `chrome()`-injected "Previewing as student · Exit preview" banner on every screen while active. For non-map missions with a case-level Archive Challenge, it navigates into the real `archiveChallengesScreen()` instead. The shared `save()` wrapper (~60+ call sites) now no-ops while `previewSession.active`, and `runEvaluation()`'s `recordSubmission()` call (a network write independent of `save()`) got the same guard — both confirmed live: exploring the real field, opening Practice Check (all 4 quest types rendering and gradable), and exiting via both the banner and the field's own "Recall to Institute" control (a safety-net guard added to `handleChromeClick`'s `home`/`archive-room` actions) all worked with a clean restore back to `manage-content-case` and zero persisted-state leakage.
  - **Verification**: `npm run test` 291/291 (repository test file updated for the renamed API, one new test asserting a draft/published row for one quest type never leaks into another type's resolution), `npm run validate:content` 58/58 (was 51 — 7 new groups: 5 schema + 2 cross-reference, for the 4 new alt files), `npm run lint`/`npm run format:check` unchanged from baseline (confirmed via `git stash` diff — identical pre-existing warnings/CRLF quirks, zero new), `npm run build` succeeds. Live-verified via Playwright against the real dev server and the real (partially-migrated) Supabase project: the full unit-grouped list with accurate kind badges across all 8 cases in 3 units; Case 1.01's editor showing sources read-only and all 4 quest types as editable dropdowns with their seeded alternates; the expected/graceful constraint-violation error on write (see above); both preview flows (map → real field → Practice Check, non-map → real Archive Challenges screen showing sibling challenges exactly as a student would see them) with clean exit/restore; a non-map case with no editable content (case-002) rendering its "no swappable content yet" state cleanly. A full anonymous-student regression pass (no classroom active) was not completed end-to-end due to hub movement/collision proximity-gating taking many turns to navigate manually in this session — not code this phase touched, and its no-op-by-default guarantee is independently covered by `main-teacher-mode-resolution.test.js` plus the same `fieldScreen()`/`practiceCheckScreen()` functions already exercised live during the teacher-preview flow above.
- Phase 25 (2026-07-21, same day, Manage Content usability follow-up — owner-directed UI redesign, presentation-layer only): Phase 24 shipped the swap/draft/publish pipeline but the owner found the actual screens unusable: the unit list rendered every mission at once with no way to collapse it, both screens were wrapped in `.completion-shell` (an 820px centered confirmation-card layout built for short "you're done!" screens, not a data editor — the literal cause of the reported stretched/clunky look), and a slot only showed its official _label_ text next to a bare `<select>` with no rendering of the actual source text or what a question/choices look like. Scoped via clarifying questions to two decisions: (1) unit accordion is one-open-at-a-time, not multi-expand; (2) missions with zero swappable content (case-002/008's bespoke ledger mechanic) get a **read-only preview only** of their real content, not a new editable-content model — keeping Phase 24's "quest builder is out of scope" boundary intact.
  - **Unit accordion**: added `manageContentExpandedUnitId` (module-level, transient, reset on `open-manage-content`) — a single scalar rather than a Set, so opening one unit always collapses whichever was open, giving accordion behavior with no extra bookkeeping. `manageContentScreen()` now renders each unit as a `toggle-manage-content-unit` button with a chevron; a unit's missions (now cards, not table rows) only render while expanded.
  - **Real inline "quest preview" per editable slot**: `manageContentCaseScreen()` replaced its `<table>` of slot rows with a stack of slot cards, each showing the existing swap `<select>` plus a live, native-looking preview of whichever content is currently selected — reusing the exact same `renderQuest(questType, quest, {})` (`quest-types/index.js`) and `sourceVisual(source)` (`main.js`) a student sees, not a bespoke re-rendering. Added `sourceAlternateById`/`questAlternateById` to `remote-content-selection-repository.js` (thin exports over maps `resolveSourceSlot`/`resolveQuestSlot` already built internally) so `loadManageContentCaseData` can resolve a slot's full draft-or-official content object, not just its `{id, label}`. Each preview is wrapped in a new `.quest-preview-frozen` (`pointer-events: none`) container so reused interactive markup (radio inputs, draggable evidence cards, drag-and-drop `<select>` fallbacks) can't fire the app's shared global click/change listeners — which key off the same `data-mcq-quest`/`data-cargo-card`/etc. attributes used in real gameplay — from a teacher's edit screen.
  - **Read-only preview for the two bespoke-mechanic missions**: added `LEDGER_PREVIEW_RECORDS_BY_CASE` (`case-002` → `EXCHANGE_RECORDS`, `case-008` → `FOUNDING_RECORDS` — confirmed via `officialQuestSlotsForCase`/`UNIT_SOURCES` that these are the only two cases with zero editable slots today) and a new `ledgerPreviewCardMarkup()` reproducing `exchangeLedgerScreen()`/`foundingScreen()`'s own `.ledger-card.ledger-card--source` markup read-only (radios shown, not wired to any action; the correct choice gets a `.is-correct-preview` highlight, a small preview-only affordance since there's no interactivity to reveal it otherwise) — additive, `exchangeLedgerScreen()`/`foundingScreen()` themselves untouched.
  - **Layout fix**: both screens moved off `.completion-shell` onto a new `.manage-content-shell` (`width: min(1180px, ...)`, same navy/gold bordered-card visual language, sized for a real editor rather than a narrow confirmation card).
  - **Verification**: `npm run build`/`npm run lint` (same 3 pre-existing warnings, 0 errors)/`npm run validate:content` (58/58, unchanged — presentation-layer only)/`npm run test` (291/291, unchanged — no new pure/exported logic) all pass; `npm run format:check` shows the same pre-existing baseline warnings (confirmed 7 of the 10 flagged files were untouched by this phase). Live-verified via Playwright against the real dev server and the real (partially-migrated) Supabase project, signed in through the dev-only fake-teacher shortcut: the accordion (collapsed by default, one unit open at a time, state persists across a mission visit and back); Case 1.01 (map) showing all 3 sources as real preview cards and all 6 quest-type slots with live previews, including selecting a curated alternate and confirming the preview updates to the alternate's actual prompt/choices, then reverting; Case 1.02 (Exchange Ledger) and the Charter & Compact Archive-Challenge-only case (case-006, evidence-organizing, no source section) both rendering correctly; "Preview as student" still working unchanged (real navigation into `archiveChallengesScreen()`, clean exit/restore). Zero console errors throughout. A cosmetic mission-card badge-wrapping issue found during this pass was fixed in the same session (`.manage-content-mission-head` given `flex-wrap`).
- Phase 26 (2026-07-21, same day, real free-text content authoring — owner-directed reversal of Phase 24's deferral): presented with the exact scope conflict (Phase 24 had explicitly flagged a "from-scratch quest builder" as out of scope, needing a new content-storage model and a not-yet-built SAQ quest type), the owner chose to build it now rather than keep the pool-swap-only model. Also fixed the real usability gap behind the original request: the flat slot list gave no visual link between a question and the source it was about, and the swap `<select>` had no visible save feedback (live-debugged: the write itself was never broken — `setDraftSelection` always succeeded — the only gap was no "this saved" signal near the control, since the only feedback was a preview quietly updating far below).
  - **Explicit boundaries kept even under "full authoring"**: map-mission sources (case-001/004/007, the only 3 cases `UNIT_SOURCES` covers, all `route === "field"`) stay locked to official text — their sources are tied to hardcoded NPC/interaction placement in the walkable field, a different, much larger body of work than a content-storage model. SAQ is still not buildable — no SAQ quest type exists anywhere in the engine (no schema, no `render`/`grade` functions) — the "add new question" type picker offers the 4 real quest types with SAQ shown visibly disabled ("not available yet"), not silently omitted.
  - **New migration `supabase/migrations/0008_custom_content_authoring.sql`** (applied to the live project this session, confirmed via a live write/read round trip — first migration confirmed applied same-session rather than left pending): new table `custom_content_items` (`id, classroom_id, case_id, slot_kind, mode` [`'replacement'`|`'addition'`]`, replaces_official_id, related_source_id, content jsonb, status, created_by, timestamps`), RLS mirroring `classroom_content_selections`' teacher-full-CRUD/student-published-only split, plus one new column on `classroom_content_selections` (`alt_kind`: `'curated'`|`'custom'`) disambiguating a curated-pool id from a `custom_content_items.id`. `mode: 'replacement'` rows ride the existing `classroom_content_selections` draft/publish lifecycle exactly like a curated alternate; `mode: 'addition'` rows (a net-new question with no official counterpart) own their own `status` directly, since there's no official slot for them to attach to. Also confirmed (live, via a real write) that **`0007_generalize_content_slots.sql`'s "not yet applied" status in Phase 24/25 was stale — it was already applied** before this phase started.
  - **New pure module `apps/web/src/engine/custom-content-authoring.js`**: converts a compact line-based authoring-form text convention into real schema-shaped content objects, validated against the exact same Zod schemas `scripts/validate-content.js` and the quest engines use (`buildSourceSchema()`, `McqQuestSchema`, `SequencingQuestSchema`, `EvidenceOrganizingQuestSchema`, `SourceAnalysisQuestSchema` — all confirmed browser-safe, pure `zod` + local modules). MCQ choices/sequencing items are one-per-line (sequencing's authored line order is the _correct_ order, shuffled before storage per `sequencing-quest.js`'s own documented anti-pattern warning — `position` stays the real answer key); evidence-organizing sources and HIPP prompts use a "Key: value" block format, parsed by two small block parsers. 41 new tests cover every builder's success/validation-failure/round-trip paths.
  - **New repository `remote-custom-content-repository.js`** (CRUD for `custom_content_items`) and extensions to `remote-content-selection-repository.js`: `loadSelectionsForResolution` now also fetches referenced `custom_content_items` rows and caches them alongside the curated maps (so both the real student path and "Preview as student" resolve custom replacements correctly), and `setDraftSelection`/`publishCaseSelections` carry `alt_kind` through the draft→publish copy.
  - **`main.js` — source-grouped, collapsible redesign of `manageContentCaseScreen()`**: replaced the flat slot-card stack with a per-source `<section>` (JS-tracked expand/collapse state, not native `<details>`, since `render()` fully replaces markup on every change and would otherwise reset it) showing the source's real preview, an **Edit** action (non-map cases only), every question grouped under it via a new small hand-authored `OFFICIAL_QUEST_SOURCE_LINKS` map (quest id → source id, covering case-001/004/007's real source-specific questions; anything unmapped falls into a "General questions" section, not an error), and a **"+ Add new question about this source"** action. A mission-flow strip at the top states what's structurally fixed vs. editable, derived from existing fields (`caseKindLabel`, `UNIT_SOURCES`, `archiveChallenge`), not hand-written per case. Each editable slot gained an **Edit** button opening the authoring form pre-filled via `authoringFieldsFromContent()`, and the swap `<select>` now shows a persistent "✓ Saved as draft — not visible to students until you publish" note directly under the control once a draft differs from published — the fix for the "does this dropdown do anything" complaint. "Publish to students" now also publishes any draft `mode: 'addition'` items alongside the existing per-slot swap publish/revert logic.
  - **Verification**: `npm run test` 322/322 (was 291 — 31 new: `custom-content-authoring.test.js`, `remote-custom-content-repository.test.js`, plus 2 new cases in `remote-content-selection-repository.test.js` for custom-kind resolution), `npm run validate:content` 58/58 unchanged, `npm run lint`/`npm run build` unchanged from baseline. Live-verified via Playwright against the real dev server and the real (now fully-migrated) Supabase project, signed in through the dev-only fake-teacher shortcut, against Case 1.01: grouping renders correctly (each of the 3 sources showing only its own questions, unrelated sequencing/evidence-organizing questions correctly falling into "General questions"); editing an official MCQ's prompt end-to-end (form → validate → `custom_content_items` insert → draft selection → live preview update → the new "Saved as draft" note appearing); adding a brand-new MCQ question via the type picker (SAQ confirmed visibly disabled) through to a "Teacher-added"-badged draft card; "Publish to students" correctly publishing both the edited swap and the new addition in one action (confirmed via network trace: the addition's `status` PATCHed to `published`, the swap's `classroom_content_selections` row upserted with `alt_kind: 'custom'`, and every other untouched slot's stale published row correctly deleted, matching the existing per-slot revert-to-official semantics exactly); "Preview as student" still landing on the real walkable field unchanged. Two-step "Confirm delete"/"Cancel" for teacher-added questions verified working. All test data (the edited swap and the added question) was reverted/deleted in the same session, confirmed via a final snapshot showing every slot back to "Published: Official" and the Publish button correctly disabled. Zero console errors throughout every step.
- Phase 27 (2026-07-22, Manage Content usability + Case 1.02 real editing — owner-directed, scoped via clarifying questions from live screenshots): the owner found Phase 26's case editor still hard to read (no collapsible structure, a source's own locked "Chronicler prompt" visually indistinguishable from the real editable questions below it, a "LINKED SOURCE" `<select>` that looked editable but was purely presentational grouping) and two workflow gaps (HIPP/evidence-organizing forms required retyping a source's text by hand; a confirmed dead end where "+ Add new question" saved successfully but never rendered to any real student screen). Scoped into 4 phases plus 3 explicit decisions: fix the confirmed "add new question" bug by **disabling creation** rather than wiring the render-path gap closed (a separately-scoped, larger fix); **extend real editing to Case 1.02**'s Exchange Ledger; source-text autofill is a **one-time copy-in, not a persistent link** (no schema change).
  - **Phase 1 (visual redesign, no schema/data changes)**: split `sourcePreviewCardMarkup()` into an always-visible header, a collapsible "About this source" metadata block (reusing the existing `manageContentSectionMarkup()` toggle, nested), and a visually locked Chronicler-prompt block, with a plain "these are editable" label above the real question list. Replaced `linkedSourceFieldMarkup()`'s `<select>` with a plain, non-interactive "Grouped under: X" label — `related_source_id` was already presentational-only (confirmed: never read by grading or by what a student sees), so this is a pure UI fix, zero backend change. Added plain-language help text for HIPP's "ID-only distractor" (grounded in the schema's own doc comment: a wrong answer that correctly names the right person/place but doesn't explain how it shapes the argument — mirrors the real AP DBQ rubric rule that identification alone scores zero) and evidence-organizing's "Skill" field (the real College Board historical-thinking skill the record demonstrates, not a free-reference label). **Found and fixed a real correctness bug**: `canPreview` (the Preview button's disabled state) and `enterContentPreview()`'s actual guard used different conditions, so a case with only addition-slot content and no `archiveChallenge` showed an enabled button that silently no-op'd on click — unified behind one new `caseIsPreviewable()` helper.
  - **Phase 2 (source-text autofill)**: added `caseSourcesForCopy()`/`sourceCopyOptionsMarkup()` and a "Copy in an existing source's text" picker to the HIPP document-text fields and a per-row "Copy from an existing source" picker to each evidence-organizing record row, both reading `UNIT_SOURCES[caseId]` and writing directly into the live authoring-form field state via new `handleAppChange()` branches (`[data-copy-hipp-source]`/`[data-copy-evidence-source]`) — ordinary text inputs afterward, confirmed live that a post-copy edit persists rather than reverting (proving it's a one-time copy, not a live link).
  - **Phase 3 (disable the confirmed dead end)**: **root cause confirmed by direct code trace, not assumption** — `archiveChallengesScreen()` iterates only `unit.cases.filter(c => c.archiveChallenge)` and `practiceCheckScreen()` iterates only `PRACTICE_CHECK_QUESTS[caseId]`, so a `mode: 'addition'` row (no official `questId` to be found by either static list) can never be reached by any student-facing render path — this is the exact bug behind the reported "added a question, it never showed up in preview." By contrast, `mode: 'replacement'` (editing an _existing_ official question, addressed by `officialId` and resolved through `resolveQuestSlot`) already works end-to-end and was left untouched. Both "+ Add new question" buttons now render `disabled` with an explanatory tooltip ("Coming soon — for now you can adjust existing questions or swap in an alternate"), matching the file's existing house style for a flagged, known gap (the pre-existing disabled "SAQ — not available yet" button). The `start-add-question`/`pick-question-type` handlers were left in place as harmless unreachable code, not deleted, since re-enabling later is cheap once the render-path gap is separately closed.
  - **Phase 4 (real editing for Case 1.02)**: added a new `"ledger-record"` slot kind, generalizing the existing per-slot swap/draft/publish/custom-authoring pipeline (`officialQuestSlotsForCase()`, `resolveQuestSlot`, `alternativesForQuestSlot`, `custom-content-authoring.js`'s builder dispatch) to Exchange Ledger records — confirmed this pipeline is **entirely slot-kind-agnostic already**, so wiring in the new kind required zero changes to `remote-content-selection-repository.js`'s resolution/CRUD logic beyond registering one new content array. New migration `supabase/migrations/0010_ledger_record_slots.sql` (additive, **not yet applied to the live project** — see below) widens both `classroom_content_selections`/`custom_content_items`'s `slot_kind` CHECK constraints, following `0007`'s exact widening pattern. New `apps/web/src/content/quests/case-002-ledger-alternates.js` (one real alternate, reusing the official "maize" record's already-verified excerpt/citation with a different sourcing-focused question, matching `case-001-hipp-alternates.js`'s "same document, different pedagogical angle" precedent rather than introducing an unverified new quote). New `ledgerRecordFieldsMarkup()`/`buildLedgerRecordContent()`/`ledgerRecordToFields()` (validated against the existing `ExchangeRecordSchema`) — a ledger record combines a source card and its one MCQ in a single form, unlike every other slot kind, since there's no separate source for it to group under. **Closed a resolution gap that would have repeated Phase 3's exact bug class**: `exchangeLedgerScreen()` and the `check-ledger` grading handler previously read the static `EXCHANGE_RECORDS` array directly, with no resolution step at all — both now go through a new `resolvedExchangeRecords()` (`EXCHANGE_RECORDS.map(record => resolveQuestSlot("ledger-record", record))`), so a published swap now actually reaches both the real render and the real grading. `LEDGER_PREVIEW_RECORDS_BY_CASE`'s `case-002` entry was removed (now genuinely dead — case-002 always has real slots) and `case-008`'s Founding Debate ledger deliberately left untouched on the old read-only path, per the explicit case-002-only scope decision. `scripts/validate-content.js` gained one new schema group and one new cross-reference group (`checkAlternateReferences`) for the new alternates file.
  - **A pre-existing, whole-editor gap was discovered, not introduced, during Phase 4's live verification**: `contentUiState.slots[].alternatives` (curated-alternate metadata) is computed by `loadManageContentCaseData()` for every slot kind but is **never rendered anywhere** — no dropdown or picker UI consuming it exists in the shipped app, for _any_ quest type, not just the new ledger-record kind. A teacher can only free-text-edit-replace a question today; picking from a curated alternate has no UI despite the backend fully supporting it since Phase 23. Not fixed this phase (a separate, larger UI-building task); `CASE_002_LEDGER_ALTERNATES` is validated and ready for whenever that picker gets built, same as the pre-existing HIPP/MCQ/sequencing/evidence-organizing alternate pools.
  - **Verification**: `npm run test` 323/323 (was 322 — unchanged pure-logic surface, no new exported functions), `npm run validate:content` 89/89 (was 87 before this phase's 2 new groups — see §1's note on the stale intermediate baseline), `npm run lint`/`npm run build` unchanged from baseline (same pre-existing warnings, 0 errors), `npm run format:check` clean on every file this phase touched. Live-verified via Playwright against the real dev server and the real Supabase project, signed in through the dev-only fake-teacher shortcut: Phase 1–3's redesign on Case 1.01 (collapsible metadata, locked-prompt separation, no more `<select>`, HIPP/evidence-organizing help text, save-after-dropdown-removal still preserves grouping correctly) and on Case 1.03 (the `canPreview` fix — Preview correctly enabled and landing on the real Archive Challenges screen); Phase 2's autofill on both a fresh HIPP form and an existing evidence-organizing row (field values populate correctly and remain independently editable after); Phase 3's disabled buttons confirmed inert while existing "Edit →" stays fully functional; Phase 4's full Case 1.02 editor (all 4 ledger records rendering as real editable cards, the full field form pre-filled correctly, "Preview as student" landing on the real `exchangeLedgerScreen()` with all 4 records correctly resolved) and a full Case 1.01 regression pass confirming zero visual/behavioral change. **One write path is blocked on the pending migration**: saving a ledger-record edit correctly 400s against the live database's still-old `slot_kind` constraint (confirmed via the exact same "expected, graceful failure" pattern `0007`/`0008` also produced when first shipped) — the error surfaces legibly in the UI (`contentUiState.error`), not a crash; reads (the ledger editor rendering, "Preview as student") work today regardless, since only the actual publish/draft write touches the constrained column.

- Phase 28 (2026-07-23, quest-type status/hint consolidation — item 1 of a new `FOCUSED-MODERNIZATION-ROADMAP.md`, commit `fddf145`): closed the exact gap this doc's own §6 had been flagging as unscheduled follow-up since Phase 19/20 — `practiceCheckScreen()` and the Archive/Investigation Challenge screens each independently re-derived a quest's answered/complete/partial/hint state per quest type, and the two had already drifted apart (HIPP's partial-credit status only existed in one of them). Each of the 4 `quest-types/*.js` modules gained real `answeredAny`/`isComplete`/`partialSuccess`/`hint` functions, and `main.js`'s 4 call-site groups now call through that shared contract instead of re-deriving it inline. Verified with Vitest (403/403 at the time), lint/build/validate:content unchanged, and a live dev-server pass confirming Practice Check and Archive Challenges render identical status/feedback to the pre-refactor behavior, including the evidence-organizing partial-success case. **This entry backfills a quickref update that was missed when the commit landed** — a same-day lapse of this file's own "update after every phase" rule, caught while starting Phase 29.
- Phase 29 (2026-07-23, commit the Playwright suite — item 2 of the same modernization roadmap): the roadmap ranked "commit the Playwright suite" as the second item and explicitly flagged it "Recommended mode: Plan" given its size (11 scenarios drawn from `docs/architecture/PLAYWRIGHT-ADOPTION-PLAN.md`) — this phase planned then built it. Research during planning found 2 of the 11 scenarios (preview-as-student no-write protection, teacher content edit/draft/publish) need a real, reachable Supabase backend with no offline/mock auth path anywhere in the code (`apps/web/src/lib/supabase-client.js`'s placeholder client just fails all network calls when unconfigured) — owner decision: **defer both**, ship the other **9** now, documented here as backlog needing a dedicated disposable Supabase test project rather than pointed at the repo's live shared project.
  - **New infra, no `apps/web/src/` changes**: `playwright.config.js` (repo root, `testDir: tests/e2e`, `webServer` auto-starting `npm run dev` against port 5173), `@playwright/test` added as a devDependency (the repo already had bare `playwright` from the tooling audit, but not the test-runner package), `npm run test:e2e` script. `vite.config.js`'s `server.open: true` gained a one-line `!process.env.E2E` guard (the `webServer` config sets `E2E=1`) so the automated test run doesn't pop a real OS browser window every time it starts the dev server — the one deliberate exception to this session's usual "avoid touching `vite.config.js`" caution, justified by that concrete need. `eslint.config.js` gained a `process: "readonly"` override for `vite.config.js`/`playwright.config.js`, matching the existing `scripts/**`/`api/**` Node-context pattern.
  - **`tests/e2e/helpers/progress-seed.js`**: seeds `localStorage` directly (mirroring `chronicle-progress-store.js`'s key/merge shape) to jump near a target screen without replaying onboarding — `readProgress()`'s deep-merge means a seed only needs to name fields that differ from `DEFAULT_PROGRESS`, and a seed with any key at all auto-resolves `progress.tutorial` to `"complete"` (bypassing the post-onboarding guided-tour movement lock) unless overridden. **A real gotcha found and fixed during this phase**: Playwright's `addInitScript` re-runs before every navigation in a page, including `page.reload()` — an unconditional `localStorage.setItem` there would silently clobber real gameplay writes back to the original seed on the one scenario (save-persistence) that deliberately reloads mid-test. Fixed by only seeding when the key is still empty.
  - **9 scenarios shipped** (`tests/e2e/*.spec.js`, one file per scenario): boot/onboarding (the one spec that plays real onboarding rather than seeding, using `page.emulateMedia({reducedMotion: "reduce"})` to collapse the typewriter/hallway-walk animations), Main Hall movement + `HUB_BLOCK_RECTS` collision, Archive Room entry/exit + Terminal proximity interaction, field movement/collision + dialogue (the one that actually exercises "camera is a pure function of position" — confirmed live that the Main Hall has no scroll/camera transform at all, so that assertion belongs here, not in the hub-movement scenario the roadmap originally paired it with), one Investigation Challenge (case-001 `taino-context`, driven for real since `investigationScreen()` reads an ephemeral module variable that can't be seeded), one Archive Challenge (case-006, directly seedable), one Practice Check covering all 4 quest types, local save persistence across a real reload, and the legacy-save-route boot-guard fallback (3 sub-cases: unknown screen name, `travel` without an active case, `travel` with one).
  - **Real, previously-undocumented app behavior confirmed live while writing these** (not app bugs — just things the plan's research pass hadn't nailed down to the exact line): `showMainMenu` is a runtime-only variable always `true` on cold boot/reload, so even a fully-seeded save still has to click through Student → Load Save; the identity screen's name input is pre-filled with the default profile name (`"Chronicler"`), so testing the empty-name validation path requires clearing it first, not just landing fresh; that same input has `maxlength="14"`; `practiceCheckScreen()`'s overall-complete counter treats every MCQ item as complete once _answered_, not once _correct_ (matching `main.js`'s existing `if (answered) overallComplete += 1` for that one quest type specifically), so reaching a full N/N count requires answering (not necessarily correctly solving) every MCQ item; every quest-type's root render element carries a `.quest` class in addition to `data-quest-id`, needed as a scoping anchor since nested `<select>`/`<option>` elements inside evidence-organizing quests also carry `data-quest-id`.
  - **Verification**: `npm run test` 403/403, `npm run lint`/`npm run build`/`npm run validate:content` (89/89) all unchanged from baseline (confirmed the two new `no-undef` errors from `vite.config.js`/`playwright.config.js`'s `process` usage were fixed via the eslint.config.js override above, not suppressed). `npx playwright test` run clean multiple times in a row, including with `--repeat-each=2` under 6 parallel workers, confirming no flakiness — one field-movement collision assertion needed a looser timing margin after an initial run showed it right at the boundary under parallel-worker CPU load. Orphaned dev-server processes from repeated local `playwright test` invocations (an artifact of iterating on selectors, not of the shipped config) were cleaned up before finishing.
- Phase 30 (2026-07-23, same day, native `<dialog>` for the delete-confirmation — item 3 of the same modernization roadmap): replaced the hand-rolled two-click "Delete" → "Confirm delete"/"Cancel" button swap (`manageContentConfirmDeleteId`, a module-level scalar tracking which `custom_content_items.id` was mid-confirm) with a real `<dialog>` per teacher-added-question card, opened via `showModal()`. Native `<dialog>` gives focus trapping and Escape-to-cancel for free — the exact two gaps the roadmap flagged this UI as the one modal-shaped surface in the app missing. The Cancel button is a plain `<button type="submit">` inside a `<form method="dialog">` with no `data-action`, so it closes via the browser's own dialog-form-submission behavior with zero JS; "Confirm delete" keeps `data-action="confirm-delete-addition"` through the existing `handleManageContentClick` dispatch, closing the dialog by id lookup (`delete-confirm-dialog-${id}`) before calling the unchanged `deleteCustomContent()`. New CSS (`.manage-content-delete-dialog`/`::backdrop`) reuses the app's existing navy/gold/blur visual vocabulary (`--navy`, the same `rgba(239,204,122,…)` border and `rgba(2,20,31,.46)`/`blur(3px)` backdrop already used by `.hub-dialogue`), not a new pattern.
  - **Verification**: `npm run test` 403/403 unchanged (no new pure/exported logic — this is markup/event-wiring only), `npm run lint`/`npm run build` unchanged from baseline (same pre-existing warnings, 0 errors). No committed Playwright spec exercises Manage Content's delete flow yet (the roadmap item's own "re-run the existing Playwright flow" assumed coverage that Phase 29 didn't actually ship — none of the 9 committed scenarios touch Manage Content), so this was verified live instead: signed in via the dev-only fake-teacher shortcut, added a real custom MCQ question to case-003's General Questions, then confirmed (a) clicking Delete opens the dialog as a real top-layer modal with Cancel receiving initial focus, (b) pressing Escape closes it with the question left intact, (c) reopening and clicking Confirm delete closes the dialog and actually removes the item (card gone, "Publish to students" correctly returned to disabled). Zero console errors throughout.
- Phase 31 (2026-07-23, same day, global Escape-key dismissal — item 4 of the same modernization roadmap): added one `handleEscapeDismiss()` function (`main.js`, called from a new `key === "escape"` branch at the top of `handleWindowKeydown`, before any screen-specific dispatch, so it fires regardless of `progress.currentScreen`) covering the two non-`<dialog>` overlay surfaces the roadmap item named: field/hub NPC dialogue bubbles (`progress.activeFieldNpc` / `hubDialogueId`, mirroring the existing `field-dialogue-close`/`hub-dialogue-close` click handlers exactly) and the teacher "Preview as student" banner (`previewSession.active` → calls the existing `exitContentPreview()`, same function the banner's own "Exit preview" button already calls). Item 3's native `<dialog>` (Phase 30's delete-confirmation) needed no new code — the browser already closes it on Escape via its own close-watcher — but `handleEscapeDismiss()` opens with a `document.querySelector("dialog[open]")` guard that returns immediately if one is open, so this handler never fights with or double-handles that native behavior. No `event.preventDefault()` call was added (Escape has no default browser action to suppress outside a `<dialog>`/fullscreen context, and the guard above already defers to the one case that does).
  - **Verification**: `npm run test` 403/403 unchanged (no new pure/exported logic — `handleEscapeDismiss()` only calls existing functions/mutates existing module state), `npm run lint` unchanged from baseline (same pre-existing 5 warnings, one shifted line number, 0 errors — confirmed via `git stash` diff), `npm run build`/`npm run validate:content` (89/89) unchanged. No committed Playwright spec covers Escape dismissal yet (the same pre-existing Manage-Content coverage gap Phase 30 noted), so verified live against the real dev server: (1) opened Dr. Amani Soto's Institute hub dialogue via a real proximity-gated "E" interact, pressed Escape, confirmed it closed exactly like the existing "×" close button; (2) opened the Taíno gardener's field dialogue bubble the same way, pressed Escape, confirmed the same clean close; (3) signed in via the dev-only fake-teacher shortcut, opened Case 1.01's real "Preview as student" flow into the live walkable field, confirmed the preview banner was showing, pressed Escape, confirmed it called `exitContentPreview()` and landed back on the real `manage-content-case` editor screen (not just hid the banner). Zero console errors throughout. The native-`<dialog>` guard itself was verified by code inspection rather than a live click-through — Phase 27 disabled the only UI path ("+ Add new question") that creates a deletable custom question, so no dialog-eligible content exists to open right now; the guard is a one-line early return with no interaction with the dialog's own native behavior, and `npm run test` stayed green throughout.
- Phase 32 (2026-07-23, same day, curated-alternate-content picker — item 5 of the same modernization roadmap, planned first per the roadmap's own "owner check-in recommended" note): closed the whole-editor gap Phase 27 first flagged and Phase 28-31's own quickref summary kept naming as unstarted — `contentUiState.slots[].alternatives` (curated + custom alternate metadata) has been computed by `loadManageContentCaseData()` since Phase 23 but nothing ever rendered it; a teacher could only replace a slot by writing a brand-new custom question through the full authoring form. Added one `<select data-alt-select>` per official slot in `manageContentCardSummaryMarkup()`'s existing `.manage-content-slot-controls` row (a `"— Official version —"` option plus every entry in `entry.alternatives`, labeled `"(teacher-added)"` when `kind === "custom"`), wired through a new `handleAppChange` branch that calls the already-existing, already-generic `setDraftSelection(classroomId, caseId, slotKind, officialId, altId, altKind)` then reloads the case. No new repository method, schema, or migration — `hasUnpublishedDraft`, `publishCaseSelections()`, and `enterContentPreview()` were all already kind-agnostic (confirmed by reading each before building). Picking `"— Official version —"` sends a null `altContentId`, which `setDraftSelection` already treats as "delete the draft row" — this doubles as the first revert-to-official control this screen has ever had, for free.
  - **One real bug fixed along the way**: `groupAlternativesBySlot()` (`remote-content-selection-repository.js`) only read `content.title || content.prompt` for an alternative's dropdown label — case-002's ledger-record alternates use `content.label` instead (see `case-002-ledger-alternates.js`), so its option rendered with blank text. Added `|| content.label`. This was dead code with zero visible effect until this phase's picker was the first thing to ever render `.alternatives` for any slot kind.
  - **Verification**: `npm run lint`/`npm run build` unchanged from baseline (same pre-existing warnings, 0 errors). No unit/Playwright test was added — this is pure UI wiring over repository functions (`setDraftSelection`/`publishCaseSelections`/`resolveSourceSlot`/`resolveQuestSlot`) that already have coverage in `tests/unit/remote-content-selection-repository.test.js`; a first teacher-flow Playwright spec needing seeded Supabase fixtures is flagged as separately-scoped future work, not bundled in. Verified live instead, signed in via the dev-only fake-teacher shortcut against the real project: on a case-003 quest slot, picked a curated/custom alternate → pill flipped to "Draft — not yet published" and Publish enabled; picked `"— Official version —"` → draft correctly reverted (pill still reflected the real draft/published mismatch); re-picked the original alternate → pill returned to "Published", leaving the classroom's real state exactly as found. **Also live-confirmed a pre-existing gap, not caused by this phase**: picking a curated alternate for a case-002 ledger-record slot fails with a Postgres check-constraint violation on `classroom_content_selections.slot_kind` — `0010_ledger_record_slots.sql` (written in Phase 27) fixes this but is confirmed, via this real failed write, still not applied to the live project. Owner will apply it by hand.

## 6. Exact next phase

- Phase 33 (2026-07-23, same day, centralize the repeated teacher-screen async/error-state pattern — item 6 of the same modernization roadmap): added two small helpers next to the `teacherUiState`/`contentUiState`/`gradingUiState` declarations — `reportUiError(state, err, fallback)` (sets `.error` from an error object or a fallback string, no render — for use inside an existing `try/catch` whose caller already renders after) and `catchUiError(state, fallback)` (returns a `.catch()`-ready function that does the same and also renders — for the far more common `promise.then(...).catch((err) => { x.error = ...; render(); })` shape). Replaced all 16 call sites across these 3 UI-state objects (`loadTeacherDashboardData`, `openGradingScreen`, `loadManageContentCaseData`'s 3 try/catch blocks, plus 13 promise-chain `.catch()` sites spanning classroom creation/roster provisioning/password reset/student removal/unit advancement/source-pool toggling/manual grading/question save-publish-delete/alternate-content selection) — a wider sweep than the roadmap item's own "~5 blocks" estimate, once every occurrence of the identical shape was actually grepped rather than sampled (the same kind of undercount item 7's own count needed correcting for during Phase 29). Also added `feedbackError(state)`, a one-line markup helper replacing the `${state.error ? '<p class="feedback error">...' : ""}` fragment repeated 3 times (teacher dashboard, grading, Manage Content) — `authUiState`'s 4 occurrences of the same fragment were deliberately left alone since that state is shared with the student join/login screens, outside this item's "teacher-screen" scope. Net effect: `main.js` shrank by 20 lines (8,037 → 8,017) despite adding the 3 new helper functions, confirming the roadmap's "~25 lines removed" estimate was in the right range. No behavior change intended or observed — every replacement preserves the exact same fallback message, the same `render()` timing relative to the state mutation, and the same markup output.
  - **Verification**: `npm run lint` unchanged (same pre-existing 5 warnings, 0 errors), `npm run build` unchanged, `npm run validate:content` 89/89 unchanged, `npm run test` 402/403 (the 1 failure, `scripts-assets-audit-glob.test.js`'s Windows-path-normalization case, is pre-existing and unrelated — confirmed by re-running it against a `git stash` of this change, same failure). Verified live against the real dev server using the dev-only fake-teacher shortcut (a real Supabase round trip, same account Phase 30/32 used): (1) Teacher Dashboard loaded cleanly through the now-consolidated `loadTeacherDashboardData()` try/catch, showing the existing "Dev Test Classroom (RTZ547)" and its roster/status/progress table with zero console errors; (2) clicking "Create classroom" with an empty name produced the exact "Enter a classroom name." inline error via the new `feedbackError(teacherUiState)` helper, styled identically to before; (3) opening a real case with quest content (Triangle Ledger, case-005) through `loadManageContentCaseData()`'s full try block (a real `Promise.all` against `listSelectionsForCase`/`listCustomContentForCase`) rendered its accordion/questions correctly, and opening a Map Mission case (case-001) exercised the function's early-return branch (no network call) cleanly — both with zero console errors.

- Phase 34 (2026-07-23, same day, consolidate `previewSession`'s repeated manual guard — item 7 of the same modernization roadmap): the 6 remaining direct reads of `previewSession.active` (`save()`, `chrome()`'s banner render, `runEvaluation()`'s real-submission-recording skip, `handleChromeClick()`'s `home`/`archive-room` branches, `handleEscapeDismiss()`) now go through two small named helpers added next to `exitContentPreview()`: `isPreviewingContent()` (the predicate itself — `save()`, the banner check, and the evaluation-recording guard now read `previewSession.active` through it rather than reaching into the object directly) and `exitPreviewIfActive()` (the 3 identical `if (previewSession.active) { exitContentPreview(); return ...; }` bodies at `home`, `archive-room`, and Escape dismissal collapsed to `if (exitPreviewIfActive()) return ...;`). No behavior change — every guard still short-circuits on exactly the same condition it did before, just through one named predicate instead of 6 places independently reaching into `previewSession`'s internal shape.
  - **Verification**: `npm run lint` unchanged (same pre-existing 5 warnings, 0 errors), `npm run build` unchanged, `npm run test` 402/403 unchanged (the 1 failure is the same pre-existing Windows-path-normalization case Phase 33 already noted, unrelated to this change). Verified live against the real dev server using a throwaway Playwright script (not committed — the 2 preview/content-edit e2e scenarios needing a live Supabase teacher account are still deferred per Phase 29) driving the dev-only fake-teacher shortcut: entered "Preview as student" on a real map case, pressed Escape and confirmed the banner closed (`exitPreviewIfActive()`'s Escape path), re-entered preview and clicked the header's Institute/home button and confirmed it exited preview and landed back on the Manage Content case editor rather than navigating to the Institute (`exitPreviewIfActive()`'s `home` path) — zero console errors throughout. `archive-room` calls the identical `exitPreviewIfActive()` guard, so it wasn't separately click-tested live (same pattern precedent as Phase 31's dialog-guard note).
  - `main.js` grew by 8 lines (8,017 → 8,025) despite removing the 3 duplicated inline bodies — the two new named helpers (with their explanatory comments) cost more lines than the inline duplication they replaced, matching the roadmap item's own framing that the win here is fewer places a future edit could miss, not a line-count reduction.

- Phase 35 (2026-07-23, same day, native `<details>`/`<summary>` prototype for the Manage Content unit accordion — item 8 of the same modernization roadmap, the roadmap's own "bounded prototype" item): `manageContentUnitSectionMarkup()` (called from `teacherUnitsTabMarkup()`, the Teacher Dashboard's Units tab) swapped its hand-rolled `<section>` + `<button>` + manual `▸`/`▾` chevron span + `aria-expanded` for a native `<details class="manage-content-unit"${isOpen ? " open" : ""}>`/`<summary class="manage-content-unit-toggle" data-action="toggle-manage-content-unit" ...>`. The specific concern Phase 26's decision log recorded — that `render()`'s full `app.innerHTML` replacement resets native disclosure state — does not apply here, because the `open` attribute is still emitted from `manageContentExpandedUnitId` on every render exactly like the old `aria-expanded` was; nothing relies on the browser remembering DOM state across a replace. What native `<details>` actually buys: `handleAppClick()` already calls `event.preventDefault()` before dispatching any `data-action`, which suppresses the browser's own click-to-toggle on `<summary>`, so there's no double-toggle race between native behavior and the state-driven re-render — confirmed empirically (see Verification), not just reasoned about. New CSS is scoped to the `summary.manage-content-unit-toggle` tag selector specifically (`::-webkit-details-marker` hidden, a `::before` chevron toggled by the `details[open] > ...` selector) so the *other*, unrelated accordion sharing the same `.manage-content-unit-toggle` class name (`teacherSourcesTabMarkup()`'s Sources-pool unit list, still a plain `<button>`, `toggle-sources-unit` action, out of this item's scope) renders unaffected. The prototype succeeded — no follow-up migration of the Sources tab's accordion to `<details>` is scoped or scheduled by this phase.
  - **Verification**: `npm run lint`/`npm run build` unchanged from baseline (same pre-existing warnings, 0 errors), `npm run test` 402/403 unchanged (same pre-existing Windows-path-normalization failure noted since Phase 33, unrelated). Before touching the live app, the exact mechanic in question (open-state survival across a full innerHTML replace, and the preventDefault/native-toggle interaction) was isolated in a standalone Playwright harness reproducing the real markup/CSS/click-delegation pattern byte-for-byte — confirmed: click opens (single toggle, no race), an unrelated full re-render leaves the open attribute unchanged, a second click closes, and native keyboard `Enter` on the focused `<summary>` toggles it too (a11y benefit that didn't exist on the old `<button>` path, which relied on the same `data-action` dispatch either way). Then verified live against the real dev server and real Supabase project, signed in via the dev-only fake-teacher shortcut (same "Dev Test Classroom (RTZ547)" account Phases 30/32/33/34 used, no data written): opened the Units tab, clicked Unit 1's summary open (chevron rotated ▸→▾, mission grid rendered correctly, scrolled to confirm all 3 Unit-1 mission cards intact), switched to the Classrooms tab and back to Units (a real unrelated full re-render) and confirmed the unit was still open, clicked again to close. Zero console errors throughout both the harness and the live pass.
- Phase 36 (2026-07-24, `aria-live` audit follow-up — item 9 of the same modernization roadmap; shipped in commit `2262b9f` but not folded into this file until now, the exact kind of one-phase documentation lag §12/CLAUDE.md's update rule warns against): the Manage Content draft/published status pill (`manageContentCardSummaryMarkup()`, `main.js`) and `feedbackError()` (the shared markup helper Phase 33 added for `teacherUiState`/`gradingUiState`/`contentUiState`) were the two remaining dynamic-text surfaces that didn't announce updates to screen readers, unlike the 17 other confirmed `aria-live` uses elsewhere in the app. Both now carry `role="status" aria-live="polite"` — a two-line change, no new function, no markup restructuring.
  - **Verification**: `npm run test` unchanged (no exported/pure logic touched), `npm run lint`/`npm run build` unchanged from baseline. Not independently re-verified live in this pass (folding in an undocumented prior commit) — the change is two attribute additions to existing, already-live-verified markup (the status pill from Phase 24, `feedbackError()` from Phase 33), not new interaction surface.
- Phase 37 (2026-07-24, documentation-only fix for the "two Manage Content screens" split — item 10 of the same modernization roadmap, the roadmap's own last item): `manageContentScreen()`, the standalone unit/mission-listing screen this file's Phase 23/24 entries above describe as one of two Manage Content screens (alongside `manage-content-case`), no longer exists — `main.js:2687-2690` carries a comment stating it "was folded into the Teacher Dashboard's Units tab (`teacherUnitsTabMarkup()`)"; `manageContentUnitSectionMarkup()`/`manageContentMissionCardMarkup()` are called from there instead. `VALID_SCREENS` (`main.js:1452`) confirms this directly: it lists `"manage-content-case"` but has no `"manage-content"` entry. This consolidation happened silently sometime before Phase 28 with no dedicated phase entry recording it — `FOCUSED-UI-AND-MECHANICS-REUSE-AUDIT.md` §0 (2026-07-23) is what first caught the drift between the docs and the code. No source, schema, or dependency change in this phase — this entry, plus the correction below, is the fix. The Phase 23/24/25/26/27 entries above are left as-is (they're accurate as a historical record of what existed *at the time*); only the still-current summary line at the end of this section is corrected.
- Phase 38 (2026-07-24, applied the two outstanding migrations — not part of the modernization roadmap, which closed at Phase 37; this is the migration-application backlog Phase 27/32 flagged): owner applied `0009_classroom_unit_source_pool.sql` and `0010_ledger_record_slots.sql` by hand via the Supabase SQL editor against the live project (`rxhpxqbtrnswjevcuxon`). Live-verified both via a scripted Playwright pass against the real dev server, signed in through the dev-fake-teacher shortcut: on case-002's Manage Content editor, selecting the one curated ledger-record alternate ("Maize") for its Exchange Ledger slot now succeeds with no error (status pill flips to "Draft — not yet published"), confirming `0010`'s widened `slot_kind` CHECK constraint fixed the exact check-constraint violation Phase 32 hit live; reverting the selection back to "— Official version —" also succeeded cleanly (pill back to "Published"), leaving the classroom's real state exactly as found. On the Teacher Dashboard's Sources tab, expanding Unit 1 (a real `getUnitSourcePool` read against the now-live `classroom_unit_source_pool` table) and toggling a source's pool membership (a real `setSourceInPool` write) both succeeded with no error, and toggling back restored the prior state. No source/code changes were needed — both features' frontend/repository code (`remote-source-pool-repository.js`, the case-002 `ledger-record` slot-kind plumbing) had been fully built and wired since Phase 27; this phase only unblocked the database layer. Also fixed two stale `CLAUDE.md` claims caught during this pass: the "migration `0007` still needs to be applied" line (stale since Phase 26) and a reference to `manageContentScreen()` as a still-existing standalone screen (stale since Phase 37).
  - **Verification**: no source changes, so `npm run test`/`npm run validate:content`/`npm run lint`/`npm run build` are unaffected by definition — confirmed unchanged from baseline anyway. A raw service-role write/read script against the database was attempted first and not used: a direct-write probe was blocked by the harness's own action classifier, and a separate read-only probe returned "permission denied for table" for `classrooms`/`classroom_content_selections` (the local `.env.local` service-role key does not currently have working table grants against this project — a separate, non-blocking finding, since the deployed app only ever uses the anon key through PostgREST, never this key). Verification went through the real app UI instead, which is both the safer path and this repo's own established convention for exactly this kind of check.

**All migrations through `0010_ledger_record_slots.sql` are now applied to the live project** (`rxhpxqbtrnswjevcuxon`) **and live-verified (Phase 38)** — Case 1.02 ledger-record alternate-picking and the classroom Sources-pool feature both persist correctly now. The actual end-to-end verification neither Phase 22 nor Phase 23 could complete locally (the claim/login/provision/reissue/evaluate/grade round trips and RLS cross-tenant isolation from Phase 22's own pending list) is still outstanding. **The curated-alternate-content picker gap flagged since Phase 27 shipped in Phase 32** — a `<select>` now renders `entry.alternatives` on every editable slot; not scheduled: an `Assignment`/due-date model, richer analytics, more cases'/quest-types' worth of curated alternate content, a real SAQ quest type (a prerequisite for SAQ ever appearing in the authoring type picker), unlocking source authoring for map-mission cases (would require walkable-field NPC/placement tooling), extending real ledger-record editing to case-008's Founding Debate ledger (mechanically cheap — `foundingScreen()`/`FOUNDING_RECORDS` weren't touched this phase, deliberately out of the approved scope), or closing the confirmed "add new question" render-path gap Phase 27 chose to disable rather than fix (would need `archiveChallengesScreen()`/`practiceCheckScreen()` to also read `contentUiState.additionSlots`-equivalent data, a larger change than this phase's UI-focused scope).

Separately, unit content work continues independently: Unit 3 now has 2 of its planned "flagship + companion(s)" cases (Phase 21); `case-009` remains an explicit, unscheduled future phase per `unit-03-campaign.js`'s own header comment. The Investigation/Archive Challenge catalog (Phases 0–19) still covers 5 Investigation modes and 7 Archive modes of the plan's original 6+6 Launch-tier catalog — remaining backlog, not scheduled: `observation-challenge` (needs a new hotspot-click interaction engine), the literal `political-cartoon` mode (needs a political-cartoon source), the "Second wave"/"Experimental" tier modes from the plan's §6 catalog, and migrating `empireScreen()`/`triangleScreen()` off the Navigation Table now that their content also has an Archive Challenge home. The `QUEST_TYPES`-module-level `isComplete`/`answeredAny`/`hint` consolidation flagged here since Phase 19/20 is **no longer unscheduled follow-up — it shipped in Phase 28**.

**Modernization roadmap (`docs/architecture/FOCUSED-MODERNIZATION-ROADMAP.md`, 2026-07-23) — all 10 items of its ranked top 10 are now shipped** (Phase 28: quest-type consolidation; Phase 29: the Playwright suite, 9 of its 11 planned scenarios; Phase 30: native `<dialog>` for the delete-confirmation; Phase 31: global Escape-key dismissal; Phase 32: the curated-alternate-content picker; Phase 33: centralized teacher-screen error-state handling; Phase 34: consolidated `previewSession`'s repeated manual guard; Phase 35: the native `<details>`/`<summary>` prototype for the Manage Content unit accordion — prototype succeeded and shipped as the real implementation, not just a discarded experiment; Phase 36: `aria-live` audit follow-up on the status pill and `feedbackError()`; Phase 37: this documentation-only correction). **`manageContentScreen()` no longer exists as a standalone screen** — it was consolidated into the Teacher Dashboard's Units tab (`teacherUnitsTabMarkup()`, calling `manageContentUnitSectionMarkup()`/`manageContentMissionCardMarkup()`) sometime before Phase 28, undocumented until Phase 37 above; `VALID_SCREENS` has only `"manage-content-case"`, not `"manage-content"` — don't describe this as "two Manage Content screens" going forward. The roadmap is fully closed; no further roadmap-driven phase is queued. **The migration-application backlog from Phase 27/32 is closed — `0009`/`0010` applied and live-verified in Phase 38.** Still outstanding, unrelated to the roadmap: scenarios 9/10 from the Playwright adoption plan (deferred in Phase 29, need a dedicated Supabase test project), and the end-to-end claim/login/provision/reissue/evaluate/grade round-trip and RLS cross-tenant isolation verification flagged since Phase 22.

Do not begin Phaser, Tiled-as-authoring-pipeline, inkjs, `WorldComposition`, `QuestEngine`'s renderer/evaluation registries, `WorldRuntime`, full `packs/<subject>/` extraction, or a `TeacherWorld`/`PublicationVersion` draft-publish-versioning pipeline without a concrete forcing function — see `POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10 for the specific conditions that would count. `PlatformCore`/accounts/classrooms are no longer on that deferred list — Phase 22 above is that forcing function having become real. **Playwright is no longer on that deferred list either** — Phase 29 above is a real, committed, rerunnable suite, not just the ad hoc usage this section used to describe.

## 7. Approved immediate dependencies

- **Vitest** and **Zod** — the only two adopt-now major dependencies from the original architecture pass. No POC required for either. **Both are now installed**: Vitest (plus `jsdom` as its DOM-environment dependency) from Phase 2, Zod from Phase 3.
- **`@supabase/supabase-js`** — approved and installed in Phase 22 (Postgres + Auth + RLS), the concrete backend for real accounts/classrooms/submissions/grading. Used both from the browser (anon key, RLS-scoped) and from `api/` serverless functions (service-role key, privileged roster operations only).
- **Playwright** — the `playwright` core library was already installed (tooling audit era); Phase 29 added the actual `@playwright/test` test-runner package plus `playwright.config.js`/`tests/e2e/`, converting years of ad hoc manual passes into a real, committed, rerunnable suite (9 of 11 planned scenarios; 2 deferred, need a dedicated Supabase test project). `npm run test:e2e` runs it.
- ESLint + Prettier are already installed and working (`npm run lint`, `npm run format`) — not new, just confirm they stay enforced.

## 8. Deferred systems and tools

- **Tools deferred, not rejected:** Phaser 4.1.x, inkjs. Real candidates, no concrete forcing function yet (Phaser: no perf complaint exists yet; inkjs: today's dialogue is one static line per NPC, no branching need exists yet). **Playwright is no longer deferred** — see §7 above; Phase 29 shipped a real committed suite, not just ad hoc usage. **Tiled is no longer fully deferred**: `.tmj` rendering via `apps/web/src/engine/tiled-map-loader.js` is now an established narrow pattern for three real, live maps (Unit 2's Riverbend field since the Phase 7-era proof of concept, Unit 1's Caribbean field since Phase 12, and the Institute Archive Room since Phase 14 — see `docs/decision-log/0029-caribbean-tiled-rebuild.md` and `0030-archive-room-tiled-interior.md`). This is still not license for a bigger Tiled-authoring pipeline, a desktop-editor workflow, or a `QuestEngine` renderer registry — collision stays hand-coded in `main.js`, generated to match the tile art rather than the reverse.
- **Architecture deferred, not rejected:** `WorldComposition` (Blueprints/AI-generation/Publishing, including the full `TeacherWorld`→`PublicationVersion` draft/publish/versioning pipeline — Phase 22's content overrides deliberately stayed a flat classroom-scoped patch, not this), `QuestEngine`'s renderer/evaluation registries, `WorldRuntime`, 2 of the proposal's 7 repositories still not built (`World`/`Asset` — `Auth`/`Classroom`/`Submission` are now real as of Phase 22, `Content`/`Progress` were already real), full `packs/<subject>/` extraction. Revisit only when a second real subject pack exists — not on a calendar schedule. **`PlatformCore` (Identity/Classroom/Enrollment) is no longer deferred** — a real (if minimal) version shipped in Phase 22 once a real teacher/classroom pilot became real, per the exact condition this section used to name.
- **Explicitly rejected:** Yarn Spinner (unshipped official JS runtime), H5P-as-platform-core (state-ownership conflict), Spine, LDtk, TexturePacker (no current problem they solve). Consider-later, not adopted: Storybook, Phaser Editor v5, Sentry, GitHub Actions CI, axe-core/Lighthouse CI.

## 9. Systems that must not be restored

- Founder Paths, professions, Historian Skills, clothing/wardrobe-slot systems — removed from the design, do not reintroduce even if a future request seems to imply them.
- The player identity model stays minimal: display name + one of two appearance choices only.

## 10. Verification commands currently available

- `npm run dev` / `npm run build` / `npm run preview` — real, working (Vite).
- `npm run lint` — real, working (ESLint flat config).
- `npm run format` / `npm run format:check` — real, working (Prettier).
- `npm run validate:content` — real, working, added in Phase 3. Runs `scripts/validate-content.js` under plain Node (not Vite), validating `unit-01-campaign.js`/`unit-02-campaign.js` with Zod schemas from `apps/web/src/content/schemas/` plus two cross-file id-uniqueness checks. See `docs/content/CONTENT-VALIDATION.md`.
- `npm run test` (Vitest, non-watch, CI-compatible) and `npm run test:watch` — real, working, added in Phase 2. Config: `vitest.config.js` (repo root, deliberately separate from `vite.config.js`). Tests live in `tests/unit/`. See `docs/development/UNIT-TESTING.md`, `docs/content/CONTENT-VALIDATION.md`, and `docs/architecture/LOCAL-PROGRESS-REPOSITORY.md`.
- `npm run test:e2e` (Playwright, real browser against a real dev server) — real, working, added in Phase 29. Config: `playwright.config.js` (repo root). Specs live in `tests/e2e/*.spec.js`, one file per scenario, sharing `tests/e2e/helpers/progress-seed.js` for localStorage-based state seeding. 9 of 11 planned scenarios; 2 deferred pending a dedicated Supabase test project (see Phase 29 above).
- Manual browser verification via `npm run dev` remains required for any player-visible change — lint/build passing is not sufficient, per `CLAUDE.md`'s development-workflow expectations.

## 11. Placeholder documents repaired in this pass

Four confirmed placeholder-stub documents (verbatim "Recovered placeholder file restored..." body) were found and repaired: `docs/architecture/repository-map.md`, `docs/content-guide/naming-and-placement.md`, `docs/decision-log/0001-engine-and-campaign-boundaries.md`, `docs/vertical-slice/case-1-01-atlantic-crossroads.md`. (`CURRENT-REPOSITORY-AUDIT.md` named the first, second, and fourth explicitly and reported "4 of ~50 markdown files" as stubs without naming the fourth; a direct repository grep for the stub's exact boilerplate text confirmed `decision-log/0001` as that fourth file — it is repaired here too since it's the founding ADR `README.md` and `CLAUDE.md` both point readers to.)

## 12. Longer documents (consult only when you need the rationale)

- `docs/architecture/CURRENT-REPOSITORY-AUDIT.md` — verified current-state findings.
- `docs/architecture/art-and-map-style-guide.md` — living reference for canonical tileset-element dictionary (which pack/sheet supplies grass, trees, buildings, etc., per historical setting). Updated during planning passes. Pairs with `docs/decision-log/0031-art-style-unification.md` and `docs/decision-log/0032-common-cause-tiled-rebuild.md` (the latter closed the guide's one Gap Register entry — case-007's Common Cause field is now a real `.tmj`, not CSS-drawn).
- `docs/architecture/THIRD-PARTY-TOOLING-AUDIT.md` — dependency research and verdicts.
- `docs/architecture/PLATFORM-ARCHITECTURE-PROPOSAL.md` — the long-term multi-subject-platform design.
- `docs/architecture/ARCHITECTURE-REVIEW-AND-SIMPLIFICATION.md` — the binding near-term scope cut; when it disagrees with the proposal on what to build _now_, follow this document.
- `docs/development/UNIT-TESTING.md` — Phase 2 writeup: what's tested, why, the `main.js` import-safety guard, and what's deliberately untested.
- `docs/content/CONTENT-VALIDATION.md` — Phase 3 writeup: schemas added, the `local-content-repository.js` wrapper, the validation command, cross-reference checks, and known limitations (notably: `main.js`'s own embedded NPC/coordinate/badge dictionaries aren't reachable by a plain-Node validator script).
- `docs/architecture/LOCAL-PROGRESS-REPOSITORY.md` — Phase 4 writeup: why it's the only repository added, the actual (corrected) call-site count, save versioning, and save-compatibility verification.
- `docs/migrations/DEAD-CODE-REMOVAL.md` — Phase 5 writeup: per-candidate reverification results, what was deleted vs. preserved, reference cleanup performed, and post-deletion verification.
- `docs/teacher-mode/MINIMAL-LOCAL-OVERRIDES.md` — Phase 6 writeup: the two confirmed-broken fields, the override store's shape/stable-key convention/validation, resolution and reset behavior, and the manual verification procedure.
- `docs/architecture/POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` — Phase 7 writeup: verification status of all prior phases, remaining defects, the forcing-function evaluation for every deferred tool/system, the recommended next product milestone, and the conditions that should trigger another reassessment.
- `docs/architecture/QUEST-TYPE-ARCHITECTURE.md` — Phase 8 writeup (updated in place for Phase 9): all four quest types built, why each schema decision was made, verification status, the mini-games layer (§7), and an explicit flag that the quest-type layer is not yet wired into any live screen.
- `docs/architecture/session-reports/2026-07-11-overnight-quest-types-and-minigames.md` — Phase 9 session report: full punch list of what was built, judgment calls made, and what was deliberately left unfinished.
- `docs/architecture/session-reports/2026-07-18-real-teacher-mode-accounts-classrooms-grading.md` — Phase 22 session report: the Supabase schema/RLS design, every new file across both repos, judgment calls made (teacher self-serve signup), and the exact manual verification still required now that a real backend exists to test against.

## 13. Project-level Claude Code subagents

Six project subagents live in `.claude/agents/` (git-tracked, shared across sessions once the harness has restarted after their creation): `content-designer`, `map-implementer`, `content-validator`, `test-writer`, `code-reviewer`, `doc-sync`. Each is scoped to one part of the content/test/validate/review/doc-sync loop described in `docs/architecture/QUEST-TYPE-ARCHITECTURE.md` — see each file's own frontmatter for its exact tool access and responsibilities.

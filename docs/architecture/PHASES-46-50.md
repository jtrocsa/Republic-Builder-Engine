# Phases 46–50 — Chronicle / Odysso Improvement Program

**Status:** Phases 46 and 47 done; Phase 48 next. This is the one living roadmap for this program — per Phase 46E, don't create a new stand-alone audit/roadmap document per pass; extend this file instead, and update `ARCHITECTURE-QUICKREF.md`'s one-line phase index when a phase closes.

## Context

Phases 23–44 were teacher-facing; Phase 45 was a gameplay CSS/test-infrastructure pass. The owner asked for five things in one program, with no end date — "improve this every hour of every day for the foreseeable future."

Four problems drove this plan, each verified against the repo, not inferred:

1. **Session cost was structurally wrong.** A usage report showed 91% of usage at >150k context, 70% from subagent-heavy sessions, 65% from the Playwright MCP server. The causes were measurable: `ARCHITECTURE-QUICKREF.md` was 232 lines but 172,761 bytes (~43k tokens) — §4/§5/§6 alone were 160 KB of phase changelog — and `CLAUDE.md` ordered every session and every subagent to read it first. A committed 11-spec Playwright suite with 20 visual baselines already existed (`npm run test:e2e`), but `CLAUDE.md` never mentioned it and still called Playwright "deferred," so browser verification was done by hand through the MCP server. A prior audit (`docs/architecture/AI-USAGE-AND-DEVELOPMENT-COST-AUDIT.md`, 2026-07-23) produced ten prose recommendations and zero config changes; 8 of 10 were never adopted and the QUICKREF had since grown. The repo had no `.claude/settings.json`, no permissions allowlist at all.

2. **The teacher UI was not professional.** The dashboard rendered an `h1` at up to 83px directly above 12px buttons (7×). Teacher surfaces used ~26 distinct font sizes against a 6-step token scale, and bare `<h2>`/`<h3>`/`<h4>` tags with no CSS rule at all fell back to UA-default DM Sans bold. Phase 44 built a token layer and a `c-*` primitive set, then never adopted it: `fieldMarkup()`, `pageHeaderMarkup()`, `sectionHeadMarkup()` had zero call sites; `btn()` had one. Mission-card buttons didn't align, and a 41-character badge wrapped to 2–3 lines while its sibling stayed a pill.

3. **The Navigation Table hid two thirds of the game.** Only 3 of 8 cases rendered markers, because `navigationTableVisible: false` was hard-coded on case-002/003/005/006/008. Every hidden case already had a real `mapPosition`, and `caseMarker()` already implemented a greyed-out `locked` state.

4. **Chronicle covers ~1/3 of APUSH; Odysso is a brochure.** Three units (Periods 1–3), 8 cases, 4 quest types. No SAQ quest type exists (20% of the exam, 30 raw points); no DBQ/LEQ student surface exists (40% combined) even though `api/_lib/rubrics.js` already implements their rubrics. Odysso is a static React marketing site with a placeholder sign-in and no backend.

**Standing constraints that do not relax.** No Phaser, inkjs, `WorldComposition`, `QuestEngine` registries, `WorldRuntime`, or `packs/<subject>/` without a concrete forcing function (`POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10). No Founder Paths / professions / Historian Skills / wardrobes. No extracting movement/collision/camera from `main.js` for neatness. Every change leaves `npm run dev` player-visibly identical unless new behavior is the explicit goal.

**Sequencing rationale:** 46 first, because every later phase is cheaper once the context floor drops. 47 and 48 next — both are the owner's directly observed complaints, and both are largely already-built systems that need finishing rather than inventing. 49 and 50 open with a research sub-phase because the ask was to "research and consider"; the ranked lists in each are the starting position, not the conclusion.

---

# Phase 46 — Session Cost Discipline (enforced, not advisory)

**Goal:** cut the per-session context floor from ~50k to ~10k and stop paying for browser verification twice. The difference from the 2026-07-23 audit: every item is a config file or a moved/deleted line, not a paragraph of advice.

- **46A — Split the QUICKREF. DONE.** §4/§5/§6 (the phase-by-phase changelog) moved verbatim to `docs/architecture/PHASE-HISTORY.md`, an archive nothing is instructed to read. `ARCHITECTURE-QUICKREF.md` rebuilt as ≤150 lines of pure boundaries plus a one-line-per-phase index. **Measured: 172,761 bytes → 18,599 bytes (89% reduction, ~43k → ~4.6k tokens).**
- **46B — Rewrite `CLAUDE.md`'s verification and reading rules. DONE.** Replaced the "reproduce in the browser" instruction with an explicit verification ladder (targeted vitest → validate:content → `npm run test:e2e` → visual-regression → manual/MCP browser only for uncovered interactions, then banked as a new spec). Fixed the stale "Playwright is deferred" line. Added a "minimum required reading by task type" table. Removed the dangling `/verify` skill reference (no such skill exists). Added an explicit "don't spawn a subagent for a single-file edit" rule.
- **46C — Create the repo's Claude Code configuration. DONE.** Added a tracked `.claude/settings.json`: a permissions allowlist for routine read-only commands (`npm run test`/`test:e2e`/`lint`/`build`/`validate:content`/`format:check`, `git status`/`diff`/`log`, `wc`, `rg`/`grep`), and `deniedMcpServers: [{serverName: "playwright"}]` — the correct mechanism for gating a server regardless of where it's configured (verified against the real settings schema; `disabledMcpjsonServers` was the wrong key, since it only applies to servers declared in a project `.mcp.json`, which this repo doesn't have — Playwright is configured at user/global scope). Pixellab and Vercel MCP stay on, untouched. **Note: the MCP gate takes effect on the next session/`/config` reload, not retroactively within the session that added it.**
- **46D — Fix the subagent multiplier. DONE.** Fixed `.claude/agents/code-reviewer.md`, which still flagged Playwright (and implicitly Tiled) as deferred and would therefore have flagged legitimate e2e/map work as scope creep. The "when not to spawn a subagent" rule landed as part of 46B's CLAUDE.md rewrite rather than as a separate edit, since it's read from the same file every subagent inherits.
- **46E — Stop the audit-doc proliferation. DONE.** Moved 5 closed documents (`FOCUSED-MODERNIZATION-ROADMAP.md` — fully shipped, plus `PLAYWRIGHT-ADOPTION-PLAN.md`, `TEACHER-UI-ACCESSIBILITY-AUDIT.md`, `WORKFLOW-STATE-AUDIT.md`, `AUTHORING-SYSTEMS-AUDIT.md`) to `docs/architecture/archive/` with a one-line index (`archive/README.md`). This file (`PHASES-46-50.md`) is the one living roadmap going forward.

**Verification:** measured, not just asserted — QUICKREF byte count before/after recorded above. No application code was touched in this phase (docs + `.claude/` config only), so `npm run test`/`lint`/`build`/`validate:content` are unaffected by construction; confirmed via `git status` showing only doc/config files changed.

---

# Phase 47 — Professional Teacher Interface

**Goal:** one type scale, one card, one page header, one container width — a professional web app inside the existing navy/gold/parchment palette.

**Isolation strategy (load-bearing).** Teacher screens get a scope class `.c-app`, and — wherever cheap — stop emitting the shared gameplay class entirely rather than overriding it. `.completion-shell` and `.review-shell` are removed from teacher markup and replaced with `.c-page`. That deletes the conflict at the source instead of layering specificity on it.

| Selector | Verdict |
|---|---|
| `.completion-shell`, `.review-shell` | Don't touch. Stop emitting from teacher screens. Both are visual-regression baselines. |
| `.kicker`, `.btn*`, `.text-button`, `.back-link`, `.shell`, `.feedback`, `.unit-tab`, `.choice` | Fork via `.c-app` prefix only — additive overrides, never edits. |
| `.c-*` (global.css 182–511) | Free to extend — zero gameplay consumers. |
| `.manage-content-*`, `.source-pool-*`, `.case-kind-badge`, `.roster-table`, `.auth-shell`, `.manual-grade-entry` | Free to edit in place — teacher-only, zero baselines. |

There are zero teacher-screen visual baselines, so all 20 gameplay PNGs must stay byte-identical through Phase 47. That is the primary regression gate.

**Type scale — 7 tokens, one addition.** Keep the existing 6; add `--c-text-micro: 0.68rem`. Card/`h3` titles are `--c-text-body` at `700 var(--c-font-label)`, so no `--c-text-lead` is needed.

| Current cluster | → Token |
|---|---|
| 0.58 / 0.62 / 0.65 / 0.67 | `--c-text-micro` (0.68rem) |
| 0.68 / 0.70 / 0.72 / 0.75 / 0.76 | `--c-text-label` (0.72rem) |
| 0.78 / 0.80 / 0.82 / 0.85 / 0.87 / 0.88 / 0.90 | `--c-text-meta` (0.85rem) |
| 0.95 / 1.0 / 1.05 | `--c-text-body` (1rem) |
| 1.1 / 1.15 / 1.2 | `--c-text-section` (1.2rem) |
| `clamp(3rem,5vw,5.2rem)` | deleted from the teacher path — comes from `.completion-shell h1`, fixed by not emitting that class |
| bare `<h2>/<h3>/<h4>` UA defaults | fixed by a scoped element reset (`.c-app h1..h4, .c-app p, .c-app li`) — safe only because it's scoped |

### Sub-phases

- **47A — DONE.** Added `.c-app` scope block + `--c-text-micro` token (global.css); gave bare `<h1>-<h4>/p/li/dd` inside `.c-app` real rules on the token scale; retuned ~26 stray font-size values in confirmed teacher-only selector blocks (`.manage-content-*`, `.source-pool-*`, `.roster-table`, `.case-kind-badge`, `.locked-note`, `.password-field/-toggle`, `.archive-feedback-*`) — verified selector-by-selector first, since this CSS region also interleaves genuine gameplay selectors (`.completion-shell`, `.field-npc`, `.hub-player`) that must not be touched. Added `c-app` to all 13 teacher `<main>` occurrences (join/login/dashboard/grading/manage-content-case), additively alongside the existing shared classes rather than replacing them. **Verified: `npx playwright test visual-regression.spec.js` 8/8 passed, zero pixel diff across all 20 gameplay baselines; `npm test` 454/454; lint/build/validate:content clean; manually confirmed live that the dashboard's "Missions"/"Unit access" headings now render on the Spectral type scale instead of unstyled UA defaults.**
- **47B — DONE.** `teacherDashboardScreen()`, `teacherUnitsTabMarkup()`, `teacherUnitAccessMarkup()` stopped emitting `.completion-shell` (820px boxed-card, meant for gameplay completion/auth screens) in favor of `.c-page` (1180px). Hand-rolled kicker/h1/p header → real `pageHeaderMarkup()` call; bare `<h2>Missions</h2>`/`<h2>Unit access</h2>` → `sectionHeadMarkup()`. Also fixed a real `pageHeaderMarkup()` bug found while wiring it up: `eyebrow` was rendered unguarded (unlike `description`), so an omitted eyebrow rendered an empty `<p>`. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher sign-in) that the dashboard header and both Units-tab section heads render on the real type scale at 1180px.**
- **47C — DONE.** Fixed the exact reported bug: "Map Mission"/"Activity Mission — Archive Challenge only" badges didn't line up because the combined string wrapped to 2-3 lines for the long case while the short one stayed one line. `caseKindLabel()` now returns short labels only; new `caseKindDetail()` renders the qualifier as a `.c-help` line under the `<h3>`. `.case-kind-badge` (a bespoke pill duplicating `chip()`) is deleted — both call sites (mission-card kind badge, Sources tab's "Locked" source badge) now use `chip()`. `.manage-content-mission-card .btn`'s `margin-top: 8px` → `auto`, pinning "Edit mission →" to the bottom of every card in a grid row regardless of that card's own copy length. `.manage-content-mission-head` → `flex-wrap: nowrap` now that the badge is always short. Found live and fixed: `chip()` as a column-flex child was being stretched full-width by `.source-pool-row`'s default flex stretch — added `.source-pool-row > .c-chip { align-self: flex-start }`. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live that all three Unit 1 mission cards show single-line badges with aligned buttons regardless of card height, and Sources-tab "Locked" chips hug their own text.**
- **47D — DONE.** `pageHeaderMarkup()` on Manage Content's four hand-rolled headers: the signed-out guard, the no-case guard, the locked Map Mission view (previously had no heading inside `<main>` at all — relied solely on the command bar's breadcrumb), and `manageContentWizardHeaderMarkup()` (shared by every Activity Mission wizard step). `.manage-content-shell` (already 1180px) is untouched — consolidating it with `.c-page` into one container implementation is 47G's job, not this one. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live that the locked Caribbean view and the Atlantic Routes wizard header both render correctly.**
- **47E — DONE.** Extended `btn()` (`labelHtml`/`href`, for the classroom-switch-code span and the "View source ↗" external link), `fieldMarkup()` (`placeholder`/`autocomplete`/`attrs`/`control`/`select`), and `passwordFieldMarkup()` (`class="c-input"`). Converted `loginScreen()`, `joinScreen()`, `teacherClassroomsTabMarkup()`, and `gradingScreen()`'s form fields to `pageHeaderMarkup()`/`fieldMarkup()`. All existing input `id`s preserved exactly (`teacher-email`, `new-classroom-name`, `grade-label`, etc. — confirmed via grep against every `getElementById` call site); previously id-less classroom-setup-row inputs got new synthesized ids matched via their existing `data-classroom-row-*` attributes. Found and fixed a real 47B regression: removing `.completion-shell` from the dashboard had silently broken `new-classroom-name`/`provision-count` styling, since that ancestor rule was their only source of visual styling — fixed as a side effect of the `fieldMarkup()` conversion. Also fixed `gradingScreen()`'s `grade-label`/`grade-teacher-feedback`, which had never been styled at all. Caught and reverted two of my own mistaken cascade assumptions via the visual-regression suite before they shipped: (1) initially deleted `.completion-shell label/input/textarea` believing them dead, which broke the `identity-screen` gameplay baseline — restored exactly, with a comment naming `identityScreen()` as the real dependent; (2) initially recolored `.password-toggle` assuming `.c-input`'s dark background now applied to auth screens, which was wrong because `.completion-shell`'s higher-specificity `input`/`textarea` rule is still an active ancestor on login/join (only the inner field markup was converted, not the shell) — reverted to the original color. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (Playwright MCP) that Teacher Sign In, Join a Classroom, and the Dashboard Classrooms tab all render correctly.**
- **47F — DONE.** Unified the Sources tab's `teacherSourcesUnitSectionMarkup()` onto the Units tab's existing native `<details>/<summary>` pattern (`manageContentUnitSectionMarkup()`), removing the last hand-rolled `<section>`/`<button>`+chevron accordion — both tabs now share one markup shape and one `handleAppClick()` preventDefault path. The three bare `<h4>` inside it ("Locked (official)", "Unit N Mission Pool Sources", "Available pool") converted to `sectionHeadMarkup()`. Deleted the now-dead `.manage-content-unit-chevron` CSS rule and rewrote the stale comment that used to explain why two accordion patterns coexisted. Deliberately deferred `sourceRowMarkup()`'s surface (border/radius/background) to 47G rather than doing it here: 47G already folds four near-identical card surfaces (`.manage-content-mission-card`, `.source-pool-row`, `.manage-content-editor-section`, `.manage-content-source-summary-card`) onto `.c-card` in one pass, and touching `.source-pool-row` twice across two phases would be wasted motion. **Verified: visual-regression 8/8 zero diff (no baseline covers this screen, but the 20 gameplay baselines are unaffected by construction); `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher, 1366×768) that both the Sources and Units tab accordions open/close correctly and render identically styled headings.**
- **47G — DONE.** Folded all four near-identical card surfaces onto `.c-card`: `.manage-content-mission-card` (+ `.c-card--interactive`, replacing its own duplicate hover-lift rule with the shared one), `.source-pool-row`, `.manage-content-editor-section` (its `.is-collapsed` background override still wins on specificity over `.c-card`'s, so the dimmed-collapsed state is unaffected), and `.manage-content-source-summary-card`. Each surface-specific class now carries only its own layout (padding/gap/flex-direction/margin) plus any genuine state variant (focus outline, collapsed background) — border/radius/background/shadow all come from one shared rule instead of four hand-copied near-duplicates. Also folded `.manage-content-status-pill` onto `chip()`: its recipe (padding, `rgba(gold,0.4)` border, 999px radius, `#d8e3de` text) was a byte-for-byte duplicate of `.c-chip`'s default tone, so the three status-bar pills (Published Mission / Student View / Source) now render via `chip()` calls instead of a fourth bespoke pill class. Considered and explicitly deferred: consolidating `.manage-content-shell`'s outer container with `.c-page` — both already render at the same 1180px width so there's no visible bug to fix, and `.manage-content-shell`'s 235px top padding exists specifically to reserve room for the wizard's fixed header, which `.c-page` has no equivalent for; forcing them into one implementation would be pure risk for zero visible payoff. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher, 1366×768) that mission cards (with hover lift), Sources-tab rows, the Evidence Organizing editor's Question/Answer-structure sections, the source-summary card, and the bottom status-pill row (now lowercase-styled chips instead of uppercase pills) all render correctly with no layout shift.**

**Phase 47 status: DONE (47A–47G all complete).** The teacher-facing UI now has one type scale, one card surface, one page header, one container width, and one accordion pattern throughout.

**Verification per sub-phase:** `npm run test:e2e -- visual-regression` zero-diff on all 20 gameplay baselines, `npm run test` 454/454, `lint`/`build` unchanged, plus a live pass at 1366×768 (the project's named Chromebook target). Add a teacher-screen smoke e2e spec so the next pass isn't hand-verified either.

*`.legend-locked` (emitted with no CSS rule) is fixed in Phase 48, not 47 — it's a gameplay selector inside `archiveScreen()` whose baseline 48 re-records anyway.*

---

# Phase 48 — The Full Mission Board

**Goal:** the Navigation Table shows all three missions per period, locked ones greyed out and explained, available as standard and changed only when a teacher edits that mission.

The rendering already supports this: `caseMarker()` computes `complete`/`available`/`locked`, `.route-marker--locked` exists, `declutterMarkerPositions()` already fans out overlapping markers, and every hidden case has a real `mapPosition`. **But unhiding alone would ship three bugs**, which is the real content of this phase:

1. `travelScreen()`'s timeout is `currentScreen = c?.route || "archive"` — an Archive-Challenge mission warps and dumps the student back on the Navigation Table.
2. `archiveChallengesScreen()` renders every case's challenge for the selected unit, and `goToCase()` never sets `selectedUnitId` — so traveling to case-005 lands on a page listing case-002…case-006.
3. `caseMarker()` puts `disabled` on locked markers, which kills `select-case` — a student can't even read what a locked mission is.

### Sub-phases

- **48A — DONE.** `CASE_ROUTES` replaced with `["field", "archive-challenges"]` (`unit.schema.js`) — named after the screen id it dispatches to, so `travelScreen()`'s `currentScreen = c?.route || "archive"` handoff is now a pure passthrough with no null special case. The 4 dead per-case route names (`ledger`/`empire`/`triangle`/`founding`) are retired along with `route`'s `.nullable()` — every case now requires a real route. At the 5 content sites (case-002/003/005/006/008): `route: null` → `route: "archive-challenges"`, `navigationTableVisible: false` deleted. `archiveScreen()`'s `navigationTableVisible !== false` filter deleted — every case in the unit gets a marker now (`caseMarker()`'s existing complete/available/locked state still governs how it looks). Fixed the real bug: `goToCase()` never set `progress.selectedUnitId`, so Chronotravel to a case in a different unit than whatever was last selected left `archiveChallengesScreen()` showing the wrong unit's challenges — now set via `unitForCase(caseId)?.id`, matching the pattern `previewCase()` already used. `archiveChallengesScreen()` now reorders its case cards so the just-traveled-to case leads the list (stable sort, no-op for any other entry point like the Archive Terminal). `caseKindDetail()`'s `kase.route === null` check updated to `=== "archive-challenges"`. Updated the 4 e2e "no longer appears as a Navigation Table marker" assertions (`archive-challenge.spec.js`) to the opposite: each case now asserts `toHaveCount(1)` plus a `.route-marker--locked` class check (all 4 are locked by default — only case-001 is unlocked in a fresh save). Updated `content-schemas.test.js`'s null-route test to `"archive-challenges"` and added a new test confirming `route: null` is now rejected. Deliberately did NOT add DOM `.focus()` to the reordered leading card — being first in visual/DOM order was judged sufficient without introducing a focus-management side effect this app doesn't otherwise use. **Verified: `npx vitest run tests/unit/content-schemas.test.js` 20/20; `npm run validate:content` clean; `npm run test` 455/455; `npx playwright test tests/e2e/archive-challenge.spec.js` 9/9; lint/build clean. Re-recorded the `archive-navigation-table` visual baseline (the diff showed exactly the two new expected markers, nothing else moved) and reconfirmed zero-diff on a second run. Manually confirmed live (Playwright MCP, 1366×768) that: the Philadelphia cluster (case-007/case-008, identical map coordinates) is cleanly spread apart by `declutterMarkerPositions()` with distinct readable labels — the exact untested collision case the plan flagged; clicking a newly-unhidden marker (Hispaniola/case-003) populates the route panel correctly; and Chronotravel from it lands directly on Archive Challenges with the Hispaniola card first, not the Navigation Table.** One pre-existing flaky e2e test (`field-movement-dialogue.spec.js`, a 1px camera-position timing race unrelated to any file touched here) failed once under full-suite load and passed cleanly in isolation — not a regression from this phase.
- **48B — DONE.** `caseMarker()` no longer sets `disabled` on locked markers — `select-case` has no unlock gate, so a locked marker is now genuinely clickable and reads its details/unlock reason in the route panel instead of doing nothing; `aria-disabled="true"` plus an "— locked" suffix on the aria-label communicate the state to assistive tech without removing it from the interaction surface. (Found live: Playwright's own `.click()` actionability check treats `aria-disabled="true"` as non-actionable and refuses to click it — a real difference from `disabled` worth knowing if a future e2e test ever needs to click a locked marker; `{force: true}` or a raw DOM `.click()` dispatch gets around it. This doesn't affect real users — `aria-disabled` never blocks native browser click events, only Playwright's synthetic actionability gate.) `.route-marker--locked`'s `cursor: not-allowed` → falls back to `.route-marker`'s own `pointer`, since it's clickable now. Added `chronotravelLabel` in `archiveScreen()`: "Initiate Chronotravel →" for `route === "field"`, "Open Archive Challenge →" otherwise — both still call the same `data-action="travel"` → `goToCase()` → travel-vortex screen, so both mission types get the same warp beat. Added `lockedReasonForCase()`, mirroring `unlockNext()`'s own array-index logic rather than inventing a second "what unlocks what" model: index 0 of a unit → "Your teacher hasn't opened this unit yet."; any later index → "Complete {prior mission's shortTitle} to unlock this mission." — rendered as a new `.route-locked-reason` line under the (still-disabled) Chronotravel/Archive-Challenge button whenever the selected case is locked. Added the missing `.legend-locked` CSS rule (previously fell back to `.archive-legend`'s base color with no dedicated locked treatment). **Verified: `npm test` 455/455; lint/build clean; `npx playwright test tests/e2e/archive-challenge.spec.js` 9/9 (the `.route-marker--locked` class assertions from 48A still pass unchanged). Re-recorded `archive-navigation-table` (diff was exactly the "○ Teacher locked" legend text's new color, nothing else) and reconfirmed zero-diff after. Manually confirmed live (Playwright MCP, 1366×768): clicking a locked marker (Atlantic Routes/case-002) now populates the route panel with "Open Archive Challenge →" (disabled) and "Complete Caribbean to unlock this mission."; clicking an available field-route case (case-001) shows "Initiate Chronotravel →" with no reason line and no disabled state.**
- **48C** — Teacher per-mission visibility toggle. Delete `navigationTableVisible` rather than repurposing it (global default vs. per-classroom opt-out are different layers). Use the existing `teacher-override-repository.js` generic `(contentId, fieldName, value)` store — no new table/schema.
- **48D** — Unit 3's third mission. Promote the existing unit-level bonus challenge (`unit-03-archive-appeal-form-comparison`, already authored and validated) to `case-009` with real case-level framing, rather than authoring new content from scratch. Fallback: ship Unit 3 with two markers and author case-009 separately if the quest is judged unsuitable standalone.

---

# Phase 49 — Chronicle: highest-ROI moves as an APUSH game (not started)

**Research grounding:** the 2026 APUSH exam is fully digital in Bluebook with identical content/timing/rubrics to the paper exam — MCQ 40% / SAQ 20% / DBQ 25% / LEQ 15%; DBQ 7 points, LEQ 6, SAQ 3×1 ([College Board](https://apcentral.collegeboard.org/courses/ap-united-states-history/exam), [exam guide](https://apushscorecalculator.us/apush-exam-guide)). Spaced repetition carries a meta-analytic effect of d ≈ 0.78 across 21,415 learners, distributed-over-massed practice d ≈ 0.60 ([systematic review](https://asmepublications.onlinelibrary.wiley.com/doi/10.1111/tct.70353), [summary](https://www.justinmath.com/cognitive-science-of-learning-spaced-repetition/)).

Ranked by exam weight ÷ build cost:

- **49A** — A real SAQ quest type. 20% of the exam, 30 raw points, no quest type exists today. The rubric and evaluator wiring already exist.
- **49B** — Skill mastery, surfaced to the student. `SKILL_CATEGORIES` is real but tagged on evidence-organizing quests only — extend across all types, record per-attempt outcomes, add a mastery record screen.
- **49C** — "The Archive Rotation": a spaced-repetition daily loop reusing the existing MCQ/sequencing/HIPP pool as its item bank. Must respect the standing rule that progression never confers a graded-assessment advantage.
- **49D** — CED alignment tagging (Period/Key Concept/Theme/Historical Thinking Skill) surfaced on the teacher dashboard.
- **49E** — The DBQ capstone ("Chronicle Dossier"). 25% of the exam, biggest build, strongest pedagogy fit — scope after 49A proves the written-response pipeline end to end.

**Ongoing track:** Chronicle covers Periods 1–3 of 9; content velocity, not architecture, is the constraint.

---

# Phase 50 — Odysso: highest-ROI moves as a learning platform (not started)

**Research grounding:** SSO (Google/Microsoft), rostering (Clever/ClassLink), and LMS push (Google Classroom/Canvas/Schoology) are described as non-negotiable at K-12 scale ([EdTech Insiders](https://edtechinsiders.substack.com/p/the-new-era-of-k12-rostering-what), [Clever](https://www.clever.com/blog/2022/07/google-classroom-rostering)).

- **50A** — Turn on Google sign-in for teachers. Code already exists (`remote-auth-repository.js`) but no Google provider is configured in any Supabase project — close to free.
- **50B** — Kill the manual roster via Google Classroom import.
- **50C** — One identity across Odysso and Chronicle: point Odysso's placeholder sign-in at the same Supabase project.
- **50D** — Assignments with due dates + class outcome reporting.
- **50E** — Second-subject proof: a written go/no-go decision, not a `packs/` build — no forcing function exists yet per `POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10.

**Verification:** the claim/login/provision/reissue/evaluate/grade round trip and RLS cross-tenant isolation — outstanding since Phase 22, still the largest untested surface in the product.

---

## Program-level verification

Before every commit: `npm run test` (454 baseline), `npm run lint` and `npm run build` unchanged from baseline, `npm run validate:content` when content changed, `npm run test:e2e` including the 20 visual baselines at 1366×768, and a live pass on the specific interaction changed. Push to `main` after verification (standing permission; Vercel auto-deploys).

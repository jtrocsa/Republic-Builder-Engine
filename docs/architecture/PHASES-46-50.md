# Phases 46–50 — Chronicle / Odysso Improvement Program

**Status:** Phases 46, 47, and 48 done; Phase 49A–49E all done. Phase 50 (Odysso platform work) next. This is the one living roadmap for this program — per Phase 46E, don't create a new stand-alone audit/roadmap document per pass; extend this file instead, and update `ARCHITECTURE-QUICKREF.md`'s one-line phase index when a phase closes.

## Context

Phases 23–44 were teacher-facing; Phase 45 was a gameplay CSS/test-infrastructure pass. The owner asked for five things in one program, with no end date — "improve this every hour of every day for the foreseeable future."

Four problems drove this plan, each verified against the repo, not inferred:

1. **Session cost was structurally wrong.** A usage report showed 91% of usage at >150k context, 70% from subagent-heavy sessions, 65% from the Playwright MCP server. The causes were measurable: `ARCHITECTURE-QUICKREF.md` was 232 lines but 172,761 bytes (~43k tokens) — §4/§5/§6 alone were 160 KB of phase changelog — and `CLAUDE.md` ordered every session and every subagent to read it first. A committed 11-spec Playwright suite with 20 visual baselines already existed (`npm run test:e2e`), but `CLAUDE.md` never mentioned it and still called Playwright "deferred," so browser verification was done by hand through the MCP server. A prior audit (`docs/architecture/AI-USAGE-AND-DEVELOPMENT-COST-AUDIT.md`, 2026-07-23) produced ten prose recommendations and zero config changes; 8 of 10 were never adopted and the QUICKREF had since grown. The repo had no `.claude/settings.json`, no permissions allowlist at all.

2. **The teacher UI was not professional.** The dashboard rendered an `h1` at up to 83px directly above 12px buttons (7×). Teacher surfaces used ~26 distinct font sizes against a 6-step token scale, and bare `<h2>`/`<h3>`/`<h4>` tags with no CSS rule at all fell back to UA-default DM Sans bold. Phase 44 built a token layer and a `c-*` primitive set, then never adopted it: `fieldMarkup()`, `pageHeaderMarkup()`, `sectionHeadMarkup()` had zero call sites; `btn()` had one. Mission-card buttons didn't align, and a 41-character badge wrapped to 2–3 lines while its sibling stayed a pill.

3. **The Navigation Table hid two thirds of the game.** Only 3 of 8 cases rendered markers, because `navigationTableVisible: false` was hard-coded on case-002/003/005/006/008. Every hidden case already had a real `mapPosition`, and `caseMarker()` already implemented a greyed-out `locked` state.

4. **Chronicle covers ~1/3 of APUSH; Odysso is a brochure.** Three units (Periods 1–3), 8 cases, 4 quest types. No SAQ quest type exists (20% of the exam, 30 raw points); no DBQ/LEQ student surface exists (40% combined) even though `api/_lib/rubrics.js` already implements their rubrics. Odysso is a static React marketing site with a placeholder sign-in and no backend.

**Standing constraints that do not relax.** No Phaser, inkjs, `WorldComposition`, `QuestEngine` registries, `WorldRuntime`, or `packs/<subject>/` without a concrete forcing function (`POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10). No Founder Paths / professions / Historian Skills / wardrobes. No extracting movement/collision/camera from `main.js` for neatness. Every change leaves `npm run dev` player-visibly identical unless new behavior is the explicit goal.

**Sequencing rationale:** 46 first, because every later phase is cheaper once the context floor drops. 47 and 48 next — both are the owner's directly observed complaints, and both are largely already-built systems that need finishing rather than inventing. 49 and 50 open with a research sub-phase because the ask was to "research and consider"; the ranked lists in each are the starting position, not the conclusion.

---

# Phase 46 — Session Cost Discipline (enforced, not advisory)

**Goal:** cut the per-session context floor from ~50k to ~10k and stop paying for browser verification twice. The difference from the 2026-07-23 audit: every item is a config file or a moved/deleted line, not a paragraph of advice.

- **46A — Split the QUICKREF. DONE.** §4/§5/§6 (the phase-by-phase changelog) moved verbatim to `docs/architecture/PHASE-HISTORY.md`, an archive nothing is instructed to read. `ARCHITECTURE-QUICKREF.md` rebuilt as ≤150 lines of pure boundaries plus a one-line-per-phase index. **Measured: 172,761 bytes → 18,599 bytes (89% reduction, ~43k → ~4.6k tokens).**
- **46B — Rewrite `CLAUDE.md`'s verification and reading rules. DONE.** Replaced the "reproduce in the browser" instruction with an explicit verification ladder (targeted vitest → validate:content → `npm run test:e2e` → visual-regression → manual/MCP browser only for uncovered interactions, then banked as a new spec). Fixed the stale "Playwright is deferred" line. Added a "minimum required reading by task type" table. Removed the dangling `/verify` skill reference (no such skill exists). Added an explicit "don't spawn a subagent for a single-file edit" rule.
- **46C — Create the repo's Claude Code configuration. DONE.** Added a tracked `.claude/settings.json`: a permissions allowlist for routine read-only commands (`npm run test`/`test:e2e`/`lint`/`build`/`validate:content`/`format:check`, `git status`/`diff`/`log`, `wc`, `rg`/`grep`), and `deniedMcpServers: [{serverName: "playwright"}]` — the correct mechanism for gating a server regardless of where it's configured (verified against the real settings schema; `disabledMcpjsonServers` was the wrong key, since it only applies to servers declared in a project `.mcp.json`, which this repo doesn't have — Playwright is configured at user/global scope). Pixellab and Vercel MCP stay on, untouched. **Note: the MCP gate takes effect on the next session/`/config` reload, not retroactively within the session that added it.**
- **46D — Fix the subagent multiplier. DONE.** Fixed `.claude/agents/code-reviewer.md`, which still flagged Playwright (and implicitly Tiled) as deferred and would therefore have flagged legitimate e2e/map work as scope creep. The "when not to spawn a subagent" rule landed as part of 46B's CLAUDE.md rewrite rather than as a separate edit, since it's read from the same file every subagent inherits.
- **46E — Stop the audit-doc proliferation. DONE.** Moved 5 closed documents (`FOCUSED-MODERNIZATION-ROADMAP.md` — fully shipped, plus `PLAYWRIGHT-ADOPTION-PLAN.md`, `TEACHER-UI-ACCESSIBILITY-AUDIT.md`, `WORKFLOW-STATE-AUDIT.md`, `AUTHORING-SYSTEMS-AUDIT.md`) to `docs/architecture/archive/` with a one-line index (`archive/README.md`). This file (`PHASES-46-50.md`) is the one living roadmap going forward.

**Verification:** measured, not just asserted — QUICKREF byte count before/after recorded above. No application code was touched in this phase (docs + `.claude/` config only), so `npm run test`/`lint`/`build`/`validate:content` are unaffected by construction; confirmed via `git status` showing only doc/config files changed.

---

# Phase 47 — Professional Teacher Interface

**Goal:** one type scale, one card, one page header, one container width — a professional web app inside the existing navy/gold/parchment palette.

**Isolation strategy (load-bearing).** Teacher screens get a scope class `.c-app`, and — wherever cheap — stop emitting the shared gameplay class entirely rather than overriding it. `.completion-shell` and `.review-shell` are removed from teacher markup and replaced with `.c-page`. That deletes the conflict at the source instead of layering specificity on it.

| Selector | Verdict |
|---|---|
| `.completion-shell`, `.review-shell` | Don't touch. Stop emitting from teacher screens. Both are visual-regression baselines. |
| `.kicker`, `.btn*`, `.text-button`, `.back-link`, `.shell`, `.feedback`, `.unit-tab`, `.choice` | Fork via `.c-app` prefix only — additive overrides, never edits. |
| `.c-*` (global.css 182–511) | Free to extend — zero gameplay consumers. |
| `.manage-content-*`, `.source-pool-*`, `.case-kind-badge`, `.roster-table`, `.auth-shell`, `.manual-grade-entry` | Free to edit in place — teacher-only, zero baselines. |

There are zero teacher-screen visual baselines, so all 20 gameplay PNGs must stay byte-identical through Phase 47. That is the primary regression gate.

**Type scale — 7 tokens, one addition.** Keep the existing 6; add `--c-text-micro: 0.68rem`. Card/`h3` titles are `--c-text-body` at `700 var(--c-font-label)`, so no `--c-text-lead` is needed.

| Current cluster | → Token |
|---|---|
| 0.58 / 0.62 / 0.65 / 0.67 | `--c-text-micro` (0.68rem) |
| 0.68 / 0.70 / 0.72 / 0.75 / 0.76 | `--c-text-label` (0.72rem) |
| 0.78 / 0.80 / 0.82 / 0.85 / 0.87 / 0.88 / 0.90 | `--c-text-meta` (0.85rem) |
| 0.95 / 1.0 / 1.05 | `--c-text-body` (1rem) |
| 1.1 / 1.15 / 1.2 | `--c-text-section` (1.2rem) |
| `clamp(3rem,5vw,5.2rem)` | deleted from the teacher path — comes from `.completion-shell h1`, fixed by not emitting that class |
| bare `<h2>/<h3>/<h4>` UA defaults | fixed by a scoped element reset (`.c-app h1..h4, .c-app p, .c-app li`) — safe only because it's scoped |

### Sub-phases

- **47A — DONE.** Added `.c-app` scope block + `--c-text-micro` token (global.css); gave bare `<h1>-<h4>/p/li/dd` inside `.c-app` real rules on the token scale; retuned ~26 stray font-size values in confirmed teacher-only selector blocks (`.manage-content-*`, `.source-pool-*`, `.roster-table`, `.case-kind-badge`, `.locked-note`, `.password-field/-toggle`, `.archive-feedback-*`) — verified selector-by-selector first, since this CSS region also interleaves genuine gameplay selectors (`.completion-shell`, `.field-npc`, `.hub-player`) that must not be touched. Added `c-app` to all 13 teacher `<main>` occurrences (join/login/dashboard/grading/manage-content-case), additively alongside the existing shared classes rather than replacing them. **Verified: `npx playwright test visual-regression.spec.js` 8/8 passed, zero pixel diff across all 20 gameplay baselines; `npm test` 454/454; lint/build/validate:content clean; manually confirmed live that the dashboard's "Missions"/"Unit access" headings now render on the Spectral type scale instead of unstyled UA defaults.**
- **47B — DONE.** `teacherDashboardScreen()`, `teacherUnitsTabMarkup()`, `teacherUnitAccessMarkup()` stopped emitting `.completion-shell` (820px boxed-card, meant for gameplay completion/auth screens) in favor of `.c-page` (1180px). Hand-rolled kicker/h1/p header → real `pageHeaderMarkup()` call; bare `<h2>Missions</h2>`/`<h2>Unit access</h2>` → `sectionHeadMarkup()`. Also fixed a real `pageHeaderMarkup()` bug found while wiring it up: `eyebrow` was rendered unguarded (unlike `description`), so an omitted eyebrow rendered an empty `<p>`. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher sign-in) that the dashboard header and both Units-tab section heads render on the real type scale at 1180px.**
- **47C — DONE.** Fixed the exact reported bug: "Map Mission"/"Activity Mission — Archive Challenge only" badges didn't line up because the combined string wrapped to 2-3 lines for the long case while the short one stayed one line. `caseKindLabel()` now returns short labels only; new `caseKindDetail()` renders the qualifier as a `.c-help` line under the `<h3>`. `.case-kind-badge` (a bespoke pill duplicating `chip()`) is deleted — both call sites (mission-card kind badge, Sources tab's "Locked" source badge) now use `chip()`. `.manage-content-mission-card .btn`'s `margin-top: 8px` → `auto`, pinning "Edit mission →" to the bottom of every card in a grid row regardless of that card's own copy length. `.manage-content-mission-head` → `flex-wrap: nowrap` now that the badge is always short. Found live and fixed: `chip()` as a column-flex child was being stretched full-width by `.source-pool-row`'s default flex stretch — added `.source-pool-row > .c-chip { align-self: flex-start }`. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live that all three Unit 1 mission cards show single-line badges with aligned buttons regardless of card height, and Sources-tab "Locked" chips hug their own text.**
- **47D — DONE.** `pageHeaderMarkup()` on Manage Content's four hand-rolled headers: the signed-out guard, the no-case guard, the locked Map Mission view (previously had no heading inside `<main>` at all — relied solely on the command bar's breadcrumb), and `manageContentWizardHeaderMarkup()` (shared by every Activity Mission wizard step). `.manage-content-shell` (already 1180px) is untouched — consolidating it with `.c-page` into one container implementation is 47G's job, not this one. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live that the locked Caribbean view and the Atlantic Routes wizard header both render correctly.**
- **47E — DONE.** Extended `btn()` (`labelHtml`/`href`, for the classroom-switch-code span and the "View source ↗" external link), `fieldMarkup()` (`placeholder`/`autocomplete`/`attrs`/`control`/`select`), and `passwordFieldMarkup()` (`class="c-input"`). Converted `loginScreen()`, `joinScreen()`, `teacherClassroomsTabMarkup()`, and `gradingScreen()`'s form fields to `pageHeaderMarkup()`/`fieldMarkup()`. All existing input `id`s preserved exactly (`teacher-email`, `new-classroom-name`, `grade-label`, etc. — confirmed via grep against every `getElementById` call site); previously id-less classroom-setup-row inputs got new synthesized ids matched via their existing `data-classroom-row-*` attributes. Found and fixed a real 47B regression: removing `.completion-shell` from the dashboard had silently broken `new-classroom-name`/`provision-count` styling, since that ancestor rule was their only source of visual styling — fixed as a side effect of the `fieldMarkup()` conversion. Also fixed `gradingScreen()`'s `grade-label`/`grade-teacher-feedback`, which had never been styled at all. Caught and reverted two of my own mistaken cascade assumptions via the visual-regression suite before they shipped: (1) initially deleted `.completion-shell label/input/textarea` believing them dead, which broke the `identity-screen` gameplay baseline — restored exactly, with a comment naming `identityScreen()` as the real dependent; (2) initially recolored `.password-toggle` assuming `.c-input`'s dark background now applied to auth screens, which was wrong because `.completion-shell`'s higher-specificity `input`/`textarea` rule is still an active ancestor on login/join (only the inner field markup was converted, not the shell) — reverted to the original color. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (Playwright MCP) that Teacher Sign In, Join a Classroom, and the Dashboard Classrooms tab all render correctly.**
- **47F — DONE.** Unified the Sources tab's `teacherSourcesUnitSectionMarkup()` onto the Units tab's existing native `<details>/<summary>` pattern (`manageContentUnitSectionMarkup()`), removing the last hand-rolled `<section>`/`<button>`+chevron accordion — both tabs now share one markup shape and one `handleAppClick()` preventDefault path. The three bare `<h4>` inside it ("Locked (official)", "Unit N Mission Pool Sources", "Available pool") converted to `sectionHeadMarkup()`. Deleted the now-dead `.manage-content-unit-chevron` CSS rule and rewrote the stale comment that used to explain why two accordion patterns coexisted. Deliberately deferred `sourceRowMarkup()`'s surface (border/radius/background) to 47G rather than doing it here: 47G already folds four near-identical card surfaces (`.manage-content-mission-card`, `.source-pool-row`, `.manage-content-editor-section`, `.manage-content-source-summary-card`) onto `.c-card` in one pass, and touching `.source-pool-row` twice across two phases would be wasted motion. **Verified: visual-regression 8/8 zero diff (no baseline covers this screen, but the 20 gameplay baselines are unaffected by construction); `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher, 1366×768) that both the Sources and Units tab accordions open/close correctly and render identically styled headings.**
- **47G — DONE.** Folded all four near-identical card surfaces onto `.c-card`: `.manage-content-mission-card` (+ `.c-card--interactive`, replacing its own duplicate hover-lift rule with the shared one), `.source-pool-row`, `.manage-content-editor-section` (its `.is-collapsed` background override still wins on specificity over `.c-card`'s, so the dimmed-collapsed state is unaffected), and `.manage-content-source-summary-card`. Each surface-specific class now carries only its own layout (padding/gap/flex-direction/margin) plus any genuine state variant (focus outline, collapsed background) — border/radius/background/shadow all come from one shared rule instead of four hand-copied near-duplicates. Also folded `.manage-content-status-pill` onto `chip()`: its recipe (padding, `rgba(gold,0.4)` border, 999px radius, `#d8e3de` text) was a byte-for-byte duplicate of `.c-chip`'s default tone, so the three status-bar pills (Published Mission / Student View / Source) now render via `chip()` calls instead of a fourth bespoke pill class. Considered and explicitly deferred: consolidating `.manage-content-shell`'s outer container with `.c-page` — both already render at the same 1180px width so there's no visible bug to fix, and `.manage-content-shell`'s 235px top padding exists specifically to reserve room for the wizard's fixed header, which `.c-page` has no equivalent for; forcing them into one implementation would be pure risk for zero visible payoff. **Verified: visual-regression 8/8 zero diff; `npm test` 454/454; lint/build clean; manually confirmed live (dev-fake-teacher, 1366×768) that mission cards (with hover lift), Sources-tab rows, the Evidence Organizing editor's Question/Answer-structure sections, the source-summary card, and the bottom status-pill row (now lowercase-styled chips instead of uppercase pills) all render correctly with no layout shift.**

**Phase 47 status: DONE (47A–47G all complete).** The teacher-facing UI now has one type scale, one card surface, one page header, one container width, and one accordion pattern throughout.

**Verification per sub-phase:** `npm run test:e2e -- visual-regression` zero-diff on all 20 gameplay baselines, `npm run test` 454/454, `lint`/`build` unchanged, plus a live pass at 1366×768 (the project's named Chromebook target). Add a teacher-screen smoke e2e spec so the next pass isn't hand-verified either.

*`.legend-locked` (emitted with no CSS rule) is fixed in Phase 48, not 47 — it's a gameplay selector inside `archiveScreen()` whose baseline 48 re-records anyway.*

---

# Phase 48 — The Full Mission Board

**Goal:** the Navigation Table shows all three missions per period, locked ones greyed out and explained, available as standard and changed only when a teacher edits that mission.

The rendering already supports this: `caseMarker()` computes `complete`/`available`/`locked`, `.route-marker--locked` exists, `declutterMarkerPositions()` already fans out overlapping markers, and every hidden case has a real `mapPosition`. **But unhiding alone would ship three bugs**, which is the real content of this phase:

1. `travelScreen()`'s timeout is `currentScreen = c?.route || "archive"` — an Archive-Challenge mission warps and dumps the student back on the Navigation Table.
2. `archiveChallengesScreen()` renders every case's challenge for the selected unit, and `goToCase()` never sets `selectedUnitId` — so traveling to case-005 lands on a page listing case-002…case-006.
3. `caseMarker()` puts `disabled` on locked markers, which kills `select-case` — a student can't even read what a locked mission is.

### Sub-phases

- **48A — DONE.** `CASE_ROUTES` replaced with `["field", "archive-challenges"]` (`unit.schema.js`) — named after the screen id it dispatches to, so `travelScreen()`'s `currentScreen = c?.route || "archive"` handoff is now a pure passthrough with no null special case. The 4 dead per-case route names (`ledger`/`empire`/`triangle`/`founding`) are retired along with `route`'s `.nullable()` — every case now requires a real route. At the 5 content sites (case-002/003/005/006/008): `route: null` → `route: "archive-challenges"`, `navigationTableVisible: false` deleted. `archiveScreen()`'s `navigationTableVisible !== false` filter deleted — every case in the unit gets a marker now (`caseMarker()`'s existing complete/available/locked state still governs how it looks). Fixed the real bug: `goToCase()` never set `progress.selectedUnitId`, so Chronotravel to a case in a different unit than whatever was last selected left `archiveChallengesScreen()` showing the wrong unit's challenges — now set via `unitForCase(caseId)?.id`, matching the pattern `previewCase()` already used. `archiveChallengesScreen()` now reorders its case cards so the just-traveled-to case leads the list (stable sort, no-op for any other entry point like the Archive Terminal). `caseKindDetail()`'s `kase.route === null` check updated to `=== "archive-challenges"`. Updated the 4 e2e "no longer appears as a Navigation Table marker" assertions (`archive-challenge.spec.js`) to the opposite: each case now asserts `toHaveCount(1)` plus a `.route-marker--locked` class check (all 4 are locked by default — only case-001 is unlocked in a fresh save). Updated `content-schemas.test.js`'s null-route test to `"archive-challenges"` and added a new test confirming `route: null` is now rejected. Deliberately did NOT add DOM `.focus()` to the reordered leading card — being first in visual/DOM order was judged sufficient without introducing a focus-management side effect this app doesn't otherwise use. **Verified: `npx vitest run tests/unit/content-schemas.test.js` 20/20; `npm run validate:content` clean; `npm run test` 455/455; `npx playwright test tests/e2e/archive-challenge.spec.js` 9/9; lint/build clean. Re-recorded the `archive-navigation-table` visual baseline (the diff showed exactly the two new expected markers, nothing else moved) and reconfirmed zero-diff on a second run. Manually confirmed live (Playwright MCP, 1366×768) that: the Philadelphia cluster (case-007/case-008, identical map coordinates) is cleanly spread apart by `declutterMarkerPositions()` with distinct readable labels — the exact untested collision case the plan flagged; clicking a newly-unhidden marker (Hispaniola/case-003) populates the route panel correctly; and Chronotravel from it lands directly on Archive Challenges with the Hispaniola card first, not the Navigation Table.** One pre-existing flaky e2e test (`field-movement-dialogue.spec.js`, a 1px camera-position timing race unrelated to any file touched here) failed once under full-suite load and passed cleanly in isolation — not a regression from this phase.
- **48B — DONE.** `caseMarker()` no longer sets `disabled` on locked markers — `select-case` has no unlock gate, so a locked marker is now genuinely clickable and reads its details/unlock reason in the route panel instead of doing nothing; `aria-disabled="true"` plus an "— locked" suffix on the aria-label communicate the state to assistive tech without removing it from the interaction surface. (Found live: Playwright's own `.click()` actionability check treats `aria-disabled="true"` as non-actionable and refuses to click it — a real difference from `disabled` worth knowing if a future e2e test ever needs to click a locked marker; `{force: true}` or a raw DOM `.click()` dispatch gets around it. This doesn't affect real users — `aria-disabled` never blocks native browser click events, only Playwright's synthetic actionability gate.) `.route-marker--locked`'s `cursor: not-allowed` → falls back to `.route-marker`'s own `pointer`, since it's clickable now. Added `chronotravelLabel` in `archiveScreen()`: "Initiate Chronotravel →" for `route === "field"`, "Open Archive Challenge →" otherwise — both still call the same `data-action="travel"` → `goToCase()` → travel-vortex screen, so both mission types get the same warp beat. Added `lockedReasonForCase()`, mirroring `unlockNext()`'s own array-index logic rather than inventing a second "what unlocks what" model: index 0 of a unit → "Your teacher hasn't opened this unit yet."; any later index → "Complete {prior mission's shortTitle} to unlock this mission." — rendered as a new `.route-locked-reason` line under the (still-disabled) Chronotravel/Archive-Challenge button whenever the selected case is locked. Added the missing `.legend-locked` CSS rule (previously fell back to `.archive-legend`'s base color with no dedicated locked treatment). **Verified: `npm test` 455/455; lint/build clean; `npx playwright test tests/e2e/archive-challenge.spec.js` 9/9 (the `.route-marker--locked` class assertions from 48A still pass unchanged). Re-recorded `archive-navigation-table` (diff was exactly the "○ Teacher locked" legend text's new color, nothing else) and reconfirmed zero-diff after. Manually confirmed live (Playwright MCP, 1366×768): clicking a locked marker (Atlantic Routes/case-002) now populates the route panel with "Open Archive Challenge →" (disabled) and "Complete Caribbean to unlock this mission."; clicking an available field-route case (case-001) shows "Initiate Chronotravel →" with no reason line and no disabled state.**
- **48C — DONE.** Deleted `navigationTableVisible` from `CaseSchema` entirely (`unit.schema.js`) rather than repurposing it — a content-authored boolean is a global default, while the requirement is a per-classroom opt-out, and keeping both means a global default permanently fighting an override. New `resolvedNavTableVisible(kase)` in `main.js` resolves `teacher-override-repository.js`'s generic `(contentId, fieldName, value)` store: `resolveTeacherOverride(kase.id, "navTableVisible", "true") !== "false"`, default-visible with zero migration for the local/signed-out path. `archiveScreen()`'s `visibleCases` now filters through it. UI: a new checkbox in `missionRenameControlMarkup(kase)` (`.manage-content-visibility-toggle`), beside the existing rename control, wired via a new `[data-case-visibility]` branch in `handleAppChange()`. Found the plan's "no new table and no new schema" claim was only half right: `local-teacher-override-store.js`'s `SUPPORTED_FIELD_NAMES` allow-list *and* a real Postgres CHECK constraint (`content_overrides_field_name_check` in `0001_init.sql`) both gate which field names `content_overrides` accepts — updated the allow-list and wrote `supabase/migrations/0011_case_visibility_override.sql` widening the constraint to include `'navTableVisible'`, following the exact same "written, not yet applied — apply by hand via the Supabase SQL editor" pattern migrations 0006–0010 already shipped in this repo (no CLI/automation exists here, and applying it is outside this session's access). **Until that migration is applied by hand, the per-classroom remote path 400s on write (confirmed live: `content_overrides_field_name_check` violation) — the local/signed-out override path is unaffected and already fully correct.** Found and fixed a real, pre-existing bug while live-testing: `handleAppChange()` called `render()` synchronously right after firing `setTeacherOverride()` (async — it awaits `getSession()` before touching the override cache), so every override write raced its own re-render and the just-changed field visually reverted to its old value until some unrelated later render happened to catch up — reproduced live for the *existing* `[data-case-title]` branch too (typing a new mission name and tabbing away silently reverted the input), not just the new checkbox. Fixed by making `handleAppChange` `async` and `await`-ing `setTeacherOverride()` in all three affected branches (`[data-copy]`, `[data-case-title]`, `[data-case-visibility]`) before calling `render()`. **Verified: `npm run lint`/`build` clean; `npm test` 455/455; `npm run validate:content` clean (0 errors across all schema/cross-reference groups); `npx playwright test tests/e2e/archive-challenge.spec.js tests/e2e/visual-regression.spec.js` 17/17, zero pixel diff on all 20 gameplay baselines (Manage Content has no baselines to move). Manually confirmed live (Playwright MCP, dev-fake-teacher): the checkbox renders checked by default on Case 1.02; unchecking it updates the in-memory override immediately and correctly (previously it silently snapped back to checked — the exact bug just described — until the awaited fix); the console shows the expected `content_overrides_field_name_check` 400 (unapplied migration) rather than a client-side error; a parallel mission-rename edit round-tripped correctly through Supabase and persisted across a full page reload, confirming the async/await fix didn't break the already-working remote path for allow-listed fields.** **Action needed from the user: apply `supabase/migrations/0011_case_visibility_override.sql` by hand via the Supabase SQL editor before the visibility toggle will persist for signed-in classrooms** (it already works today for local/offline dev).
- **48D — DONE.** Promoted Unit 3's only unit-level bonus Archive Challenge (`unit-03-archive-appeal-form-comparison` — Prince Hall's 1777 public petition for freedom vs. Abigail Adams's 1776 private letter to John Adams, both invoking the Revolution's rights language, in `content/quests/unit-03-quests.js`) to a real `case-009` ("Appeals to Liberty") in `unit-03-campaign.js`'s `cases[]`, with genuine case-level framing authored from scratch: `shortTitle`/`title` "Appeals to Liberty", `date: "1776–1777"`, `mapPosition` anchored on Boston (where Hall's petition was addressed and near where Adams wrote) rather than reusing case-007/008's Philadelphia coordinates, `mechanic: "Appeal Ledger"`, `route: "archive-challenges"`, and a new `question`/`summary` pair. `UNIT_03.archiveChallenges[]` (the unit-level bonus array) is now empty — no new quest content was authored; the same real, cited quest simply moved from one schema field to the other, exactly as the plan anticipated. Confirmed `unitReadyForReview()`/`unitArchiveChallengesComplete()` need no code change: an empty `archiveChallenges[]` trivially satisfies `.every()`, so unit completion now flows entirely through `unit.cases.every(isComplete)`, with case-009 gating in the normal per-case way every other Archive-Challenge-only case already does. Banked the manual live-check as two new e2e tests in `archive-challenge.spec.js` (case-009 locked-marker assertion; full placement/reflection/completion of the real 2-source quest) rather than leaving it a one-off browser pass. **Verified: `npm run validate:content` clean (0 errors, cross-reference: archive challenge quest references still resolves the moved questId correctly); `npm test` 455/455; lint/build clean; `npx playwright test tests/e2e/archive-challenge.spec.js` 11/11 (9 prior + 2 new case-009 tests); `npx playwright test tests/e2e/visual-regression.spec.js` 8/8 zero-diff (Unit 3's Navigation Table isn't among the 20 committed baselines, so no re-recording was needed). Manually confirmed live (Playwright MCP): "Appeals to Liberty" renders as Unit 3's third Navigation Table marker with its own clean position (no Philadelphia-cluster collision, by design); a locked marker's route panel correctly reads "Complete Founding Debate to unlock this mission."; once case-008 is completed, the marker becomes `✦ Available` with an enabled "Open Archive Challenge →" button; and Chronotravel from it lands directly on Archive Challenges with "Appeals to Liberty" (the real Prince Hall/Abigail Adams content) leading the list, matching the exact reordering behavior Phase 48A built for every other Archive Challenge case.**

**Phase 48 status: DONE (48A–48D all complete).** The Navigation Table now shows all three missions per period with locked ones genuinely readable and explained, a per-classroom teacher visibility opt-out exists (pending one manual Supabase migration application), and every unit in the game now has exactly three real, case-level missions.

---

# Phase 49 — Chronicle: highest-ROI moves as an APUSH game (49A–49E all done)

**Research grounding:** the 2026 APUSH exam is fully digital in Bluebook with identical content/timing/rubrics to the paper exam — MCQ 40% / SAQ 20% / DBQ 25% / LEQ 15%; DBQ 7 points, LEQ 6, SAQ 3×1 ([College Board](https://apcentral.collegeboard.org/courses/ap-united-states-history/exam), [exam guide](https://apushscorecalculator.us/apush-exam-guide)). Spaced repetition carries a meta-analytic effect of d ≈ 0.78 across 21,415 learners, distributed-over-massed practice d ≈ 0.60 ([systematic review](https://asmepublications.onlinelibrary.wiley.com/doi/10.1111/tct.70353), [summary](https://www.justinmath.com/cognitive-science-of-learning-spaced-repetition/)).

Ranked by exam weight ÷ build cost:

- **49A — DONE.** A real `saq` quest type in `QUEST_TYPES` (`apps/web/src/quest-types/history/saq-quest.js`), reusing `review.schema.js`'s existing `SaqSchema` (now exported) extended with an `id`. Design fork resolved via `AskUserQuestion`, since it was genuinely the owner's call: the other 4 quest types are fully synchronous (local-state-only, zero evaluator coupling — confirmed by re-reading `source-analysis-quest.js`'s "hipp" entry as precedent), but SAQ's real grading is inherently an async AI-evaluator round trip. Chosen resolution — **"complete" means "submitted," not "AI-graded"** — keeps `renderSaqQuest`/`gradeSaqQuest` synchronous like every other type (`complete` = every part has a non-empty response, mirroring `reviewScreen()`'s existing bespoke SAQ completion check verbatim); the AI Archive Evaluator round trip (formative feedback only, never an auto-grade — same as every other evaluator surface) composes separately at the call site in `archiveChallengeQuestCard()` (`main.js`), exactly the way `sourceReader()`/`reviewScreen()` already layer their own bespoke evaluator sections around their own content rather than baking evaluator coupling into a quest type itself. New `buildSaqQuestEvaluationRequest()` (`engine/evaluator-requests.js`) reuses the existing content-independent `taskType: "saq"` rubric (`api/_lib/rubrics.js`) with a per-quest-instance `taskId` (`saq-quest-${quest.id}`) instead of the unit-level SAQ's `saq-${unit.id}`. New `handleAppChange` branch for composite `data-saq-quest`/`data-saq-index` textareas, persisting `progress.questResponses[questId] = {responses: {index: value}}`. Real content authored to prove the pipeline end to end rather than a synthetic fixture: `unit-03-archive-common-cause-saq` (Dickinson's "Letters from a Farmer" Letter II, already a real case-007 source) as a new Unit 3 bonus Archive Challenge (`UNIT_03.archiveChallenges[]`, previously emptied by Phase 48D) — deliberately *not* swapped into any existing case's `archiveChallenge` slot, since every one of those already has a committed e2e spec asserting its specific content/quest id, and a unit-level bonus addition carries zero completion-gating regression risk for anything an existing test depends on. **Found and fixed a real, pre-existing latent bug while banking the live-test as an e2e spec:** `archiveChallengeQuestCard()` mutates `progress.archiveChallenges[questId]` to `"complete"` as a side effect of `render()`, but never called `save()` itself — case-level challenges happened to get re-saved anyway because their `onComplete` callback (`unlockNext()`) calls `save()` internally, but a *unit-level bonus* challenge (no `onComplete`) had nothing to trigger a follow-up save, so its completion silently existed only in memory and was lost on reload. This bug predates 49A (Unit 1/Unit 2's own bonus `archiveChallenges[]` were equally affected, just never exercised by a test) — fixed by adding an explicit `save()` call inside `archiveChallengeQuestCard()`'s completion-transition block, unconditionally, so it no longer depends on `onComplete` happening to save. Deliberately out of scope for this pass: wiring `saq` into Manage Content's from-scratch teacher-authoring picker (`QUEST_TYPE_DISPLAY_NAMES`/`QUEST_TYPE_ICONS`/`syncAuthoringFieldsFromDom`/`buildAuthoredContent` — a separate, sizable surface only used for per-case addition-slot authoring, which unit-level bonus challenges never route through anyway); SAQ is real, live, playable, evaluator-backed content today, just not yet an option a teacher can hand-author from scratch. **Verified: new `tests/unit/quest-type-saq.test.js` (13 tests) and extended `tests/unit/evaluator-requests.test.js` (+3 tests) — `npm test` 475/475 (up from 454 baseline); `npm run validate:content` clean, 82 groups (up from 74) including the new `UNIT_03_ARCHIVE_SAQ_QUESTS` schema/cross-reference checks; lint/build clean (same 5 pre-existing warnings only). Live-verified via Playwright MCP against `npm run dev`: seeded Unit 3's Archive Challenges screen, typed real responses into all three SAQ parts, confirmed each persisted correctly across re-renders, confirmed the card transitions to "Archive Challenge complete — case record preserved." only once every part is non-empty (and stays incomplete with a partial response), confirmed the "Get Archive Evaluator feedback →" button appears only once complete and correctly POSTs to `/api/evaluate` (404s in local Vite dev since no serverless function runs there — confirmed the friendly `evaluatorErrors` message renders instead of a crash, exactly the designed error path). The genuine AI-graded round trip against the live deployed backend remains unverified — outstanding since Phase 22, requires testing against the Vercel deployment post-push, not something local `npm run dev` can exercise. Banked both the completion and the incomplete-gating scenarios as two new `tests/e2e/archive-challenge.spec.js` cases (13/13 passing) so this coverage isn't hand-verified again next time.**
- **49B — DONE.** Skill mastery, surfaced to the student. `SKILL_CATEGORIES` (`evidence-organizing-quest.js`) was previously tagged on evidence-organizing sources only; extended across the other three graded quest types via a new optional 5th `QUEST_TYPES` contract slot, `skillOutcomes(quest, state, result)` (analogous to the existing `answeredAny`/`isComplete`/`partialSuccess`/`hint` slots — `quest-types/index.js`'s new `questSkillOutcomes()` wrapper returns `[]` for a type that doesn't implement it, so callers never branch on questType). Genericity boundary respected: `mcq-quest.js`/`sequencing-quest.js` (in `quest-types/generic/`, explicitly subject-agnostic — see their own header comments) gained a plain opaque `skillCategory: z.string().optional()` field rather than importing the history-specific `SKILL_CATEGORIES` enum directly (an earlier draft of this change did import it and was caught/reverted before landing — that would have coupled a generic module to APUSH-specific content). `source-analysis-quest.js` (already history-coupled) hardcodes every HIPP quest's outcome to `"Sourcing"` (asserted at module load against the real `SKILL_CATEGORIES` export, so a future rename fails loudly instead of silently mislabeling data) since HIPP sourcing reasoning *is* that skill by definition — no per-quest tag needed. `evidence-organizing-quest.js` reports one outcome **per placed source** (`<questId>::<sourceId>` key), matching its existing multi-skill-per-quest content shape, rather than one per quest like the other three types. SAQ deliberately excluded — its "complete" means "submitted," not graded (per 49A), so there's no binary correct/incorrect signal to record yet. New persisted `progress.skillMastery` (`chronicle-progress-store.js`'s `DEFAULT_PROGRESS` + `readProgress()` merge) is a flat map keyed by outcome key, each entry `{skillCategory, correct, questType, updatedAt}` — an **upsert**, not an append-only attempt log, so revisiting and fixing a wrong answer flips that item to correct rather than leaving a stale wrong entry alongside a new right one; this also sidesteps needing any "did this actually change" transition-detection beyond a cheap equality check before the (guarded, idempotent) `save()` call in new `recordSkillOutcomes()` (`main.js`), called from every one of the app's graded-quest render sites (`archiveChallengeQuestCard()`, `investigationScreen()`, and all 4 `practiceCheckScreen()` blocks). New student-facing **Skill Mastery Record** screen (`masteryScreen()`, `currentScreen: "mastery"`), reached via a new "Skill Mastery Record →" button inside the Preservation Case dialog (`unitOneBadgeCaseMarkup()`) — shows a bar per `SKILL_CATEGORIES` entry (always all 5, even at 0 attempted, so a student sees the full taxonomy) with a correct/attempted ratio, deliberately reusing `data-action="home"` to return (with an explicit `hubDialogueId = null` reset first, since the trophy dialogue's own JS state isn't part of `progress` and would otherwise silently reopen the Preservation Case modal on return — a stale-dialogue bug class CLAUDE.md's own gameplay invariants call out). Retroactively tagged real `skillCategory` values onto existing mcq/sequencing content across all three units (11 quests: case-001's 3 practice mcq + 2 investigation mcq + 1 causal sequencing + its own archive-challenge sequencing quest; case-004's 3 practice mcq + 1 causal sequencing; case-007's 3 practice mcq including a genuine **Comparison**-tagged item — closing a real, previously-flagged content gap noted in `docs/content-guide/skill-taxonomy.md` — + 1 causal sequencing + its 2 investigation mcq) so the mastery screen isn't empty against any unit's existing playable content. **Verified:** new `mcqSkillOutcomes`/`sequencingSkillOutcomes`/`sourceAnalysisSkillOutcomes`/`evidenceOrganizingSkillOutcomes`/`questSkillOutcomes` unit tests (17 new tests across `quest-type-mcq.test.js`, `quest-type-sequencing.test.js`, `quest-type-source-analysis.test.js`, `quest-type-evidence-organizing.test.js`, and a new `quest-types-index.test.js`) — `npm test` 492/492 (up from 475 baseline); `npm run validate:content` clean (schema-compatible additive field, no new content groups); lint/build clean (same 5 pre-existing warnings only); `npm run test:e2e` 34/34 including all 20 visual-regression baselines byte-identical (two unrelated single-run flakes during the ladder — a worker-contention timeout and a 1px camera-rounding diff — both confirmed pre-existing by re-running each spec in isolation, where they passed clean). New `tests/e2e/skill-mastery.spec.js` (3 tests, banked rather than only hand-verified): Practice Check answers across all 4 quest types correctly upsert `progress.skillMastery` (including a wrong answer still being recorded, and per-source evidence-organizing outcomes); the mastery screen aggregates seeded per-category stats correctly, including an untouched category correctly reading "Not yet practiced"; the empty-state message renders with no prior skill-mastery data. The Preservation Case → Skill Mastery Record button's live walk-to-trophy interaction itself remains outside committed e2e coverage — pre-existing gap already flagged in `visual-regression.spec.js`'s own header comment (the badge-case modal in general has never been walked-to in any committed spec), not newly introduced here.
- **49C — DONE.** "The Archive Rotation": a daily spaced-repetition loop over the existing mcq/sequencing/hipp pool. New pure, unit-tested `apps/web/src/engine/spaced-repetition.js`: a plain 5-box Leitner system (`reviewRotationItem` — correct promotes one box capped at 5, incorrect resets to box 1; `LEITNER_INTERVAL_DAYS` maps box → re-review interval in days), `selectDailyRotationQueue` (due-for-review items first, sorted earliest-due-first, then never-seen items, capped at `DEFAULT_DAILY_ROTATION_TARGET` = 8), and UTC-based `rotationDateString`/`nextRotationDateString`/`nextStreakDays` for the daily cadence and a cosmetic-only streak counter. Deliberately excludes evidence-organizing (multi-part placement, no single binary correct/incorrect signal a Leitner box needs) and saq ("complete" means submitted, not graded, per 49A) — only the three quest types with a clean per-item correct/incorrect result feed the rotation, matching the standing rule that this must never confer a graded-assessment advantage: reviewing here is explicitly framed to the student as "not a graded assessment," touches no unlock/badge/grade state, and only optionally feeds the already-formative `skillMastery` record the same way Practice Check already does. New `progress.archiveRotation` (`chronicle-progress-store.js` `DEFAULT_PROGRESS` + `readProgress()` merge): `itemStates` keyed by `"<questType>::<questId>"`, plus today's drawn `queue`/`queueDate`/`position` and a `streakDays`/`lastCompletedDate` pair. New `rotationItemPool()` (`main.js`) gathers every mcq/sequencing/hipp item from `PRACTICE_CHECK_QUESTS` for units the player has already unlocked (`isUnlocked(caseId)`), resolved through the existing `resolveQuestSlot` so a classroom's teacher-swapped content is reflected here too — a fresh save with only case-001 unlocked gets exactly today's 5-item pool (3 mcq + 1 sequencing + 1 hipp), all under the 8-item daily cap. New `archiveRotationScreen()` (reusing `renderQuest`/`gradeQuest`/`questAnsweredAny`/`isQuestComplete` exactly like `practiceCheckScreen()` does, so answering an item writes to the same `progress.questResponses` bucket Practice Check uses) shows one item at a time with an explicit "Next →"/"Finish rotation →" button — deliberately a one-shot click-triggered commit of the Leitner review (`data-action="rotation-next"`), rather than trying to detect "answered" via the idempotent equality-gate pattern `recordSkillOutcomes` uses elsewhere, since a spaced-repetition schedule genuinely needs to advance on every distinct review event (including re-reviewing an item already correct once before), which an equality-gate would silently skip. New entry point: "The Archive Rotation →" button in the Preservation Case dialog (`unitOneBadgeCaseMarkup()`), beside the existing "Skill Mastery Record →" button, both wired the same way (`hubDialogueId = null` reset, then a `currentScreen` change). No new CSS — reuses `.mastery-shell`/`.activity-copy`/`.activity-board`/`.quest-practice-item`/`.quest-practice-summary`, all already styled. **Verified:** new `tests/unit/spaced-repetition.test.js` (21 tests) plus 2 new `chronicle-progress-store.test.js` merge/corruption-guard tests — `npm test` 515/515 (up from 492 baseline); lint/build clean (same 5 pre-existing warnings only); `npm run validate:content` untouched by this phase (no content schema changes — the rotation reuses existing content as-is). New `tests/e2e/archive-rotation.spec.js` (4 tests): a fresh save's queue is generated correctly from the unlocked pool with "Next" gated on answering; answering and advancing records a Leitner review and moves to the next item; completing every item in a (seeded, single-item) queue shows the completion state and starts a streak at 1; a save with no unlocked case shows the empty state rather than crashing. `npx playwright test` 38/38 including all 20 visual-regression baselines byte-identical (no CSS was touched).
- **49D — DONE.** CED alignment tagging (Period/Key Concept/Theme/Historical Thinking Skill), surfaced read-only on Manage Content's mission cards (Teacher Dashboard → Units tab). New `apps/web/src/content/ced-taxonomy.js` exports the College Board CED's real, verbatim 7 themes (NAT/WXT/GEO/MIG/PCE/WOR/ARC) with their full labels. New `CedAlignmentSchema` in `unit.schema.js` adds a required `case.ced: {period, keyConcepts, themes}` field — `period` is `1-9` (not narrowed to `1-3`) since Chronicle's own roadmap states it plans to grow into the CED's full 9 periods; `keyConcepts` entries are regex-validated to look like `"1.2"`; `themes` is a 1-3-entry array drawn from the real 7-code enum. Deliberately did NOT add a 4th authored field for "Historical Thinking Skill": `apps/web/src/engine/ced-alignment.js`'s new pure `skillsForQuestSlots()` instead re-derives it from the `skillCategory`/`rubric.skillCategories` tags mcq/sequencing/hipp/evidence-organizing quests already carry (Phase 49B), so it can never independently drift from what a case's actual content exercises — `main.js`'s new `caseHistoricalThinkingSkills(caseId)` feeds it `officialQuestSlotsForCase(caseId)` (already existed, unchanged) and `SKILL_CATEGORIES`' own fixed order. All 9 cases across all 3 campaign files were tagged with real, individually-reasoned Period/Key-Concept/Theme values grounded in each case's actual historical content (e.g. case-001 → KC 1.1+1.2, themes MIG+WOR; case-005's Triangle Ledger → KC 2.2, themes WXT+WOR+MIG; case-008's Founding Debate → KC 3.2, themes PCE+NAT) — reasoning for each case is inline in the content files as a comment beside its `ced` field. New cross-reference check `checkCasePeriodMatchesUnit()` (`cross-reference.js`, wired into `scripts/validate-content.js` as a new group) catches a case tagged with a period that doesn't match its own unit's period string, since a single Zod schema can't see across that case/unit boundary. New `cedAlignmentMarkup()`/`manageContentMissionCardMarkup()` change in `main.js` renders one chip row per mission card (Period + Key Concepts in a muted tone, Themes in gold, derived Historical Thinking Skills in the default tone) via the existing `chip()` primitive — no new component, and a new 6-line `.manage-content-ced-row` CSS rule (flex-wrap) is the only styling addition. **Verified:** new `tests/unit/ced-alignment.test.js` (7 tests) and 4 new `content-cross-reference.test.js` tests for `checkCasePeriodMatchesUnit`, plus 6 new `content-schemas.test.js` tests for `CedAlignmentSchema`'s validation rules (missing field, malformed key concept, invalid theme, too many themes, period-9 headroom) — `npm test` 531/531 (up from 515 baseline); `npm run validate:content` clean, 83 groups (up from 82), including the new cross-reference group; lint/build clean (same 5 pre-existing warnings only); `npx playwright test` 38/38 including all 20 visual-regression baselines byte-identical (Manage Content has no baselines to move — a pre-existing gap this phase didn't create, see Phase 47's sub-phase notes). **Known verification gap, stated plainly rather than glossed over:** Manage Content's mission-card rendering itself (where the new CED chip row actually appears) has no committed e2e coverage and could not be manually browser-verified in this session — reaching it requires a signed-in teacher session (`dev-fake-teacher`), which calls a real Supabase project this environment has no reachable/seeded credentials for; this is the same pre-existing "2 of 11 e2e scenarios deferred, need a dedicated Supabase test project" gap Phase 29 already documented, not a new one. The markup was verified by construction (lint/build catch template-string syntax errors, and `chip()`/`caseHistoricalThinkingSkills()` are exercised indirectly by the passing unit and cross-reference tests) but not visually.
- **49E — DONE.** The DBQ capstone ("Chronicle Dossier"): a real `dbq` quest type in `QUEST_TYPES` (`apps/web/src/quest-types/history/dbq-quest.js`), following the exact same "complete means submitted, not AI-graded" resolution 49A established for `saq` — `gradeDbqQuest` is a synchronous length-floor check (`DBQ_MIN_RESPONSE_LENGTH = 400` characters against the trimmed response), and the real AI Archive Evaluator round trip (`api/_lib/rubrics.js`'s pre-existing `RUBRICS.dbq`, unused until now) composes separately at the call site rather than inside the quest type. `DbqQuestSchema` (Zod) requires 4-9 real `documents` (`id`/`label`/`attribution`/`date`/`excerpt`), a `prompt`, and a `rubric` string, with a `superRefine` duplicate-document-id guard; `DbqQuestListSchema` adds the analogous duplicate-quest-id guard. New `buildDbqEvaluationRequest()` (`engine/evaluator-requests.js`) concatenates the documents into one stimulus string and reuses the same `taskType: "dbq"` rubric with a `dbq-quest-${quest.id}` taskId, mirroring `buildSaqQuestEvaluationRequest`. In `main.js`, generalized the SAQ-only evaluator UI/click-handler block in `archiveChallengeQuestCard()` into one `isWrittenResponseType` (`questType === "saq" || questType === "dbq"`) branch sharing a single `evaluate-written-quest` action that dispatches to the correct request builder — one boolean and one action name now cover both written-response types instead of duplicating the button/feedback markup. Real content, reused rather than invented: a genuine 7-document DBQ (`unit-03-archive-common-cause-dbq` in `UNIT_03_ARCHIVE_DBQ_QUESTS`) asking students to evaluate how far the Revolution's natural-rights rhetoric actually extended to Native, enslaved, free Black, and female Americans 1763-1783 — every document is either already a cited case-007 source or a real, verifiably-quoted primary source added for this quest (Pontiac's 1763 council speech, Dickinson's *Letters from a Farmer* Letter II, Patrick Henry's "give me liberty" speech, Phillis Wheatley's 1773 elegy, Dunmore's 1775 proclamation, Prince Hall's 1777 Massachusetts petition, Abigail Adams's "Remember the Ladies" letter), added as a second Unit 3 bonus Archive Challenge alongside 49A's SAQ (`UNIT_03.archiveChallenges[]`) — same zero-regression-risk rationale as 49A: a unit-level bonus addition, not a swap into any existing case's tested `archiveChallenge` slot. **Found and fixed a real design bug during e2e verification, not just a passing check:** an initial `dbqPartialSuccess` implementation returned `true` whenever a response had *any* text but hadn't reached the length floor, intending to mirror evidence-organizing/sequencing's partial-credit convention — but a new e2e test (typing a 21-character response and asserting no "success"-styled feedback, matching 49A's SAQ incomplete-gating test) caught that this was the wrong precedent: evidence-organizing/sequencing's `partialSuccess` means "got the substantive part right, one step left" (e.g. everything placed correctly, reflection pending), not "the user has typed something." A half-finished essay under 400 characters hasn't cleared any such bar, so tagging it with the green "success" CSS class was misleading. Fixed to unconditionally return `false`, matching `saq`/`mcq`/`hipp`'s "no invented partial-credit state" convention instead — corrected both the implementation and its `tests/unit/quest-type-dbq.test.js` coverage. **Verified:** new `tests/unit/quest-type-dbq.test.js` (19 tests) and 3 new `evaluator-requests.test.js` tests for `buildDbqEvaluationRequest`, plus 2 new `quest-types-index.test.js` dbq-registry-wiring tests — `npm test` 555/555 (up from 531 baseline); `npm run validate:content` clean, new groups all "ok" (`unit-03-quests.js: UNIT_03_ARCHIVE_DBQ_QUESTS` schema check, `cross-reference: dbq quest ids`, plus a previously-missing `cross-reference: saq quest ids` group noticed and added in the same pass); lint/build clean (same 5 pre-existing warnings only). New `tests/e2e/archive-challenge.spec.js` DBQ cases (2 tests, banked alongside 49A's SAQ pair): drafting a ≥400-character response with all 7 real documents visible completes the challenge and surfaces the evaluator button; a too-short response stays incomplete with no success styling and no evaluator button (the case that caught the `dbqPartialSuccess` bug above). `npx playwright test` 40/40 including all 20 visual-regression baselines byte-identical (DBQ reuses `.quest-document`/`.quest-reflection`/`.rubric-note`/`.quest-reflection-counter`/`.quest-prompt` verbatim — no new CSS).

**Ongoing track:** Chronicle covers Periods 1–3 of 9; content velocity, not architecture, is the constraint.

---

# Phase 50 — Odysso: highest-ROI moves as a learning platform (not started)

**Research grounding:** SSO (Google/Microsoft), rostering (Clever/ClassLink), and LMS push (Google Classroom/Canvas/Schoology) are described as non-negotiable at K-12 scale ([EdTech Insiders](https://edtechinsiders.substack.com/p/the-new-era-of-k12-rostering-what), [Clever](https://www.clever.com/blog/2022/07/google-classroom-rostering)).

- **50A** — Turn on Google sign-in for teachers. Code already exists (`remote-auth-repository.js`) but no Google provider is configured in any Supabase project — close to free.
- **50B** — Kill the manual roster via Google Classroom import.
- **50C** — One identity across Odysso and Chronicle: point Odysso's placeholder sign-in at the same Supabase project.
- **50D** — Assignments with due dates + class outcome reporting.
- **50E** — Second-subject proof: a written go/no-go decision, not a `packs/` build — no forcing function exists yet per `POST-MINIMAL-ARCHITECTURE-REASSESSMENT.md` §10.

**Verification:** the claim/login/provision/reissue/evaluate/grade round trip and RLS cross-tenant isolation — outstanding since Phase 22, still the largest untested surface in the product.

---

## Program-level verification

Before every commit: `npm run test` (555 baseline as of Phase 49E), `npm run lint` and `npm run build` unchanged from baseline, `npm run validate:content` when content changed (86 groups as of Phase 49E), `npm run test:e2e` including the 20 visual baselines at 1366×768 (40 specs as of Phase 49E), and a live pass on the specific interaction changed. Push to `main` after verification (standing permission; Vercel auto-deploys).

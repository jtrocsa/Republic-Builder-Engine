# Decision 0012 — Unit 1 correction pass

This correction pass stabilizes the Unit 1 gameplay loop after playtesting feedback.

- Restore the preferred PNG field sprites and retain them as the field-art baseline.
- Move only the player element between logical tiles; do not rerender the field on every keypress.
- Route each completed case through a visible Codex-to-Archive upload moment, then return directly to the map.
- Make the Exchange Ledger source-driven: each record has a distinct primary-source reading and a distinct evidence-based multiple-choice question.
- Make Empire’s Foundations a real drag-and-drop evidence ordering activity rather than a series of dropdowns.
